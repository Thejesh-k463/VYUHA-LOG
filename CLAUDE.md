@AGENTS.md

**Read `VYUHA-STATE.md` (repo root) before starting work.** It is the handoff contract —
verified current state, what shipped, what is blocked, the open decisions, and a routing
table telling you which file answers which question. It is referenced rather than imported
so it costs nothing until you read it.

On conflict: `AGENTS.md` wins over `VYUHA-STATE.md`, and the code wins over both.

Before `/clear`, update `VYUHA-STATE.md` with verified numbers — not recalled ones.
