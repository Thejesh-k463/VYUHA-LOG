# VYUHA-STATE — archived §0 blocks

Superseded §0 START HERE blocks, byte-for-byte, newest first. Nothing here is state; `VYUHA-STATE.md` §0 is.

---

## §0 as it stood until 2026-09-18 (replaced by the thirteenth session)

## §0 START HERE — reconciled 2026-09-17 IST at code `b2af3d8` (fix wave 2O, CI run 35125379974 SUCCESS 6/6; its scoped re-check RAN — 42 / 0 / 0, 16 findings; fix wave 2P DESIGNED and design-reviewed, NOT built; **the owner's instruction 2026-09-17: build 2P, then the release ritual — no more re-check loops**) by the eleventh session; the commit that landed this block is the next one in `git log`

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** (hub copy vs the project copy) and rots; it now carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/NEXT-SESSION-CONTINUATION.md` §0 — research-pack specifics for the item you are picking up (audit known-inputs, ruling tables, ledger pointers). It points back here.
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` · `package.json` `"version"` · `gh run list --limit 3` · the RAW vitest line (§0.3).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: `06-ANSWERS.md` newest table LAST; `docs/DECISIONS.md` newest entries at the END (its header still says newest-first, but every entry since 2026-09-10 is appended — grep `^## 2026-`, never read it whole) — binding, never re-ask one.
5. §3 of this file for where any other answer lives.

**The one-line state (verified 2026-09-17 at code `b2af3d8`; HEAD = the eleventh-session docs commit on it):** published release = **v4.2.0** (tag `9da7bc8` → `b488dde`).

- **⛔ OWNER INSTRUCTION 2026-09-17 (06-ANSWERS "Owner instruction 2026-09-17"), binding for the rest of 4.3.0:** build fix wave 2P,
  then go STRAIGHT to the release ritual (row 2). NO scoped re-check of 2P, NO wave 2Q, NO wave 3 inside 4.3.0 (deferred to the
  release after it), minimal agents (one builder at a time, one seam pass, one gate), orchestrate on Opus. The audit → fix wave →
  re-check → next wave loop ran from 2L to 2O, each re-check finding a new introduced silent wrong number; the owner ended it on cost.

- **In progress: the v4.3.0 fix work from the release-level audit.** The earlier commits, each CI 6/6:
  - switch-off `9e0e16f`, wave 1 `e0e6d90`, wave 2 `a7e9288`, 2R + 2F `ec89bbd` (fifth session); 2G `3feb22f`, 2H `b6c1029` (sixth);
  - waves 2I + 2J + 2K `cd1ab70` (CI 34976760609), wave 2L + M1 `8ff4288` (CI 34988232943), the guards `2bb0fa0` + `4312672` (CI 35007445603), seventh session;
  - **fix wave 2M `e23b96c`** (CI 35022758611 SUCCESS 6/6), eighth session: the three guard findings (G-G2-1, G-G3-1, G-G3-2),
    design-reviewed FIRST (`vyuha-design-reviewer` graded all three REVISE; adopted with three narrowing decisions), two builders
    on disjoint sets, then TWO seam rounds that found five more defects (D1–D5 — D1/D2 the candidate-set disagreement the reviewer
    had named, D4 a 2M regression caught before the gate, D5 a legacy-date reader) and fixed them in-session; every `it.fails` pin
    flipped — **expected fail 0**. DECISIONS "2026-09-16 — v4.3.0 fix wave 2M built". Reports `18-FIX-WORK-4.3.0/wave2h-reports/wave2m-*.md`.
  - **fix wave 2N `f9a1a6b`** (CI 35086623142 SUCCESS 6/6), ninth session: the 2L re-check's 19 product + 2 medium
    test findings as designs D1–D8 (`18-FIX-WORK-4.3.0/wave2n-designs.md`), `vyuha-design-reviewer` FIRST (7 REVISE, all adopted —
    `wave2n-design-review.md`), two owner rulings before code (06-ANSWERS "v4.3.0 fix-wave 2N rulings": an unpriced MTF row accrues
    nothing until recorded; a partly sold row keeps accruing on the whole leg, labelled), FOUR sequential Opus builders (B-IDENTITY,
    B-DATES-CHARGES, B-MTF, B-ASK — reports `wave2h-reports/wave2n-B-*.md`, each red-first), ONE seam round to convergence
    (`wave2n-seams.md`: fixF 47 → 60, seven pins flipped, no seam defect), e2e z-live-desk 9/9 locally. DECISIONS "2026-09-16 —
    v4.3.0 fix wave 2N built". Its scoped re-check RAN in the tenth session (next bullet).
  - **fix wave 2O `b2af3d8`** (CI run 35125379974 **SUCCESS 6/6** — conclusion success, six jobs success, read after `gh run watch`;
    the pages-build-deployment run 35125378826 on the same sha is the docs site, not CI), tenth
    session: the 2N re-check (42 / 0 / 0, 18 findings — 16 product, 2 test-low) folded into ONE wave with the backlog audit's three
    live defects — 20 designs (D1–D3 `18-FIX-WORK-4.3.0/wave2o-designs.md`, D4–D19 `wave2o-designs-recheck-draft.md`, D20 found by
    the MTF builder), `vyuha-design-reviewer` FIRST twice (`wave2o-design-review.md` D2 REVISE; `wave2o-design-review-recheck.md`
    D4, D6, D7, D10 REVISE — all adopted), two owner rulings before the MTF builder (06-ANSWERS "v4.3.0 fix-wave 2O rulings":
    closed staged MTF rows keep their earlier estimate; a stated funded amount is apportioned by tranche value), FOUR sequential
    Opus builders (`wave2h-reports/wave2o-B-{METRICS-PALETTE,IDENTITY,MTF,DATES}.md`, each red-first; B-MTF and B-DATES resumed once
    at the 150-turn limit), ONE seam round (`wave2o-seams.md`: `tests/seams-v43-fixH.test.ts` NEW, 18 cases; fixF F1(a) flipped for
    D11) that found TWO seam defects — one pre-existing (`DASH_FIELDS` / `LENS_FIELDS` omitted the three columns `edgeMeasurable`
    reads, so the dashboard and /lenses counted an unpriced sale as a priced WIN) and one D20-introduced (every staged notes-only
    save refused) — fixed in-session by two seam-fix builders (`wave2o-S-FIX.md`, `wave2o-S-FIX2.md`: the staged preview ≡ save gap
    too). DECISIONS "2026-09-16 — v4.3.0 fix wave 2N: the scoped re-check" and "… fix wave 2O built". **Its scoped re-check RAN in the eleventh session (next bullet).**
  - **The 2O scoped re-check (eleventh session, 2026-09-17; NOT a code commit):** `18-FIX-WORK-4.3.0/recheck-wave2o.js` (pre-wave
    `5e537b3` = code `f9a1a6b`, wave `b2af3d8`, prefix `zzprobe-rc9-`), FIVE probe-capable Fable reviewers — metrics-palette,
    identity, mtf-staged, dates-charges-ask, seams — `wave2o-recheck.json`: **42 CONFIRMED / 0 / 0, 17 raw → 16 unique findings
    (12 product, 4 test-low)**, every probe deleted, tree untouched. **2O introduced ONE silent wrong number** (mtf-staged#0: D20's
    hand-back to `rebuildStagedTrade` has no `isOpen` guard, so a notes-only save or a leg-note edit on a CLOSED legacy staged MTF
    row RELEASES the margin-config estimate — +₹80.20 net on a closed trade — breaching the owner's 2O row-1 ruling; D20 was designed
    mid-wave inside a builder report and never reviewed) + 1 medium (the job's new staged branch has no per-row try/catch: one broker
    with no eq_mtf epoch silently stops accrual for every later row) + 3 low + 2 cosmetic; 5 pre-existing (a tiered-slab silent
    wrong number in `priceLegs` — Dhan ₹416.43 apart on 8 L over 19 days; a ghost IPO link invisible to Data Quality; three date
    lows/cosmetics). DECISIONS "2026-09-17 — v4.3.0 fix wave 2O: the scoped re-check" (two new rules: a mid-wave design is a design
    and gets the review; a quoted seam red that cannot occur is a test finding). **Fix wave 2P DESIGNED: `wave2p-designs.md`
    D1–D12** (a Fable drafter, consumer + writer sets grep-verified; two sequential builders B2P-MTF-DATES D1–D9 and B2P-IDENTITY
    D10–D12; no owner question) **and design-reviewed: `wave2p-design-review.md`** (7 BUILD, 5 REVISE — D3, D4, D7, D8, D11 — every
    REVISE adopted; D7 kept narrow, D11 gains `ipo_record_ghost`). **NOT built.** No jsdom / @testing-library exists on this tree
    (the render probes used `renderToStaticMarkup` + stubs). Entry gate re-run first: 433 / 9,544 / 35 (the +22 = owner-file cases).
  - `lib/import/commit.ts` still carries no auto-close wiring against v4.2.0 (re-checked at `b2af3d8`: the D20 staged guard and the
    `lib/queries/staged.ts` import are not auto-close; 0 hits for planLotCloses / withLotCloseNote / splitByRemainder).
- **Scoped re-checks:** wave 1 43 / 3 partial → wave 2 47 / 1 → 2R + 2F 34 / 0 / 0 → 2G all CONFIRMED → 2H 49 / 1 / 0 → 19 findings →
  wave 2I → 2I/2J/2K 58 / 0 / 0 → 26 findings (15 product, 11 test) → wave 2L → 2L (over `e23b96c`, post-2M): 41 / 0 / 0 → 28
  findings (19 product, 9 test) → fix wave 2N `f9a1a6b` → **2N (over `f9a1a6b`): 42 / 0 / 0 → 18 findings (16 product, 2 test-low)
  → fix wave 2O BUILT `b2af3d8` → **2O (over `b2af3d8`, 2026-09-17): 42 / 0 / 0 → 16 unique findings (12 product, 4 test-low)
  → fix wave 2P DESIGNED + reviewed, NOT built; the loop ENDS here by the owner's instruction** (`wave2o-recheck.json`; DECISIONS
  "2026-09-17 — v4.3.0 fix wave 2O: the scoped re-check"). Graded by INTRODUCED regressions: 2L one silent wrong number (identity#0) + 1 medium + 4 low +
  2 cosmetic; 2M one silent wrong number (counted-once#0) + 1 medium + 1 cosmetic; **2N one silent wrong number (mtf#0: the staged
  ladder is a FIFTH writer of `mtf_interest` that Q-A never reached — the writer sweep stopped at commit.ts and the job because the
  ladder writes under a helper's name) + 4 medium + 3 low + 4 cosmetic + 2 test-low; 4 pre-existing (one a silent wrong number,
  mtf#1: `priceLegs` ignored the STATED funded amount).** Three consecutive waves each introduced a silent wrong number → the
  sequential rule stands for the rest of the release, and DECISIONS records the new writer-sweep rule (grep the column, the field AND
  every helper that produces it, across lib/queries/*, lib/jobs/*, lib/import/*, lib/trash.ts, app/api/**, app/**/actions.ts).
  2O's seam round found ONE introduced product defect + one introduced preview gap (both D20's), fixed before the gate — 2O's own
  scoped re-check is the measurement that counts.
- **Wave 3 is DEFERRED past 4.3.0 by the owner's instruction of 2026-09-17** (ruled into 4.3.0 on 2026-09-15, planned and researched;
  the 4.3.0 release notes and install guide claim NONE of it — no ETF tax heads, no calendar months, no tax-defect fixes). The plan
  stays for the release after 4.3.0: `18-FIX-WORK-4.3.0/plan-wave3.json`
  (3a W3-ETF 26 files, 3b W3-TAX 42 files, disjoint; 13 seam contracts; every pin that moves; the AGENTS.md "Bundled ETF list"
  text; `open_questions: []`). Its preconditions (a fix wave committed with CI 6/6; no probe file in the tree) hold at `b2af3d8` once
  its CI reads 6/6 and the 2O scoped re-check is clean; 2N and 2O moved commit.ts (+1,116 / −90 against v4.2.0), the preview route,
  data-quality.ts, trash.ts, staged.ts, mtf-accrual.ts, positions.ts, ipo.ts, ipo-link.ts, the /ipos route, cross-source.ts,
  metrics.ts, lib/queries/trades.ts, edge/page.tsx and tax-itr.ts's neighbours, so RE-MEASURE every line number the plan cites.
- **Gate on `b2af3d8`:** `npm run verify` — **433 files / 9,522 passed / 35 skipped** (the raw line; expected fail 0; typecheck 0
  errors; lint 0 errors / 3 pre-existing warnings; `next build` compiled); **re-run 2026-09-17 on the SAME code: EXIT 0, 433 /
  9,544 / 35** — the +22 are owner-broker-file cases (`tests/helpers/owner-broker-files.ts` enumerates
  `VYUHA/BROKER-FILES-FOR-TESTING/` by filename pattern; six real exports filed 2026-09-16/17), so CI stays at 9,522; `e2e/z-live-desk.spec.ts` 9 / 9 (45.6 s) locally over 2O's
  `components/live/*` changes — after clearing `.next` (§0.3 V2 FAIL-I).
- **Backlog research (ninth session):** `LIVE-DESK-RESEARCH/19-BACKLOG-RESEARCH-2026-09-16/` — 12 read-only packs + a probe-capable audit (`00-INDEX.md` first): 18 backlog lines in §8 described SHIPPED code, 6 duplicate proposals, 13 stale lines, 3 live product defects — **the three defects are FIXED in 2O (`b2af3d8`)**, and the audit's §5 CORRECTED backlog is now the text of §0.1 rows 8 and 9 below (folded in the tenth session after the owner saw the stale lists recited; §8's own bullets still carry the banner and are NOT yet struck line by line — a docs task). Four NEW owner features are recorded verbatim there (resizable sidebar width, default section order per page + user-movable sections, India-tuned metrics with simpler explanations). ~150 owner questions across the packs — asked in groups of ≤10 only when an item is scheduled.
- **NOT bumped, NOT tagged.**
- **Guards + the design-review mechanism `2bb0fa0`** (after the owner asked for "zero repeated errors"): four invariant tests
  (AGENTS.md "Invariant guards for identity, IPO-link, Trash, merge and MTF work"), `.claude/agents/vyuha-design-reviewer.md`,
  the consumer sweep in `vyuha-builder`, red-harness-first in `vyuha-seam-tester`, design review + introduced-regression grading in
  the `vyuha-audit` skill, the global `planner` (DECISIONS "why the v4.3.0 fix waves regressed"). The three defects they found on
  `8ff4288` are **fixed in 2M**; the scanner's pre-fix texts are committed fixtures since `4312672` (a test never reads git history).
  First use of the mechanism, measured: the design review turned all three decided designs into buildable ones before a line was
  written; the seam pass still found five defects after the builders, one of them a regression of the wave itself — the mechanism
  caught it before the gate instead of a re-check one wave later.
- **Recorded, not built (tenth session; each with its home):** the editor's four risk fields (`slPlanned` / `trailingSl` /
  `targetPlanned` / `riskAmount`) are re-derived by `rebuildStagedTrade` after an editor save on a STAGED parent — the ladder is the
  source of truth (invariant 4); the editor should show them read-only for a staged row (a UI follow-up); `lib/queries/ipos.ts:71,86`
  treat a WHITESPACE-only exit date as unpriced (`' '` pinned as the gap); D20's refusal sentence has a second copy in the preview
  route (`LADDER_REFUSAL`, pinned equal — one export owed); a staged parent whose stored heads are ZERO previews those zeros; the
  accrual job re-prices every open staged eq_mtf row per /equity render (N+1, idempotent, pinned); the two 2N test-low findings
  (mtf#6 the Live Desk's D7 half pinned by source only; seams#2 F18/F40 restore `charge_config` outside a `finally`); the ten seam
  boundaries with no render case (`wave2o-seams.md` §1, incl. the Add form's preview door — 2N's #6 — still estimating); no e2e
  press of Control+Shift+K (D3's rejected alternative). DECISIONS "… fix wave 2O built" has the full list.
- **Side task, twelfth session (2026-09-17, no product code):** the owner's options-strategy log (`Trade-log-book.xlsx`, 42
  closed same-day long stock-option trades) seeded into the LIVE desktop journal as account #3 "OPTIONS STRATEGY" (Dhan,
  active capital ₹1,00,000) via `scripts/seed-options-account.ts` → `commitManualTrade`; gross ₹78,084.38 · charges ₹2,951.63 ·
  net ₹75,132.75; migration 0071 applied to the live file by ruling (pre-write copy in `data/smoke-0071/`); a 3840 × 2748
  full-dashboard capture delivered for advertising. DECISIONS "2026-09-17 — The owner's options-strategy trade log seeded".
  The 5L capture was then RE-RENDERED (not retouched) from a staging copy sized at 4 lots per trade on ₹5,00,000: net
  ₹3,06,434.27 · 33 W / 9 L · charges 1.89% (`seed-options-account.ts --lots 4`; 5 lots would flip the ₹17.50 PHOENIXLTD trade
  to a win → 81.0%). The journal still holds one lot and ₹1L.
- **Product fix, twelfth session (the owner saw "8 wins · best 11W" on that dashboard; the entry order says 7 · 10):**
  `closedSorted` (`lib/analytics/metrics.ts`) sorted on `sellDate` alone over NEWEST-FIRST input, so streaks and drawdown ran
  every day backwards — wrong on every same-day book since the KPI card existed, `/lenses` too. Fixed with a `sellDate` →
  `exitTime` → `id` tiebreak (`DASH_FIELDS` gains `id`, `exitTime`; `LENS_FIELDS` keeps its /trades-wire-shape pin, id only);
  `tests/closed-sorted-tiebreak.test.ts` (4 cases) pins it. DECISIONS "2026-09-17 — Streaks and drawdown walked every trading
  day BACKWARDS". Goes into the 4.3.0 release notes (one line, "streak and drawdown order inside a day"). Commits `0bf15bf`
  (the fix) + `662b568` (README counts) — CI run 35252324892 SUCCESS 6/6; **gate re-run on this code: `npm run verify` — 434 files / 9,547 passed / 35 skipped
  on the owner's machine (its one red was the README count, fixed in the follow-up; CI expects 9,526)**; README says 9526 / 434.
- **⛔ OWNER INSTRUCTION 2026-09-17, twelfth session (06-ANSWERS "Owner instruction 2026-09-17 IST (chat, twelfth session…)"),
  BINDING — adds to the eleventh-session instruction below:** (1) **the Option Strategies plan is ARCHIVED** — every open line of
  `13-OPTION-STRATEGY-CATALOGUE.md`, row 5 (live feed pricing option underlyings) and row 9's "option-seller depth r3" are CLOSED;
  what shipped in wave 2 stays; **v4.3.0 gains ONE single strategy: the owner's own signal system** (the seeded 42-trade log —
  CE breakout at resistance / PE breakdown at support, watchlist tiers, ΔOI unwind, T1 +50% / T2 +100% / SL −35%, closed by end
  of day), expanded later on DEMAND only; **its full details arrive from the owner before any design.** (2) **ONE session** does,
  in order: fix wave 2P → the single strategy (design → `vyuha-design-reviewer` → one builder → seam pass → gate) → the release
  ritual (bump, build, push, CI 6/6, `release` skill, steward) → STOP for the owner's "tag".
- **Next (the owner's two instructions, binding; the paste block is `NEXT-SESSION-CONTINUATION.md` §0, rewritten in the twelfth
  session — old block archived as §0-prev11):** 2P → the single strategy (details pasted by the owner; ONE question group; design →
  `vyuha-design-reviewer` → one builder → seam pass → gate) → the release ritual, all in ONE session on Opus. The capture tool is
  `scripts/shoot-dashboard.mjs`; the seed tool `scripts/seed-options-account.ts` (never re-run against the live journal without
  `--dry`). CI on `c97fd4c` run 35217890785 SUCCESS 6/6 (after one macOS e2e re-run: a `waitForLoadState` timeout on a docs-only
  sha). The eleventh-session wording of this bullet follows for the 2P and release-ritual detail: **build fix wave 2P** (`NEXT-SESSION-CONTINUATION.md` §0 NEXT item 1:
  `wave2p-designs.md` D1–D12 as revised by `wave2p-design-review.md`; B2P-MTF-DATES then B2P-IDENTITY, one `vyuha-builder` at a
  time on Opus, red-first with the four guards; ONE seam pass; gate; README counts; commit; push; CI 6/6) → **row 2, the bump and
  the release ritual** (the release notes state the 2N + 2O + 2P lines — DECISIONS "… fix wave 2O built", the four `wave2o-B-*.md`
  "Release-notes" sections, `wave2p-designs.md` "Release-notes sentences" — with every auto-close AND every wave-3 / ETF / tax claim
  dropped) → STOP for the owner's "tag". NO 2P re-check, NO wave 2Q, NO wave 3. Decisions: the eleventh-session entry dated
  2026-09-17 at the END of `docs/DECISIONS.md`.

*Superseded one-line state of 2026-09-16 at `b2af3d8` (tenth session), kept for the record:* fix wave 2O `b2af3d8` (CI 35125379974
6/6); its re-check NOT run; wave 3 planned; gate 433 / 9,522 / 35; e2e 9/9; NOT bumped, NOT tagged.

*Superseded one-line state of 2026-09-16 at `f9a1a6b` (ninth session), kept for the record:* fix wave 2N `f9a1a6b` (CI 35086623142
6/6); its re-check NOT run; wave 3 planned; gate 430 / 9,410 / 35; e2e 9/9; NOT bumped, NOT tagged.

*Superseded one-line state of 2026-09-16 at `e23b96c` (eighth session), kept for the record:* fix wave 2M `e23b96c` (CI 35022758611
6/6); the 2L re-check 41 / 0 / 0 → 28 findings → wave 2N NOT designed; gate 427 / 9,323 / 35; NOT bumped, NOT tagged.

*Superseded one-line state of 2026-09-15 at `2bb0fa0` (seventh session), kept for the record:* waves 2I + 2J + 2K `cd1ab70` and 2L + M1
`8ff4288` (CI 6/6 each); the invariant guards + the design-review mechanism `2bb0fa0` / `4312672`; the guards found three defects
pinned `it.fails` → fix wave 2M before the 2L re-check; gate 426 / 9,218 | 5 expected fail | 35; NOT bumped, NOT tagged.

*Superseded one-line state of 2026-09-15 at `b6c1029` (sixth session), kept for the record:* waves 2G `3feb22f` and 2H `b6c1029`
(CI 6/6 each); 2G's re-check all confirmed, 2H's not run; the wave-3 rulings (ETF tax heads fully fixed, calendar months,
straddling rows per row + DQ, four pre-existing tax defects, MTF interest out, the STT deduction fixed, buy-side charges a note);
gate 408 / 8,644 / 35; NOT bumped, NOT tagged.

*Superseded one-line state of 2026-09-15 at `ec89bbd` (fifth session), kept for the record:* the fix work through waves 2R + 2F (`ec89bbd`, CI 34895595282 6/6); the 2R + 2F re-check 34 / 0 / 0 with 17 new findings → wave 2G; owner rulings R90 = the bundled NSE ETF list, all four out-of-list defects into 4.3.0, R13 = the inline official-close notice; gate 397 / 8,416 / 35; NOT bumped, NOT tagged.



---

# ARCHIVED 2026-09-21 — the §0 block written by the thirteenth session (v4.3.0 PUBLISHED), replaced by the fourteenth session's v4.4.0 block; byte-for-byte below

## §0 START HERE — reconciled 2026-09-18 IST by the thirteenth session: **v4.3.0 is PUBLISHED** (tag `v4.3.0` = `c5c7c5b`; CI 6/6; release run 3/3; deep verify 3/3; `releases/latest` → v4.3.0; the updater feed serves 4.3.0 for six platforms) — **NOT yet installed off the build machine, WDSI not yet submitted: both are the owner's**; the commit that landed this block is the next one in `git log`

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** (hub copy vs the project copy) and rots; it now carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/NEXT-SESSION-CONTINUATION.md` §0 — research-pack specifics for the item you are picking up (audit known-inputs, ruling tables, ledger pointers). It points back here.
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` · `package.json` `"version"` · `gh run list --limit 3` · the RAW vitest line (§0.3).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: `06-ANSWERS.md` newest table LAST; `docs/DECISIONS.md` newest entries at the END (its header still says newest-first, but every entry since 2026-09-10 is appended — grep `^## 2026-`, never read it whole) — binding, never re-ask one.
5. §3 of this file for where any other answer lives.

**The one-line state (verified 2026-09-18):** published release = **v4.3.0** (tag `v4.3.0` → `c5c7c5b`, published 2026-09-18 12:22 UTC; before it v4.2.0 `9da7bc8`). The chain: `c5c7c5b` (Windows-CI test timeout) on `55f1fd3` (the monthly plan) on `160753f` (docs) on `7537ae2` (bump + release copy) on `20c08ff` (the Signal book) on `3abcb2c` (fix wave 2P). The block this one replaced is in `VYUHA-STATE-ARCHIVE.md` (byte-for-byte, dated).

- **What the thirteenth session did, in order (each commit CI 6/6):**
  - **fix wave 2P `3abcb2c`** — D1–D12 of `18-FIX-WORK-4.3.0/wave2p-designs.md` as revised by its review; two builders in sequence, one seam pass (4 cases,
    no product defect), one gate. CI run 35322863777 SUCCESS 6/6 on its second attempt: the Windows job hit two 5 s TIMEOUTS
    (`backup-roundtrip` "a pre-lots v3 envelope…" and one preview-matrix G3 cell) and passed on `gh run rerun --failed`. No re-check followed, by the owner's instruction.
  - **the Signal book `20c08ff`** — the ONE single strategy (06-ANSWERS last two tables + the owner's `AI_MODEL_STRATEGY_BLUEPRINTS.docx`): journal side only;
    migration **0072** `trades.signal_json` (versioned envelope, tombstone on a clear); a Signal section on the Add and Edit trade forms (prefill +30 / +60 / −25 %);
    the data fix `signal-notes-backfill-v1` (the four regexes matched 42 of 42 rows of the live journal, read-only); three pure analytics; the first tab on `/strategies`
    (table free, analytics Pro, withheld server-side). Design → `vyuha-design-reviewer` (3 BUILD, 4 REVISE, all adopted) → one builder → one seam pass, which found
    ONE product defect, **SIG-1** (a decimal-comma "14,48" stored as 1448), fixed before the gate. CI run 35328890550 SUCCESS 6/6.
  - **the bump `7537ae2`** — 4.3.0 in package.json / tauri.conf.json / Cargo.toml / the footer (`v4.3`); `Cargo.lock` 1 line, `package-lock.json` 2 lines by hand;
    the release copy (every auto-close, wave-3, ETF, tax, Greeks claim dropped; TWO database upgrades 0071 + 0072). CI run 35330899427 SUCCESS 6/6.
- **Gate (2026-09-18, on `7537ae2`):** `npm run verify` EXIT 0 — **439 files / 9,711 passed / 35 skipped** on the owner's machine; **9,689 expected on CI**
  (22 owner-broker-file cases run only locally — the earlier "21" was an off-by-one); README says 9689 / 439; typecheck 0; lint 3 pre-existing warnings.
- **Desktop build (2026-09-18):** `npm run desktop:build` EXIT 0 after `cargo clean --release` — the Rust cache still named the repo's PRE-MOVE folder
  (`VYUHA-TRADE JOURNAL-V1`) in every build-script output, so the first build after a folder move needs the release cache cleared (3 m 24 s to rebuild).
  `desktop-dist/.next/BUILD_ID` written 71 s after the build started; the bundle holds `signal-notes-backfill-v1`, "R on signal SL", `rate-card-refresh.mjs` and
  migration 0072. Both `.sig` files decode to key id **`4FF85F3BBE1DA21D`** = the `tauri.conf.json` pubkey. Client ZIP `release-packages/Vyuha_4.3.0_Client_Package.zip`;
  installer SHA-256 `3695B8090300518C85AD88B7BB1623CEB3E672F0970944D7D97F1794C40B1D2A` (35,652,661 bytes).
- **Release steward (2026-09-18):** READY TO TAG, no blocking items - 11 steps walked read-only: version sync, lockfile numstat, installer freshness, key ids, CI 6/6, revocations a prerelease with releases/latest at v4.2.0, the client ZIP (13 entries, no licence key, no macOS artefact, CHECKSUMS = the independent hash), and the claims audit over 8 surfaces with 0 unsubstantiated claims; one ADVISORY: the standing blurb 'Greeks across the book' (true of the Risk cockpit) sits three lines above the /strategies table that says it has no Greeks (docs/client/README.md:25, docs/sales/landing-page.html:460); release:verify --deep NOT run (nothing published yet); winget manifest for 4.3.0 comes after publish.
- **Owner rulings of this session (06-ANSWERS, the LAST two tables — binding):** journal side only (the scanner / zone engine / alerts are NOT VYUHA's);
  one JSON column; form section + notes backfill; three analytics; T2 = scale-out; first tab on `/strategies`, analytics Pro; **model S1 / S2 only — the tier is DROPPED**;
  prefill +30 / +60 / −25 with adherence judged on each trade's OWN levels; S1 = 5 CE + 5 PE, S2 = 4 CE + 4 PE (recorded, a scanner matter); tool-bound agents on Opus.
- **Recorded, not built (DECISIONS 2026-09-18, three entries):** 2P — the unreadable-lot throw in `closeStaleLot`, the D8 route cases that cannot separate their two
  halves, no seam case for D10 / D3-tiered; Signal book — a seeded row in Trash at upgrade time restores without a signal (Edit fills it), `num()` in
  `app/trades/actions.ts` still strips every comma for qty / price, no e2e spec and no Help Desk entry for the tab, the 2 % adherence tolerance is a decision.
- **FIX LIST for the next wave (owner, 2026-09-18 — "add this to fixes list"):** the `/lenses` **Outcome** tab's **Win rate** column is a tautology — the lens groups BY
  outcome (`lib/domain/lenses.ts:167`, `outcomeGroups`), so Winners always reads 100% and Losers 0% (the owner read the 0% as "no loss shown"; the loss IS the row's
  net, −₹12,173 on account #3). Correct arithmetic, useless column. Fix: on the Outcome lens only, replace Win rate with each group's SHARE of closed trades
  (33 of 42 = 79% / 9 of 42 = 21%, header "Share of trades"), or "—" if a share is ruled out; every other lens keeps Win rate. Pin it in the lens tests.
- **After the bump, same session (each CI 6/6 unless said):** **the monthly plan `55f1fd3`** — ₹599 first month (launch offer), ₹999 from the second, given on request;
  Yearly ₹7,999 and Lifetime ₹29,999 unchanged; `license-issue.mjs --months N` (month ends roll forward), `sell.mjs --months 1 [--renewal]`; the sale script's ANNUAL receipt
  amount was still 9,999 with a test pinning it — now 7,999. Its CI run 35339488381 lost the Windows job to FOUR 5 s timeouts → **`c5c7c5b`**: `testTimeout` 20 s on the
  Windows CI runner only (DECISIONS 2026-09-18, a class not a flake) → CI 6/6 → **tag `v4.3.0`** (the owner: "proceed to do that") → release run 3/3 →
  `release:verify v4.3.0 -- --deep` 3/3 over the published bytes, key id `4FF85F3BBE1DA21D` → published, `releases/latest` → v4.3.0, `revocations` still a prerelease;
  the GitHub Pages landing page shows 4.3.0, the monthly plan and the Signal book (fetched live). Gate on the release tree: 439 files / 9,720 passed / 35 skipped locally,
  9,698 on CI; README 9698 / 439. **The installer was REBUILT after the pricing change**: client ZIP installer SHA-256
  `DB71DD7936907EFC4719FBBAC4F6035884211CCFC80F373D4E80D3DDD9C0E19B` (the earlier `3695B809…` ZIP is set aside as `…SUPERSEDED-pre-monthly-plan.zip`); the GitHub asset's
  SHA-256 is `b768c903…680e507b` (winget manifest `release-packages/winget/4.3.0/` generated from it; submission still HELD by #421585).
- **Next — the owner's, by hand:** install the client ZIP's installer on a NON-build machine → the six-item smoke test (row 3; item (e) = the Signal book on account #3:
  42 rows, net ₹75,132.75 unchanged) → the WDSI submission with the CLIENT ZIP hash above. Then the next build: v4.4.0 (rows 14, 15 + the fix list) and v4.3.1 /
  wave 3 (row 1b) — the two-session plan is in DECISIONS 2026-09-18 "the plan for the next 100 credits".
- **What WRONG looks like:** the Signal book showing fewer than 42 rows on account #3 after the upgrade (the fix logs refused ids to the console — read them, never edit
  notes by SQL); any net for account #3 other than ₹75,132.75 (migration 0072 and the fix move no money — find the writer); a release note claiming auto-close, ETF
  heads, Greeks or a scanner; a CI red that is a 5 s Windows timeout in `backup-roundtrip` or the preview matrix (re-run the job, never re-tag); a `.sig` whose key id
  is not `4FF85F3BBE1DA21D`.
- **Owed outside the release:** `/fleet-tune` (due 9 days; skipped on the owner's credit cap) and one hook proposal awaiting the owner in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`.



## ARCHIVED 2026-09-22 by the fifteenth session — the §0 block of 2026-09-21 (v4.4.0 PUBLISHED; its pickup rows 1–15, §0.2, V1–V6 and the superseded-claims ledger), byte-for-byte; replaced when v4.5.0 reached 8 of 10 waves (HEAD 185ac15)

## §0 START HERE — reconciled 2026-09-21 IST by the fourteenth session: **v4.4.0 is PUBLISHED** (tag `v4.4.0` = `8cc6ac6`, the owner's "Tag" 2026-09-21; CI 6/6 on that sha; release run 35625730563 3/3; deep verify 3/3; `releases/latest` → v4.4.0; the updater feed serves 4.4.0 for six platforms) — **NOT yet installed off the build machine, WDSI not yet submitted: both are the owner's**; the commit that landed this block is the next one in `git log`

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** and rots; it carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/20-V440-BUILD/SESSION-LOG.md` — the v4.4.0 wave-by-wave record (what each builder proved, what is unproven); then `NEXT-SESSION-CONTINUATION.md` §0 for research-pack specifics.
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` · `package.json` `"version"` · `gh run list --limit 3` · the RAW vitest line (§0.3).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: `06-ANSWERS.md` newest table LAST ("v4.4.0 session-1 rulings"); `docs/DECISIONS.md` newest entries at the END (grep `^## 2026-`, never read it whole) — binding, never re-ask one.
5. §3 of this file for where any other answer lives.

**The one-line state (verified 2026-09-21):** published = **v4.4.0** (tag `v4.4.0` → `8cc6ac6`, published 2026-09-21 17:03 UTC; before it v4.3.0 `c5c7c5b`). The release tree: `8cc6ac6` (STATE close-out) on `80eb164` (bump + release copy) on `9b338f8` (per-lot UI + explainer rewrite) on `9221cc6` (R provenance) on `a931dfc` (DECISIONS) on `22fb53f` (per-segment cap, migration 0073) on `93d2a4b` (metrics B-i) on `4ae21b6` (fix list) on `ae5e237` (UI asks) on `859459f` (monthly reprice) on `6995c3a` (parser wave) on `58bf72c`. The block this one replaced is in `VYUHA-STATE-ARCHIVE.md` (byte-for-byte, dated).

- **What v4.4.0 contains (rows 14 + 15 + the fix list — each wave red-on-revert, each commit body carries its gate):**
  - **Parser wave `6995c3a`** — Zerodha Console ledger, Groww balance statement, Groww + Upstox EQUITY contract notes, the Upstox F&O grammar VERIFIED; every pin to the paisa against the broker's own figures (Upstox trade report now 5 positions / net −355.66, was 4 / −271.92).
  - **Monthly plan `859459f`** — ₹999 first month, ₹1,499 from the second (33%, derived); Annual ₹7,999, Lifetime ₹29,999 unchanged.
  - **UI asks `ae5e237`** — resizable sidebar (180–420, per machine), the Live Desk ResizeObserver fix, Appearance LAST with its own route-handler save, the pack's default order on the Wave-A pages, Rearrange on `/settings` + `/` ONLY (per device, move only).
  - **Fix list `4ae21b6`** — Outcome lens "Share of trades"; `parseFormNumber` for every trade-form number; `closeStaleLot` BAD_DATE; Signal book help + `e2e/z-signal-book.spec.ts`.
  - **Metrics** — B-i `93d2a4b` (null denominators; PF after charges + a reproducing drill-down + a free per-segment table; annualisation on the NSE calendar, 2026 = 245); B-ii-1 `22fb53f` (**migration 0073**: per-segment cap, `trades.risk_source`, `repriceCapTrades`, data fix `risk-source-v1`, one dated risk-free rate) — designed by a Plan agent, REVISED by a Fable `vyuha-design-reviewer` (S1–S4) BEFORE any builder; B-ii-2 `9221cc6` (three-way R provenance); `9b338f8` (the per-lot second line; all 43 `healthyRange` lines rewritten to the user's own arithmetic + cited facts, no invented band, a guard that fails on an uncited number).
- **Gate (2026-09-21, on the bump tree):** `npm run verify` EXIT 0 — **450 files / 10,307 passed / 35 skipped** on the owner's machine; **9,745 on CI** (run 35621485652: the owner-file parity cases skip there); README says 9745 / 450 and 110 e2e flows in 32 specs; typecheck 0; lint 3 pre-existing warnings. CI on `80eb164` and on `9b338f8`: SUCCESS 6/6. **the CURRENT tree on `25dac06` is 451 files / 10,326 passed / 35 skipped** (the broker-guide test); README says 9745 / 451.
- **Desktop build (2026-09-21):** `npm run desktop:build` EXIT 0; `desktop-dist/.next/BUILD_ID` written 55 s after the build began; the bundle holds `risk-source-v1`, `perLot`, `vyuha-sidebar-width` and migration `0073_per-segment-cap-risk-free`. Both `.sig` files VERIFY over the bytes with key id **`4FF85F3BBE1DA21D`**. Client ZIP `release-packages/Vyuha_4.4.0_Client_Package.zip` (15 entries after the broker-API guide rebuild — see the bullet below; no licence key, no macOS artefact); installer SHA-256 **`A3BB74F406BA067F263BDED7E2D33B412B56E3466BA7504B3C54CAA85C7F9CF2`** (35,689,607 bytes) = CHECKSUMS.txt = an independent hash.
- **Release steward (2026-09-21): READY TO TAG, no blocking items** — version sync 6/6, lock numstat 2/2 + 1/1, `npm ls esbuild` resolves, revocations a prerelease with `releases/latest` = v4.3.0, claims audit over 7 surfaces 0 unsubstantiated (ONE database upgrade 0073; CAGR / Calmar stated UNCHANGED). Advisories: the winget manifest for 4.4.0 comes after publish; `release:verify v4.4.0 -- --deep` runs only over PUBLISHED bytes.
- **PUBLISHED 2026-09-21:** annotated tag `v4.4.0` = `8cc6ac6` after CI run 35624639602 SUCCESS 6/6 on that sha → release run 35625730563 3/3 (Windows x64, macOS Intel, macOS Apple silicon) → `npm run release:verify v4.4.0 -- --deep`: 3 signatures, key id `4FF85F3BBE1DA21D`, each verified over the PUBLISHED bytes → published `--latest` 17:03:46 UTC → `releases/latest` = v4.4.0, `latest.json` = 4.4.0 for 6 platforms, `revocations` still a prerelease → the Pages landing page fetched live: v4.4.0 and ₹1,499/month present, no v4.3.0 string → winget manifest `release-packages/winget/4.4.0/` from the GitHub asset sha `499ad1234f39e28851c8deb3f109f855fae20c7661ebd798628c915635465fbc` (35,650,469 bytes; the CI-built installer differs from the local client-ZIP one by construction; submission still HELD, row 4). **Next — the owner's, by hand:** install the CLIENT ZIP's installer on a NON-build machine (first launch applies 0073; an install whose caps were never edited must show the SAME Avg R as before) → the WDSI submission with the CLIENT ZIP hash below. **NEXT = v4.5.0 = Session 2, APPROVED TO BUILD (owner 2026-09-22, 06-ANSWERS "v4.5.0 opening rulings"):** row 1b auto-close rebuild + its duplicate-detection defects, wave-3 tax, the Upstox plan fix, the recorded-not-built fix list — designs being drafted in `LIVE-DESK-RESEARCH/21-V450-BUILD/` (verify they exist), then ONE Fable design review, money waves as TWO builders, one Playwright runner, its own release gate, STOP for "tag". **If the installer is REBUILT for any reason, rename the old client ZIP aside first** (`build-client-package.mjs` refuses to overwrite) and re-hash.
- **After the publish, same day (docs + client package only; the 4.4.0 installer is UNCHANGED):** a layman's **broker API setup guide** — `docs/client/BROKER_API_SETUP_GUIDE.html` + `.docx` (Angel One, Dhan, Upstox, Zerodha Kite; sections 0–8; `scripts/build-broker-api-docx.mjs` on a shared `scripts/docx-render.mjs`; `npm run client:docx` builds both guides), packed by `build-client-package.mjs` (the ZIP now has 15 entries; START_HERE points at it), pinned by `tests/broker-api-guide.test.ts`. Asked for by the owner when a paying client stuck on Angel One's SmartAPI "Add App" form. Facts it rests on: VYUHA takes API key + client code + PIN + the TOTP SECRET (the string under the QR, NOT the 6-digit code); the form REFUSES localhost / IPs in Redirect URL (observed on the client's screen: "Localhost is not allowed") — enter any real public https address, VYUHA never uses it; **the Primary Static IP gates ORDER execution only** (Angel One notice effective 2026-04-01; "for APIs other than Orders & GTT, a static IP is not mandatory") and VYUHA places no orders (zero order verbs under `lib/import/api`, `lib/quotes`, `app/api`), so a dynamic home IP does not break the pull. Client ZIP REBUILT (old one set aside as `…SUPERSEDED-pre-broker-guide.zip`); installer SHA-256 still `A3BB74F4…5C7F9CF2`. The one-line pointer added to the in-app Import Help cards ships with the NEXT release (it is app code). Gate: vitest 451 files / 10,326 passed / 35 skipped; tsc 0; README 451.
- **RULED 2026-09-22, not built:** the Upstox brokerage is PLAN-dependent (06-ANSWERS "v4.5.0 opening rulings") — deep research of Upstox's published plans, then the fix in v4.5.0 through `charge_config` (reuse the broker × plan dimension if it exists); the owner's 2026-08-28 option day (trade date per the research file; an earlier note said 09-01) (ledger implies 226.57 vs the engine's 156.76) is the pin. Do NOT re-ask which rate is right. Still open, the owner's: rows 3, 6.
- **Recorded, not built (DECISIONS 2026-09-18 / 09-21):** 14 new metric explainers deferred (no render site on a gated help page); the per-lot line has no browser assertion (no e2e fixture with a resolvable F&O lot); comma-stripping survives in `app/api/risk/limits/route.ts:18,24`, `components/sizing/lab-client.tsx:92`, `lib/domain/onboarding.ts:122`; the Rearrange mechanism covers 2 pages (others join by registry entries); tabs not movable; metrics wave C (post-tax view, Nifty series, drawdown in months, the floor ladder) NOT started; React warnings seen in e2e, none failing (4 Radix aria-hidden hydration mismatches in z-onboarding, one duplicate key `5` on /live, one pre-mount state update on /trades, one missing key in `StrategiesPage`).
- **What WRONG looks like:** Avg R on an install whose caps were NEVER edited moving after the upgrade (0073 moves no cap — find the writer; the legacy-9,500 inherit rule lives in `resolvePerTradeCap` only); a staged position's R changing on a cap edit (`'frozen'` must never re-price); "default-cap" counted on a trade whose risk the user typed; a per-lot figure on a pre-2026 index expiry without a stored `lot_size`; CAGR or Calmar differing from 4.3.0 (only Sharpe / Sortino / volatility ≈ −1.4% and alpha ≈ −2.8% move); a `healthyRange` line stating a band with no source + year; a release note claiming auto-close, F&O contract notes, movable tabs or more than ONE database upgrade; a CI red that is a Windows 5 s timeout (re-run the job, never re-tag); a `.sig` whose key id is not `4FF85F3BBE1DA21D`.
- **Owed outside the release:** `/fleet-tune` (overdue since 2026-09-09; not run in the thirteenth or fourteenth session) and one hook proposal awaiting the owner in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`. **NEXT = v4.5.0 = Session 2, APPROVED TO BUILD (owner 2026-09-22, 06-ANSWERS "v4.5.0 opening rulings"):** row 1b auto-close rebuild + its duplicate-detection defects, wave-3 tax, the Upstox plan fix, the recorded-not-built fix list — designs being drafted in `LIVE-DESK-RESEARCH/21-V450-BUILD/` (verify they exist), then ONE Fable design review, money waves as TWO builders, one Playwright runner, its own release gate, STOP for "tag".

### §0.1 Pickup table — one row per open item

| # | Item | Status | Resume from EXACTLY here | Closed when |
|---|---|---|---|---|
| 1 | v4.3.0 fix work from the release-level audit (91 confirmed; `LIVE-DESK-RESEARCH/15-RELEASE-AUDIT-4.3.0.md`) under the owner's rulings (06-ANSWERS "v4.3.0 release-level-audit rulings"): the FIFO auto-close switched OFF (restore exactly what 4.2.0 did with a SELL of a held lot); every other 4.3.0-introduced product finding; all 19 pre-existing product findings; test-only medium+ outside auto-close; the 6 docs findings | **BUILT — closed 2026-09-18**: every wave through 2P `3abcb2c` (CI 6/6); no re-check of 2P, no 2Q, wave 3 deferred past 4.3.0, by the owner's instruction of 2026-09-17. Earlier status: IN PROGRESS. Built: switch-off `9e0e16f`, wave 1 `e0e6d90`, wave 2 `a7e9288`, 2R + 2F `ec89bbd`, 2G `3feb22f`, 2H `b6c1029`, 2I/2J/2K `cd1ab70`, 2L + M1 `8ff4288`, 2M `e23b96c`, 2N `f9a1a6b`, **2O `b2af3d8`** (each CI 6/6 through 2N; 2O's CI run 35125379974 — verdict in the one-line state; every wave through 2N scoped-re-checked; the backlog audit's three defects are in 2O). **2O's re-check RAN 2026-09-17 (42 / 0 / 0, 16 findings, one introduced silent wrong number); fix wave 2P DESIGNED + design-reviewed (`wave2p-designs.md`, `wave2p-design-review.md`), NOT built; wave 3 DEFERRED past 4.3.0 (owner 2026-09-17).** Gate 433 / 9,544 / 0 expected fail / 35 on the owner's machine (9,522 on CI) | `NEXT-SESSION-CONTINUATION.md` §0 NEXT item 1 (build 2P) → row 2 — **the owner's instruction 2026-09-17: no re-check of 2P, no further wave** | every product finding fixed or moved to row 1b by ruling; scoped re-check clean; CI 6/6 → row 2 |
| 1b | v4.3.1 — the FIFO auto-close rebuilt together with the duplicate-detection defects that share its trade-identity code (F-L1-3's Dhan GTR-name vs API-ticker mismatch, F-L1-7's same-hash second-sale drop, R43 if inseparable) | **APPROVED TO BUILD as v4.5.0 (owner 2026-09-22)** — designs in `21-V450-BUILD/autoclose-rebuild-designs.md`; by ruling, after 4.3.0 | `15-RELEASE-AUDIT-4.3.0.md` (R3, R4, R8, R72, R75, R76, R2, R14, R31, R6, R39, R41, R62, R15; tests R16, R58–R60) + DECISIONS "2026-09-11 — v4.3.0 release-level audit" | a researched design, the owner's "build", its own release-level audit |
| 2 | Bump 4.3.0 + the 11 release steps | **PUBLISHED 2026-09-18** — tag `v4.3.0` = `c5c7c5b`; CI 6/6; release 3/3; deep verify 3/3; `releases/latest` → v4.3.0. Owed by the owner: the non-build-machine install, WDSI; winget HELD | `NEXT-SESSION-CONTINUATION.md` §0 NEXT item 2 (the bump recipe; the drafts in `LIVE-DESK-RESEARCH/16-BUMP-4.3.0-DRAFTS/` — drop every auto-close claim first; the install guide says ONE upgrade, 0071 — ruled; the widened rate-card note; the bundle must carry `rate-card-refresh.mjs`) → `08-BUILD-PROMPTS/V400-LIVE-DESK-BUILD-PROMPT.md` §7 → `release` skill → `vyuha-release-steward` | owner says **"tag"**; tag `v4.3.0` pushed; release run 3/3; `release:verify v4.3.0 -- --deep` 3/3; `releases/latest` → 4.3.0; client ZIP built; winget manifest with the CI asset sha; WDSI form handed over unprompted |
| 2b | **v4.4.0 release** | **PUBLISHED 2026-09-21** — tag `v4.4.0` = `8cc6ac6`; CI 6/6; release 3/3; deep verify 3/3; `releases/latest` → v4.4.0; winget manifest generated (HELD). Owed by the owner: the non-build-machine install, WDSI | §0 "Next — on the owner's tag" | owner says **"tag"**; release 3/3; deep verify 3/3; `releases/latest` → v4.4.0; WDSI handed over |
| 3 | Owner laptop smoke test of 4.3.0 | **OPEN — row 2 is done**; the installer is `release-packages/Vyuha_4.3.0_Client_Package.zip` | the six-item list in `NEXT-SESSION-CONTINUATION.md` §0 NEXT item 2 (last sentence) — signatures in §0.3 V6 | owner confirms all six on a non-build machine with his own keys |
| 4 | winget submission | HELD | `release-packages/winget/<version>`; §8.2 | microsoft/winget-pkgs #421585 merged, then the manifest is submitted |
| 5 | Live feed pricing the UNDERLYING of open option rows | **CLOSED 2026-09-17** — the Option Strategies plan is archived by the twelfth-session owner instruction (§0 bullet) | `06-ANSWERS.md` row "underlying of open option rows" (line ~226) | the owner names its release; ships with its own audit |
| 6 | §3 Positions-tab redesign (`/live` Charts / Positions tabs) | NOT built; the owner has not said "build" on the round-3 canvas | `NEXT-SESSION-CONTINUATION.md` §3 (plan, constraints, open questions); §8.00 here; the sector-mapping inputs memory fires FIRST (`vyuha-live-desk-v4-inputs`) | owner says "build" → sector files + sizing calc collected via pop-up → wave plan → `e2e/z-live-desk.spec.ts` + seam pass + audit |
| 7 | Owner questions still open | see §0.4 | §0.4 | each answered in `06-ANSWERS.md` |
| 8 | Ledger items still unbuilt after 4.2 — **the CORRECTED list** (backlog audit §5, folded 2026-09-16): Q42b signed widget feed (the ONE delivery channel; Q51's Sentinel automation folds in here); Q51 cohort analytics (MIS-SIZED: the analytic ships; owed = freshness catch-up + 3 honesty gaps + industry-level cohorts); 06a Atlas panel v3 (sequence AFTER Q51's fixes — same files); Q18 Telegram alerts (MIS-SIZED: wiring only — one door + one gate + migration 0072; mount point `app/page.tsx:83-85`, NOT the root layout); journal-derived Kelly (fills the existing `winPpm` / `payoffPpm`); openalgo-charts facade (DUPLICATE of row 6 — the facade owns `components/live/position-chart-panel.tsx`; sequence with §3, never parallel). **The 5 size-index lists / cap band (Q-2) are BUILT (v4.0.0)** — struck from the ledger's count | unscheduled | `19-BACKLOG-RESEARCH-2026-09-16/` (`Q42b-…`, `Q51-…`, `06a-…`, `Q18-…`, `kelly-…`, `openalgo-…` packs); `NEXT-SESSION-CONTINUATION.md` §2 carries the stale seven-item list until it is trimmed | each gets an owner "build" and its own release |
| 9 | §8 backlog — **the CORRECTED list** (backlog audit §5, folded 2026-09-16; the audit found 18 §8 lines describing SHIPPED code): BUILT and struck — broker-truth screen, the Paytm P&L parser and 6 named parsers, `/trades` server pagination, short-sell / cross-exchange pairing, the floating search assistant (all v3.9.0), first-run onboarding (v3.7.0); DISCHARGED — Zerodha F&O grammar (2026-09-01; residue: Upstox F&O grammar unverified, `BROKER_FORMATS.md:358-360`); the three live defects the audit found are FIXED in 2O. Genuinely open: parser gaps — each is a missing export TYPE, none a parser waiting on a file on disk, none documented on the internet (06-ANSWERS "Owner replies on the corrected backlog" has the per-broker table): **INPUTS COMPLETE 2026-09-17** (06-ANSWERS "Owner broker files received and VERIFIED"; six files + manifest at `VYUHA/BROKER-FILES-FOR-TESTING/RECEIVED-2026-09-16.md`, each read with the repo's own libraries): the Zerodha Funds statement (`ledger-HAH611.xlsx`, sheet `Equity`, 7 columns, 6 entries — layout pinned), the Groww Balance statement (`Client Fund Ledger`, 12 columns, 447 entries, 8 segment types — rich), an Upstox trade report WITH 6 F&O rows (the F&O grammar is pinnable), an Upstox ledger with the FO segment, a Groww EQUITY contract note (30 pages, unprotected, PII → redact) and an Upstox EQUITY contract note (3 pages). CLOSED by the owner 2026-09-17: Paytm F&O (no such trading), Groww F&O (never traded); CLOSED by the pack: Kotak Neo, Sahi (no account). **Nothing is owed from the owner; the parser wave is BUILDABLE and SCHEDULED v4.4.0 beside row 14 (owner, 2026-09-17) — row 15**; B1 (the stale doc lines in `docs/BROKER_FORMATS.md`) fixed 2026-09-17; `/trades` next lever = the whole-book projections at `app/trades/page.tsx:50`, by measurement only; short-sell residue = no `side` column, ~20 re-derivations of `sellQty > buyQty`; a DERIVED "Getting started" strip (no new state); option-seller depth r3 — CLOSED 2026-09-17 by the archived-plan instruction (§0); B1/B2 funnel PARTLY built (owed: `--ref` + a ledger `ref` field in `license-mint.mjs` + `license-list --by-ref`; share-card footer → buyer link); demo video FILMED, unpublished (re-film on the tagged 4.3.0 build, +2 scenes; the phantom cite at `06-PUBLISH-COPY.md:4`); macOS notarisation (macOS not sold — §7.5) and winget (row 4); theme/skin collapse (Q67's deferral to v4.1 EXPIRED — the audit recommends NOT collapsing; §0.4 item 4); the owner's four NEW asks — **SCHEDULED v4.4.0 (row 14; 06-ANSWERS "v4.4.0 rulings", 2026-09-16)**: resizable sidebar width (collapse-to-icons already ships; only the interaction is open; per MACHINE), page-section default order then user-movable cards incl. cards nested inside a feature's client component — TABS excluded from release 1 ((A) default reorder first, (B) generalise the sidebar's `useListDrag` + Reset), India-tuned metrics with plainer explanations (`metrics-india-tuning.md`; copy wave first; the 252-day year is a money-class change, `99-…AUDIT.md` §3(c) row 8), and the Appearance card moved to the end of the settings page with its OWN route-handler save | unscheduled | `19-BACKLOG-RESEARCH-2026-09-16/99-STALE-DUPLICATE-CONTRADICTION-AUDIT.md` §5 (the source of this row); §8's own bullets still wear the banner, NOT yet struck line by line | owner picks; each ships in its own release |
| 10 | Q52 second half — the index map's sha256 beside `asOf` on `/instruments` | open | `AGENTS.md` § Bundled NSE index map, last sentence | `/instruments` shows the file digest |
| 11 | Operator-owed: hook proposal — **now the SENTINEL hooks README repoint** (its lines 22 and 31 name the pre-2026-09-10 repo path; docs only). The wave-guard proposal this row used to name was **REJECTED 2026-09-11** (premise falsified: wave-guard was replaced by `context-watch.mjs`, which logs no decisions); its diagnosis suggests a follow-up (make `context-watch` log a decision row) that is not yet written up as a proposal | awaits the operator (`hook-gate.mjs unlock`, then the session working in SENTINEL applies it) | `~/.claude/coord/learnings/HOOK-PROPOSALS.md` — the one `[PENDING]` heading | applied or rejected there |
| 12 | Operator-owed: the global trust-repo path | CLOSED 2026-09-11 — the round-3 docs auditor read `~/.claude/settings.json` line 236 and it already names `VYUHA\TRADE-JOURNAL` (0 occurrences of the old folder in that file) | — | done |
| 13 | `fleet-tune` | **DUE** (the SessionStart hook said so on 2026-09-16 and again on 2026-09-17: 7.3 days since the last tune, 215 hook decisions logged; NOT run in the tenth or eleventh session — it edits agent/skill prompts and needs its own session; one hook proposal awaits the operator in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`) | its own session (`/fleet-tune`) | weekly |
| 14 | **v4.4.0 — the owner's four asks + the Appearance move** (RULED 2026-09-16, 06-ANSWERS "v4.4.0 rulings" + "Owner replies on the corrected backlog"): resizable sidebar width (per machine); page-section default order, then user-movable CARDS incl. cards nested inside a feature's client component (tabs later — the pack's Q9); India-tuned metrics (copy wave A first, ratio wave B, lenses wave C); Appearance (theme / skin, NOT collapsed) moved to the END of the settings page with its OWN route-handler save; **+ the parser wave (row 15) — owner 2026-09-17, 06-ANSWERS "Parser-wave release ruling"** | **BUILT 2026-09-21 — in the v4.4.0 release tree (`ae5e237` UI, `93d2a4b` / `22fb53f` / `9221cc6` / `9b338f8` metrics); Rearrange is the PILOT on `/settings` + `/` only, metrics wave C NOT started; waits on the owner's "tag"**. Earlier: RULED, NOT started | the three packs in `19-BACKLOG-RESEARCH-2026-09-16/` (`sidebar-resizable-width.md`, `page-section-order-and-user-arrangement.md`, `metrics-india-tuning.md`) — each pack's remaining questions asked ≤10 per group at its start (the four build-blocking / irreversible ones are already ruled); a wave plan (`planner`), `vyuha-design-reviewer` before builders, seam pass, its own release-level audit | owner says "tag" on v4.4.0 |
| 15 | **Parser wave — inputs COMPLETE 2026-09-17** (row 9's parser line; 06-ANSWERS "Owner broker files received and VERIFIED"): a Zerodha ledger parser (Console Funds statement, `ledger-HAH611.xlsx`), a Groww ledger parser (`Client Fund Ledger`, 12 columns, 8 segment types), the Upstox F&O tradingsymbol grammar pinned from a report with 6 NIFTY option rows (`docs/BROKER_FORMATS.md:358-360`), Groww and Upstox EQUITY contract-note parsers (the Dhan parser's shape; fixtures REDACTED — the Groww note carries PAN / mobile / address); Paytm F&O, Groww F&O, Kotak Neo and Sahi CLOSED | **BUILT 2026-09-18 `6995c3a` — five formats, every pin to the paisa, the detection matrix complete; in the v4.4.0 release tree.** Earlier: SCHEDULED v4.4.0 beside row 14 — NOT started; waits on the 4.3.0 tag (row 2) like the rest of v4.4.0; each parser ships with a `tests/golden-books`-style pin against the broker's own stated figures and a row in `tests/import-detection-matrix.test.ts`; the Groww contract-note fixture REDACTED first | `VYUHA/BROKER-FILES-FOR-TESTING/RECEIVED-2026-09-16.md` (the six files, sizes, sha256, shapes); AGENTS.md "A broker-named parser must see the broker's NAME"; `lib/import/registry-meta.ts` (the ONLY source of truth for importable formats); `docs/BROKER_FORMATS.md` | five parsers green in the detection matrix, `dropzoneHint()` derived, README + help copy updated, its own scoped re-check |

### §0.2 What was left out of the pickup table deliberately

Migration re-keying rows closed by never-shipped wave-1 code — **none needed** (4.2.0 has no auto-close; ruled). `VYUHA_KEY_ARCHIVE_DIR` — SET (06-ANSWERS). Angel `/quote` home test — PASSED 2026-09-07. Intraday bar import — NOT required (owner 2026-08-15; §8.4 note).

### §0.3 Verification procedures for everything BUILT BUT UNVERIFIED — PASS and FAIL signatures

**V1 — CI on the newest CODE sha (at this hand-off 2026-09-22: `9b338f8`, the per-lot + explainer wave, SUCCESS 6/6; HEAD `25dac06` is docs-only and also green, run 35637117188; history: before it `f9a1a6b` run 35086623142 SUCCESS 6/6 and `e23b96c` run 35022758611 SUCCESS 6/6; **FAIL-F** — TWO runs appear on one sha: `pages-build-deployment` (3 jobs, the docs site under `docs/sales`) is NOT the CI workflow — judge only the run whose `workflowName` is `CI` with 6 jobs (`gh run view <id> --json workflowName,jobs`); the seventh hand-off's docs commit `b6e2353` run 35008569548 was red on the Windows job alone — three SQLite-file tests at vitest's 5 s in files the commit never touched — and its re-run passed: FAIL-B, confirmed). **FAIL-E** — the unit-test job red on EVERY runner while green locally, `fatal: invalid object name`: a test reads git history; make the text a committed fixture (`tests/fixtures/…`), never widen the checkout.** `gh run list --limit 5`. **PASS:** conclusion `success`, 6 of 6 jobs. **FAIL-A** — only the Windows job red, on a source-shape/span-cap expectation: a CRLF cap (398 LF vs 408 CRLF); widen the cap by one byte per line it can cross, touch no product code. **FAIL-B** — a job red with a timeout / network / cold-runner signature: re-run that job; never re-tag, never "fix" code for it. **FAIL-C** — the run shows `cancelled`: a later push to main cancelled it (`cancel-in-progress`); judge the run on the NEWEST sha, never call a cancelled run green. **FAIL-D** — HEAD `e0378eb` has no run or a red one: it is docs-only; the code run to judge is `a00ddd0`'s.

**V2 — the gate.** `rtk proxy npm run verify` from the repo root (heavy: coord serialises it machine-wide; a coord **DENIED** means another session holds the lock — wait and retry, it is not a failure). **PASS:** exit 0 and the RAW vitest summary line reads `Test Files ≥ 451 passed`, `Tests ≥ 10,326 passed | 35 skipped` with NO `expected fail` (the hand-off tree on `25dac06`, 2026-09-21; ≥ 9,745 on CI, where the owner-file parity cases skip). **FAIL-I** — `e2e/z-live-desk.spec.ts` red on `aside … IST` never visible with `ChunkLoadError: Failed to load chunk /_next/static/chunks/app_layout_tsx_…` in the WebServer log and a 3-minute run: the gate's `next build` left a production `.next` that the dev server's turbopack then served stale (2026-09-16, 4 failed / 5 passed); `rm -rf .next` (gitignored) and re-run — 9 / 9 in 45 s; never "fix" code for it, and run e2e BEFORE the gate or after clearing `.next`. **FAIL-H** — `tests/live-tracker-copy.test.ts` red on a word: the Live Desk vocabulary gate (never a transaction verb in components/live/*); reword the copy, never the gate (2N: "buy leg" → "original leg"). **FAIL-F** — the raw line shows an `expected fail`, or an `it.fails` file is red: somebody re-marked a pin, or a new recorded defect appeared — read that file's `// FINDING` comment. **FAIL-G** — `tests/backup-roundtrip.test.ts` "encrypted backups" red on `Test timed out in 5000ms` under the full suite (1,049 ms alone; scrypt): a pre-existing timing fragility, re-run; if it trips twice in a row, raise that `it`'s timeout with the measured local time in a comment, touch nothing else. Read the raw line, never the rtk summary. **FAIL-A** — counts SHRANK: a test file or glob was lost; a shrinking suite passes silently — find the missing file before anything else. **FAIL-B** — rtk summary green, raw line shows failures: the wrapper hid them (it did once, 4 failures); trust the raw line. **FAIL-C** — typecheck/lint/test green, `next build` red: a server-only import reached the client graph (AGENTS.md § Verify); fix the import, not the build config. **FAIL-D** — `tests/client-value-imports.test.ts` red: a server module value-imports from a `"use client"` module; Next throws at request time and nothing else can see it. **FAIL-E** — a doc guard red (`readme-claims`, `help-content`, `privacy-feed-disclosure`, `uninstall-claims`): prose drifted from code; fix the prose, not the expectation (the one deliberate expectation change was the `## 2-hist.` header rename in this pass).

**V3 — migration 0071 on a real database. PASS 2026-09-11** (DECISIONS "2026-09-11 — v4.3.0 STATE §0.3 V3/V4/V5 …"). *This procedure REPLACES the original, which could not work: `next dev` never migrates — no instrumentation file, `lib/db/index.ts` runs data fixes only.* Copy `%APPDATA%\in.vyuha.tradejournal\vyuha.sqlite` **and its `-wal`** (app closed; sha256 both first and again at the end) into a FOLDER of its own, `data\smoke-0071\vyuha.sqlite` — `attachments/`, `trash/` and `vault.key` sit beside the DB file, so a loose copy in `data\` shares them with the dev DB. Then EITHER `set VYUHA_DB_PATH=<absolute path>&& npm run db:migrate` twice, OR — the real startup path — stage `scripts/desktop-server.mjs` + `scripts/rate-card-refresh.mjs` + the repo `drizzle/` + `desktop-dist/vyuha.seed.sqlite` in one folder under `data\` and run `node <stage>\desktop-server.mjs` twice with `VYUHA_DATA_DIR` on the copy's folder (it dies on the missing `server.js` after the migration block — expected). **PASS:** `__drizzle_migrations` +1 (created_at 1788800500000) and `strategy_shelf_json` present and NULL; the second run +0; the sidecar prints "migrations applied" + "rate cards: 0 added, 45 refreshed", then "schema current" + "0 added, 0 refreshed"; `/strategies` on a dev server over the migrated copy (`.claude/launch.json` → `vyuha-dev-smoke-0071`, port 3011) draws the 8 default tiles in order (Pro). **FAIL-A** — "duplicate column": 0071 ran outside the journal; check `drizzle/meta/_journal.json`. **FAIL-B** — column present but the shelf empty: `parseShelf` rejected the stored envelope (must be `{v:1,selected}`). **FAIL-C** — backup → restore loses the shelf: `SETTINGS_MACHINE_COLUMNS` (`lib/backup-format.ts`) gained `strategyShelfJson` — NOT `BASELINE_SETTINGS_FIELDS`, which drives My Default Settings only. **FAIL-D** — the sidecar prints "rate-card refresh failed" or `UNIQUE constraint failed … effective_from`: F1 is back (`scripts/rate-card-refresh.mjs`, `tests/rate-card-refresh.test.ts`). **FAIL-E** — `/strategies` throws `no such column: strategy_shelf_json`: a dev server over an UNMIGRATED copy (dev does not migrate).

**V4 — `/strategies` free vs Pro. PASS 2026-09-11** on the copy (free = the copy's own expired trial with an undecryptable licence; Pro = `trial_started_at` = now; three seeded structures; the free bodies re-grepped after a Fable skeptic round — 0 refuted). **PASS:** a free account sees "Custom (n legs)" + ProLock and the RSC payload (view-source / network tab) carries NO strategy name; a Pro account sees the name, shelf strip, browse drawer, undo/redo/restore. **FAIL-A** — a name in the free payload: `withholdForFree` ran after serialisation — a gating defect, audit dimension security-gating-consent. **FAIL-B** — Pro sees ProLock on the whole page: a consumer treats the new `PRO_FEATURES` `partial` entry as whole-page gating (`ENTITLEMENT_PATHS` changed in wave 2). **FAIL-C** — `tests/render-windowing.test.ts` red: `LazyMount` / `PayoffChart` left the page file.

**V5 — Options Help Desk. PASS 2026-09-11** in headless Chromium — the Browser pane is hidden and lays nothing out (its rects read 0), so drive a real browser: 40 anchors set-equal to `STRATEGY_IDS` (DOM order grouped by style, by design), fresh loads land the card at 80 px under the 69 px sticky header, the palette's "jade" ranks "Jade lizard · Options" first from `/help` and `/`; also verified in the FREE state. **PASS:** `/help` shows the highlighted Options section with 40 entries; `/help#options-<id>` scrolls to the entry; the command palette finds one by term. **FAIL-A** — fewer than 40 or a dead anchor: an `options-help.ts` id no longer matches the catalogue's `STRATEGY_IDS`. **FAIL-B** — `tests/options-help.test.ts` red on vocabulary: the strict scoped vocabulary gate; fix the copy, not the gate.

**V6 — the owner's smoke test (row 3), item by item.** (a) Dhan catch-up pull over the 5 Sept gap — PASS: gap-dated rows appear once, "Pulls missed since" clears, the IST expiry stamp shows; FAIL-A duplicates → identity hash / `dedup-alias` path (money); FAIL-B no rows and the line persists → `lastPullAt` not advanced, the 90-day / 50-page cap, or an expired token (401 → the stamp). (b) Duplicate removal on Data Quality — PASS: only PLAIN copies are offered; "Remove the copy in <account>" then the confirm "Remove the copy" removes one; the group shows its own row; FAIL: an aliased/frozen row offered → identity-hash grouping regression (money). (c) The stale Dhan rows (Marksans / BEL / VBL / SBIN) on Data Quality — the FIFO auto-close is OFF in 4.3.0 by ruling (06-ANSWERS "v4.3.0 release-level-audit rulings", rebuilt in 4.3.1), so an import or pull never closes a held lot; the repair is R26 — PASS: `/data-quality` lists each stale open lot beside its later sale; "Close with the recorded sale" closes the lot at the sale's price and quantity on the confirmed date (prefilled; the user confirms a date the sale never stated), the sale row moves to Deleted items, and re-importing the sale's file skips it (the lot now answers to its hash); a partial quantity is listed without the one-click close; FAIL-A the lot's closed quantity ≠ the sale's → pairing regression (money); FAIL-B the re-import lands the sale again → the `dedup-alias` read in import dedup is gone; FAIL-C the chargesTotal differs from the lot's stored charges + the sale's stored charges → the close re-derived a side the broker stated (money). (d) `/risk` spot chip — PASS: "spot?" opens the editor, `POST /api/risk/spot` stores it, the EOD-close fallback shows when no manual value; FAIL: focus lost after the dialog → the try/finally focus restore. (e) The strategy screen on his own option book — PASS: per-symbol grouping, split on match failure, UL leg read-only, "Highest outcome" label, a positive max loss labelled as a "Worst case" gain; NOT a defect: an unrelated Sept/Oct pair named as a calendar/diagonal (accepted, module header); FAIL: `netPremium` shown including a UL leg's entry cash without the "Underlying entry" line. (f) Both feeds with his own keys — PASS: quotes on `/live`; the consent sheet names five sign-in triggers; NOT a defect: Angel One stops after three refusals and the capped sentence says "refused"; FAIL: "could not reach" is network, retry before filing.

### §0.4 Questions the owner still has to answer

1. **"build"** on the Positions-tab round-3 canvas (row 6) — the one live design question.
2. ~~"tag" for 4.3.0 / 4.4.0~~ — BOTH GIVEN (v4.3.0 2026-09-18, v4.4.0 2026-09-21). Next tag owed: **v4.5.0**, after its release-level gate.
3. When does the underlying-priced option row ship (row 5)? And which release gets the contract-keyed derivative-marks schema (06-ANSWERS "M1 … DEFERRED to its own release")?
4. ~~Theme vs accent-skin~~ — **CLOSED 2026-09-16 by the owner: do NOT collapse; move the theme/skin controls to the LAST section of the settings page** (06-ANSWERS "Owner replies on the corrected backlog"; a small UI item, scheduled with the four asks when the owner names their release).
5. ~~Zerodha F&O symbol grammar~~ — DISCHARGED 2026-09-01 (`classify.ts:26-39` pinned it from the tax P&L); the stale re-opener is the comment at `lib/import/parsers/zerodha.ts:268-270` (backlog audit S4). Residue, not a question: the Upstox F&O grammar is unverified (`docs/BROKER_FORMATS.md:358-360`).
6. Q24 attribution link for the softened re-auth sentence — only if you ever want the attribution.
7. ~~Pro annual price~~ — CLOSED 2026-09-11 from the repo: ₹7,999 (`docs/owner/MONETIZATION_PLAN.md:344` + both selling surfaces); `docs/SESSION_PROMPT.md:178` corrected.
8. When §3 opens: the sector-taxonomy files (Sentinel + Chartink Atlas), the position-sizing calculator, the "few tweaks".

**RESOLVED 2026-09-11 — the owner: "NO MINOR RELEASE REQUIRED, release all in v4.3.0 (I have made the same ruling before)" (06-ANSWERS "v4.3.0 pre-bump ruling"); kept as history:** **Asked under a premise later found false (flagged — you were never given the real choice):** every pop-up in the `06-ANSWERS.md` table titled "v4.2.1 rulings" was framed as a PATCH release; you then ruled ONE release, 4.3.0 (a migration makes it a minor). The rulings stand and shipped in 4.3.0 waves 1–2. If any of them would have been answered differently for a minor (e.g. scope you would have added or split off), say so before row 2. The §8.4 "intraday bar import blocked on a data-source decision" was never a decision you owed — you had ruled intraday NOT required on 2026-08-15; struck, not re-asked.

### §0.5 ⛔ SUPERSEDED CLAIMS ledger — each one also corrected AT ITS SOURCE on 2026-09-10

| Where it still says it | The claim | The TRUTH | How established → fix applied |
|---|---|---|---|
| this file line 4 | "verified … on 2026-09-09" | verified 2026-09-10 | this pass → rewritten |
| this file §3 routing table | DECISIONS 100 KB; CHANGELOG 152 KB / 61 sections; README 64 KB | 517 KB / 245 KB / 103 KB | `ls -l` → rows rewritten |
| this file §3 | "What is v3.8/v3.9 building" = V380 plan; opener = NEXT_SESSION_V380 / SESSION_PROMPT | shipped v3.8.0 (09-04), v3.9.0 (09-05); the opener is §0 | `git tag` → rows rewritten |
| this file §8.00 | continuation "Step 1b" | it is §3; no Step 1b exists | file headers → corrected |
| this file §8.0 header | "Next releases — v3.9.0 … v4.0 reminder" | both shipped; list is the deferred-items record | `git tag` → header note |
| this file §8.0 | "REMIND THE OWNER when v4.0 planning opens" | retargeted 2026-09-08 to §3 | 06-ANSWERS row ~205 → note |
| this file §8.0a header | "v3.9.1 candidates" | v3.9.1 shipped 2026-09-05 | `git tag` → header note |
| this file §8.1 | "v2.99.100 is PUBLISHED and is releases/latest" | v4.2.0 is | §2-published (`gh release view`) → note |
| this file §8.4 (two places) + §8.5 | "Upstox file parsing is schema-verified only / values unverified" | DISCHARGED 2026-09-04 | AGENTS.md, `tests/golden-books.test.ts` → notes |
| this file §8.4 | intraday bar import "blocked on a data-source decision" | ruled NOT required 2026-08-15 | same section → struck |
| this file §8.5 | "Public launch is planned as 3.0.0" | 3.0.0 shipped 2026-08-29 | `git tag` → note |
| this file §2-published "Next:" | "application fixes (list not yet given — ask first)" | list came 2026-09-09 → v4.3.0 wave 1 | §2 → note |
| `docs/SESSION_PROMPT.md` ready-to-paste block | v2.99.100 published; 1,962 tests / 134 files; migration 0049; candidates list | v4.2.0; 378 / 7,598 / 35; migrations 0071 and 0072; §0 pickup table | gate + `drizzle/` → banner (rules kept) |
| `docs/prompts/NEXT_SESSION_V380.md`, `…V390_V400.md`, `…V391_V400_LIVEDESK.md` | "v3.7.1 published, build 3.8" / "build 3.9.0" / "v3.9.1 built, uncommitted" | all shipped through v4.2.0 | `git tag` → banners |
| `docs/V320…V380_BUILD_PLAN.md` | "AWAITING OWNER APPROVAL" / "IN BUILD" / "approved v3.7–v3.9 roadmap" | all shipped | `git tag` → banners on all six |
| `NEXT-SESSION-CONTINUATION.md` line 6 | newest ruling table = "v4.2 build-session rulings" | "v4.3.0 audit-round-1 rulings" | 06-ANSWERS headers → corrected |
| `NEXT-SESSION-CONTINUATION.md` §2 / §2.2 | "NEXT — v4.2.0" / "first thing in the 4.2 session" | shipped 2026-09-09 / done 2026-09-07 | `git tag`, 06-ANSWERS → headers |
| `NEXT-SESSION-CONTINUATION.md` §4 | DECISIONS "400 KB" | 517 KB | `ls -l` → corrected |
| `00-INDEX.md` lines 3, 30–31 | HEAD `3d7981e`, v3.9.0 published; "v3.9.1 first"; "v4.0 targeting Sun 2026-09-13" | HEAD `e0378eb`; v3.9.1–v4.2 shipped | `git log` → banner |
| `08-BUILD-PROMPTS/V391-BUILD-PROMPT.md`, `V400-LIVE-DESK-BUILD-PROMPT.md` | HEAD `3d7981e`; "STOP unless v3.9.1 is published" | history; §4/§7/§9 of V400 remain procedure | `git log` → banners |
| `06-ANSWERS.md` table "v4.2.1 rulings" | a v4.2.1 patch release | ONE release 4.3.0 (its own last row) | read → title note |
| hub memory `vyuha-v430/v420/v390-build-state.md` + their index lines | three different "current" states | none is; the repo is | `git log` vs each → deleted, replaced by `vyuha-repo-is-authoritative.md` (both memory dirs) |
| hub memory `vyuha-web-platform-research.md`, `vyuha-v390…` | pack at hub root; owner input files at hub root | `TESTING+RESEARCH/VYUHA-WEB-PLATFORM-RESEARCH/`; `TESTING+RESEARCH/RESEARCH FILES/` | `ls` → rewritten / path table |
| hub memory `vyuha-wealth-planner-decision.md` | VYUHA v2.99.101; Atlas "phases 0–1, 49/49 tests" | Atlas state file says phase 6, 182/182 | read → rewritten without counts |
| 42 files (73 path strings) across repo, Atlas, research packs, memory, coord config, scout agent | `VYUHA-TRADE JOURNAL-V1`, `VYUHA-LIVE-DESK-RESEARCH`, `VYUHA-ATLAS`, `BROKER FILES FOR TESTING`, `MTF FILES` paths | the `VYUHA/` tree (§0 one-line state) | sweep script, re-run = 0 → applied |
| `~/.claude/settings.json` line 236 | trust-repo path `VYUHA-TRADE JOURNAL-V1` | `VYUHA\TRADE-JOURNAL` | hook-gate denies agents → row 12 → **applied by the operator; verified by the round-3 docs auditor 2026-09-11** |
| `~/.claude/hooks/learn.mjs` line 220 comment; `skills/token-efficient-coding/SKILL.md` line 200 label | mention the old folder name | historical mentions, not paths | left as history |
| this file §0.3 V3 (2026-09-11) | "`set VYUHA_DB_PATH … npm run dev` … startup applies 0071 once"; FAIL-C → `BASELINE_SETTINGS_FIELDS` | `next dev` never migrates (only `db:migrate` and the desktop sidecar do); backup survival is `SETTINGS_MACHINE_COLUMNS` | 3 explorers + a Fable skeptic (CONFIRMED) → V3 rewritten |
| `NEXT-SESSION-CONTINUATION.md` NEXT item 2 (2026-09-11) | 4.3.0 install guide: "three database upgrades (0069, 0070, 0071)" | TWO (0071, 0072) — v4.2.0's journal already holds 0069/0070; 0072 is the Signal book's column | `git show v4.2.0:drizzle/meta/_journal.json` (71 entries) + owner ruling → rewritten; re-corrected 2026-09-18 when 0072 landed |
| this file §0.1 row 11 (2026-09-11) | the hook proposal awaiting the operator = wave-guard | wave-guard REJECTED 2026-09-11; the pending one = the SENTINEL hooks README repoint | `HOOK-PROPOSALS.md` headings → row rewritten |
| this file §2 (the v4.3.0 wave-1 list, line ~172) | Data Quality "one-click" remove the copy | a "Remove the copy" button plus a confirm | the release-copy check 2026-09-11 → corrected at the source |
| the 4.3.0 build brief | "the 11 legacy names stay free" | 16 `legacyFree` rows | `lib/analytics/strategy-catalogue.ts:87` → the release copy says 16 |
| `docs/DECISIONS.md` ~2498 (a measured fact of its day) | "`charge_config` effective dates are only `1970-01-01` and `2026-04-01`" | 459 rows over NSE 2023-04-01 / 2024-04-01 / 2024-10-01 / 2026-03-01 / 2026-04-01 and the BSE boundaries (C-7, C-8) | `lib/db/seed-data.ts` → left as history |

---


## ARCHIVED 2026-09-22 by the sixteenth session — the §0 block of 2026-09-22 (v4.5.0 at 8 of 10 waves, HEAD 185ac15), byte-for-byte; replaced when v4.5.0 reached 10 of 10 waves and was bumped (HEAD 47088f7 + the bump commit)

## §0 START HERE — reconciled 2026-09-22 IST by the fifteenth session: **v4.5.0 is 8 of 10 waves BUILT, COMMITTED and PUSHED, NOT bumped, NOT tagged** (HEAD `185ac15`; tag `v4.4.0` = `8cc6ac6` remains the PUBLISHED release, `releases/latest` → v4.4.0); the commit that landed this block is the next one in `git log`

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** and rots; it carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/21-V450-BUILD/SESSION-LOG.md` — the v4.5.0 wave-by-wave record (each wave's sha, gate line, what is unproven); then `21-V450-BUILD/3b-notes.md` "Hand-off to 3b-ii (P1)" and `21-V450-BUILD/fix-list-wave.md`.
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` · `package.json` `"version"` (still 4.4.0) · `gh run list --limit 3` · the RAW vitest line (§0.3 V2) · `npm run drift:close-out` (the end-of-session drift check — it re-derives every fact THIS block claims from the tree; 0 FAIL before `/clear`, and a FAIL is fixed in the DOC, never in the check).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: `06-ANSWERS.md` newest tables LAST ("v4.5.0 opening rulings", then "v4.5.0 design-question answers"); the design review `21-V450-BUILD/design-review.md` (18 revisions — precedence: owner answers > review > the three design files); `docs/DECISIONS.md` newest entries at the END (grep `^## 2026-09-22`, never read it whole) — binding, never re-ask one.
5. §3 of this file for where any other answer lives. The v4.4.0-era §0 (its pickup rows 1–15, §0.2, V3–V6, the superseded-claims ledger) is in `VYUHA-STATE-ARCHIVE.md`, byte-for-byte, dated 2026-09-22.

**The one-line state (verified 2026-09-22):** published = **v4.4.0** (`8cc6ac6`). The v4.5.0 tree on `main`, oldest first, each with `npm run verify` EXIT 0 in its commit body: **U** `c0f392b` (Upstox plans, migration **0074**) → **W1** `b7863fe` (one trade identity) → **W2a** `4835d44` (auto-close applier, dormant) → **W3** `bad0820` (un-close, refusals) → **W2b** `85ba973` (auto-close ON; the 6 GiB e2e heap ceiling) → **TP** `419d53a` (tax per tax person) → **3a** `d5a6c74` (ETF list + STT) → `311da07` (an e2e locator, test-only) → **3b-i** `185ac15` (CG heads, calendar months, P3/P4) → `a6ff466` (this block's first cut) → `5db7543` (**the end-of-session drift check**, `npm run drift:close-out`) → the prose-audit fixes (the commit that lands this line; six stale claims, two new checks). **Gate on `185ac15`: 467 files / 10,798 passed / 35 skipped**, typecheck 0, lint 3 pre-existing warnings, `next build` passed. **Playwright: 115 / 115 in 33 specs** (last full local run on the W2b tree; `e2e/z-auto-close.spec.ts` new). **CI:** SUCCESS 6/6 on `c0f392b`, `85ba973` (re-run), `419d53a`, `311da07`; `b7863fe` / `4835d44` / `d5a6c74` each red on a since-diagnosed cause (§0.3 V1 FAIL-G/H); **`185ac15`: run 35698073411 SUCCESS 6/6 on the first attempt** (the fourth clean run in a row since the heap fix). **Nothing is unverified in the pushed tree.** Not yet run on it: the release-gate Playwright pass (§0.3 V7) — owed after the three remaining waves, before the bump.

- **What v4.5.0 contains so far** (owner rulings 2026-09-22; every wave TWO builders — product, then tests — plus `vyuha-verifier` for the gate; each red-on-revert):
  - **Upstox is plan-dependent** — `accounts.broker_plan` + `broker_plan_from` (0074), `resolvePlan` keyed on (account, trade broker), ONE pricing door `ratesForTrade` at 13 call sites at wave U, **14 at HEAD** (the applier added one; `grep -rn "ratesForTrade("` excluding the definition and imports); Upstox **Plus** seeded (₹30, MTF 14.60 %, no subscription); Basic delivery min(2.5 %, ₹20); DP ₹20. Owner's 2026-08-28 day: 156.89 default / 227.69 plus vs ledger 226.57 (the ₹1 residual is a separate open finding).
  - **ONE trade identity** `executionIdentity()`; a second sale on the same hash across two scopes is kept; Dhan GTR / P&L names resolve to tickers for display, the hash keeps the raw name; data fix `dhan-gtr-symbols-v1` (JOINs `import_batches` — a batchless row is unselectable).
  - **The FIFO auto-close REBUILT and ON** (row 1b CLOSED): one holder per execution hash; a dateless sale closes nothing (Data Quality lists it); `unCloseExecution` exact in all three shapes (`exec-bill:` per piece); partial delete / batch cascade / merge of a piece REFUSED with "un-close first"; every production caller passes `autoClose` explicitly; the preview's "Keep sells as separate rows" toggle; the result card names what it closed; /trades rows carry `closedBy` + an Un-close action.
  - **Tax per TAX PERSON** (the owner overruled "all accounts"): `lib/queries/tax-scope.ts` — the ONE widening of invariant 8, registered in `tests/account-isolation`; ten readers threaded; a blank identity is its own person; "All accounts" with two persons → a person picker and NO figure.
  - **The bundled NSE ETF list** (350; `/instruments` shows `asOf` + sha256) and STT from `etf_equity` / `etf_other` rows (0.001 % seller-side from 2013-06-01; 0 for other units); a staged ETF ladder now prices through the same door.
  - **CG heads by transfer date** from one cited band table; calendar months everywhere (a 365-day sale is SHORT-TERM — up to two days' shift on every equity trade, release-noted); ITR-2 111A code A3 → **A2**; P3, P4; STT added back in CG buckets only; MTF/pledge "not deducted" as a stated floor; CII blank; no total for a slab year.
- **What is NOT built (the next session's first three waves, in this order):** **3b-ii = P1** (a partly-sold staged position in no tax year → per-fill rows from `summarise().fills`, apportion the per-row add-backs by fill quantity, replace all EIGHT `!isOpen` filters incl. `app/api/ais/route.ts` — `3b-notes.md` "Hand-off to 3b-ii", design-review revision 17) → **3c** (`vyuha-seam-tester` writes `tests/seams-v45-etf-tax.test.ts`: GOLDBEES symbol-only STT 0 AND head; NIFTYBEES STT ₹1 AND 111A/112A; LIQUIDBEES acquired 2023-05-02 SMF-deemed; an unlisted INF ETF: equity-share rate, blank head, DQ; HYBRIDETF STT 0 + UNDET) → **the fix-list wave** (`fix-list-wave.md`: 11 items from this session + 3 from the v4.4.0 list; two DECIDED there) → gate → bump 4.5.0 → the `release` skill → `vyuha-release-steward` → **STOP for "tag"**. The release copy owed is listed in `fix-list-wave.md` §C. The CI test count (~9,7xx — the owner-file parity cases skip there) is re-derived from the run log, never copied.
- **What WRONG looks like:** account #3 (Dhan, manual, 42 closed option rows) with net ≠ **₹75,132.75** or any moved stored column (every wave proved it untouched — each has a test); a stored broker-stated charge moving on the Upstox upgrade (only ESTIMATES move); a re-import of a pre-4.5.0 file adding or dropping rows (no stored hash moved: 0 / 178 on the real GTR); two rows holding one execution hash; a tax figure that changes when a DIFFERENT person's account gains a trade; a total under "All accounts" with two persons; a gold ETF in the ₹1.25 L exemption; a 2007 equity sale at 15 %; an AY 2026-27 ITR-2 export emitting A3/B4; a blank head printed as ₹0; a CI red that is a Windows 5 s timeout (re-run the job, never re-tag).
- **Owed outside the release:** `/fleet-tune` (overdue since 2026-09-09; the SessionStart hook said 12.4 days on 2026-09-22; NOT run — it needs its own session) and one hook proposal awaiting the owner in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`. Owner-owed and unchanged: the non-build-machine install of 4.4.0, its WDSI, winget (HELD).

### §0.1 Pickup table — one row per open item

| # | Item | Status | Resume from EXACTLY here | Closed when |
|---|---|---|---|---|
| 1b | The FIFO auto-close rebuilt with its identity defects | **BUILT 2026-09-22** — W1 `b7863fe`, W2a `4835d44`, W3 `bad0820`, W2b `85ba973`; ON by default with the per-import toggle (owner A1) | `21-V450-BUILD/SESSION-LOG.md`; DECISIONS `^## 2026-09-22` | its release-level gate → v4.5.0 tag |
| 16 | **v4.5.0** — Session 2 (the auto-close rebuild, the Upstox plan fix, wave-3 tax, the fix list) | **8 of 10 waves BUILT** (HEAD `185ac15`); NOT bumped, NOT tagged | §0 "What is NOT built": 3b-ii → 3c → fix list → gate → bump → release ritual | owner says **"tag"** after CI 6/6 on the bump sha; release 3/3; deep verify 3/3; `releases/latest` → v4.5.0; client ZIP (rename the old one aside first); WDSI handed over |
| 2b | v4.4.0 release | PUBLISHED 2026-09-21 (`8cc6ac6`); owner-owed: the non-build-machine install, WDSI; winget HELD | archive §0.1 rows 2, 2b, 3, 4 | — |
| 6 | §3 Positions-tab redesign | NOT built; no "build" on the round-3 canvas | archive row 6; `vyuha-live-desk-v4-inputs` memory fires FIRST | owner says "build" |
| 8 / 9 / 10 | The corrected backlog ledger; §8's corrected list; Q52's sha256 on `/instruments` | unscheduled / unscheduled / **DISCHARGED for the ETF list (3a); still open for the index map** | archive rows 8, 9, 10 | each gets an owner "build" |
| 11 / 13 | Hook proposal (SENTINEL README repoint); `fleet-tune` | operator-owed / **DUE, not run** | `~/.claude/coord/learnings/HOOK-PROPOSALS.md`; its own session | applied; weekly |

### §0.2 Decisions taken this session under the decision policy (recorded in DECISIONS; the owner may overrule)
A broker-STATED bill survives a re-tag (fix-list A4); a failed un-close returns a refusal, never a 500 (A7); an equity-oriented ETF traded intraday keeps the equity-share intraday row (s.98); a s.10(38)-exempt gain is its own head `ltcgExempt1038`; the MTF/pledge GST split is a stated FLOOR (not computable in a pure module); the grandfather epoch is 2018-02-01; an `acquisition = "unknown"` row is never a lot; the merge refusal keys on an auto-close NOTE, never a bare alias.

### §0.3 Verification — PASS and FAIL signatures (V3–V6 of the v4.4.0 era are in the archive; they still describe 0071–0073 and the 4.3.0 smoke test)

**V1 — CI on the newest CODE sha.** `gh run list --limit 5`; judge only the run whose `workflowName` is `CI` with 6 jobs. **PASS:** `success`, 6/6. **FAIL-B** — a job red with a timeout / network / cold-runner signature: re-run that job; never re-tag. **FAIL-G (NEW, 2026-09-22)** — the macOS Playwright job red with 101–114 passed and ONE spec dead on `ECONNREFUSED :3100` or a half-rendered page, and `[WebServer] ⚠ Server is approaching the used memory threshold, restarting...` in the log: Next's dev-server heap watchdog (80 % of `heap_size_limit`, checked per request) restarted mid-spec; the product does not leak (57 → 58 MB over 60 previews); it is the turbopack compile cache growing with the source. `playwright.config.ts` sets `NODE_OPTIONS=--max-old-space-size=6144` (4096 is Node's own default and changed nothing) — three clean runs since. If it returns, the source grew again: raise the ceiling, do not re-run five times. **FAIL-H (NEW)** — BOTH Playwright jobs red on ONE spec with `strict mode violation: … resolved to 2 elements`: a page gained a second element matching an old locator (the ETF stamp on `/instruments` did this to `nse-map.spec.ts`); pin both, never `.first()`. **FAIL-A / C / D / E / F** as before (archive).

**V2 — the gate.** `rtk proxy npm run verify` (heavy; coord serialises it). **PASS:** exit 0 and the RAW vitest line `Test Files ≥ 467 passed`, `Tests ≥ 10,798 passed | 35 skipped`, no `expected fail` (`185ac15`); ~9,7xx on CI where the owner-file parity cases skip — re-derive the CI number from the run log, the README figure is stale. **FAIL-J (NEW, 2026-09-22)** — one or two `Test timed out in 5000ms` in DIFFERENT files on consecutive runs, each green alone, the suite's `Duration` climbing (105 → 138 → 164 s): **check free memory first** (`Get-CimInstance Win32_OperatingSystem`) — 3.9 GB free of 32 with the owner's browser holding ~14 GB made vitest's worker pool swap; a timeout that passes alone under that pressure is FAIL-G's cousin, not code; four whole-tree source scans were raised to 20 s WITH their measured times in comments — raise a third only with a measurement. **FAIL-I** (a stale production `.next` under the dev server → `rm -rf .next`), **FAIL-H** (the Live Desk vocabulary gate), **FAIL-A** (counts SHRANK), **FAIL-B** (rtk summary green, raw line red), **FAIL-C** (build red on a server-only import), **FAIL-D**, **FAIL-E** (a doc guard: fix the prose) as before.

**V7 — the release-gate Playwright pass owed before the bump:** one full run on the final tree: `z-auto-close` (5 flows) green; `/reports/tax` opened with TWO tax persons seeded shows the person picker and NO figure, one person shows the header line; the console clean of the three warnings fix-list B2 fixes.

### §0.4 Questions the owner still has to answer
1. "build" on the Positions-tab round-3 canvas (row 6). 2. **"tag" for v4.5.0** — after the three remaining waves and its release gate. 3. Row 5 / the derivative-marks schema release (archive §0.4 item 3). 4. The ~₹1 statutory-rounding residual on the Upstox option day (engine 227.69 vs ledger 226.57) — a finding to size, not a question yet. 5. Whether the A4 / A7 decisions of §0.2 stand.

## ARCHIVED 2026-09-22 by the seventeenth session — the §0 block of 2026-09-22 (v4.5.0 10 of 10 waves, BUMPED, NOT tagged, HEAD b6b8863), byte-for-byte; replaced when v4.5.0 was TAGGED and PUBLISHED (tag `v4.5.0` = `b6b8863`)

## §0 START HERE — reconciled 2026-09-22 IST by the sixteenth session: **v4.5.0 is 10 of 10 waves BUILT, COMMITTED, PUSHED and CI-green (HEAD `47088f7`), BUMPED to 4.5.0 in the commit that lands this block, NOT tagged** — tag `v4.4.0` = `8cc6ac6` remains the PUBLISHED release, `releases/latest` → v4.4.0; the owner's "tag" is the next step

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** and rots; it carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/21-V450-BUILD/SESSION-LOG.md` — the v4.5.0 wave-by-wave record (each wave's sha, gate line, what is unproven); `21-V450-BUILD/fix-list-wave.md` is the fix-list ledger (A10 discharged; A12 added and built).
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` · `package.json` `"version"` (**4.5.0** since the close-out commit) · `gh run list --limit 3` · the RAW vitest line (§0.3 V2) · `npm run drift:close-out` (0 FAIL before `/clear`; a FAIL is fixed in the DOC, never in the check).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: `06-ANSWERS.md` newest tables LAST; the design review `21-V450-BUILD/design-review.md`; `docs/DECISIONS.md` newest entries at the END (grep `^## 2026-09-22`, never read it whole — this session appended FOUR entries: 3b-ii, 3c, the fix-list wave, the bump) — binding, never re-ask one.
5. §3 of this file for where any other answer lives. Every earlier §0 block is in `VYUHA-STATE-ARCHIVE.md`, byte-for-byte (the 8-of-10 block of 2026-09-22 archived by this session).

**The one-line state (verified 2026-09-22):** published = **v4.4.0** (`8cc6ac6`). The v4.5.0 tree on `main`, oldest first, each with `npm run verify` EXIT 0 in its commit body: **U** `c0f392b` → **W1** `b7863fe` → **W2a** `4835d44` → **W3** `bad0820` → **W2b** `85ba973` → **TP** `419d53a` → **3a** `d5a6c74` → `311da07` → **3b-i** `185ac15` → `a6ff466` · `5db7543` · `f66dce1` · `ae39cf1` (docs, the drift check, the licence key moved out of the repo) → **3b-ii** `59c4242` → **3c** `af115f5` → **the fix-list wave** `47088f7` → **the bump + this block** `90036e2` → the CHANGELOG uninstall paragraph (the next commit in `git log`; CI on `90036e2` was red on ONE doc guard, `tests/uninstall-claims.test.ts` — the new changelog section lacked the uninstaller paragraph every release carries; the guard did its job, the DOC was fixed). **Gate on the `47088f7` tree: 473 files / 10,894 passed / 40 skipped**, typecheck 0, lint 6 pre-existing warnings (was recorded as 3 — stale; the six are in `app/api/live/feed/route.ts`, `components/live/tracker-client.tsx`, `scripts/license-list.mjs` ×2, `scripts/state-drift-check.mjs`, `tests/seams-v42-fix3.test.ts`), `next build` passed. **Playwright (V7, the release gate): 120 / 120 in 35 specs, 7.6 min, zero dev-server restarts**, on the fix-list tree; `zz-console-audit` 0 messages. **CI:** `59c4242` SUCCESS 6/6 first attempt; `af115f5` SUCCESS 6/6 first attempt; `47088f7` SUCCESS 6/6 first attempt (run 35748145274); `ae39cf1` green on a re-run (ubuntu's `next/font/google` fetch failed once — a runner network red, FAIL-B). **The bump commit changes version strings and the sidebar footer only; its gate is its CI run.** Nothing in the pushed tree is unverified.

- **What v4.5.0 contains** (every wave two builders + `vyuha-verifier`; each red-on-revert; the owner's rulings 2026-09-22): Upstox is plan-dependent (0074, `resolvePlan`, ONE door `ratesForTrade`, Plus seeded; D2/D3 rate corrections); ONE trade identity `executionIdentity()`; the FIFO auto-close REBUILT and ON with the toggle, the result card, Un-close, the refusals; tax per TAX PERSON (`tax-scope.ts`, the ONE widening of invariant 8); the bundled NSE ETF list + STT from `etf_*` rows; CG heads by transfer date, calendar months, P3/P4; **3b-ii = P1:** a partly-sold staged ladder's fills are realised rows in their OWN year, one per (fill × consumed FIFO tranche) — tranche DATE, moving-average BASIS — from ONE pure module `lib/analytics/realised-rows.ts`, six consumers incl. the capital summary (counted-once in EVERY consumer; oracle A1STG now 196.67, AIS sale 32100 → 33100), a closed ladder also per-fill (the FY shift, release-noted); **3c:** the ETF × tax seam pinned (`tests/seams-v45-etf-tax.test.ts`, 7 cases, rates read back through the engine); **the fix-list wave:** A1 A2 A3 A4 A7 A8 A9 A11 A12 B1 B2 B6 B7 (A4 = a `stated-bill` marker in `import_notes`, all eleven stored values survive a re-tag, pre-4.5.0 rows still re-price — stated; A3 names no rupee figure; B2 ONE live warning fixed, `e2e/zz-console-audit.spec.ts` runs LAST every pass; `e2e/z-tax-person.spec.ts` = the two-tax-person browser proof, zero ₹ under two persons); B3 owed nothing (`25dac06`).
- **What is NOT done:** the tag (owner); then the release run 3/3, `npm run release:verify v4.5.0 -- --deep`, the client ZIP (rename the old one aside first), the non-build-machine install, WDSI — all in the tag session. **Release copy:** see the pickup table row 16 for what the steward found still saying 4.4.0.
- **What WRONG looks like:** account #3 (Dhan, manual, 42 closed option rows) with net ≠ **₹75,132.75** or any moved stored column; a stored broker-stated charge moving on a re-tag (only ESTIMATES move — and only rows WITHOUT the `stated-bill` marker); a staged ladder's realised fill absent from every tax year, or counted again when the ladder closes; the capital summary and the tax base disagreeing by one fill; a tax figure that changes when a DIFFERENT person's account gains a trade; a total under "All accounts" with two persons; a gold ETF in the ₹1.25 L exemption; a 2007 equity sale at 15 %; an ITR-2 export emitting A3; a blank head printed as ₹0; `zz-console-audit` red (a React key / setState-in-render warning came back); a CI red that is a Windows 5 s timeout (re-run the job, never re-tag); a vitest 5 s timeout that MOVES between files with memory to spare (§0.3 V2 FAIL-J refined — raise the CLASS with the JSON sweep, never one per gate run).
- **Owed outside the release:** `/fleet-tune` (13.1 days overdue at this session's start; NOT run — needs its own session) and one hook proposal awaiting the owner in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`. Owner-owed and unchanged: the non-build-machine install of 4.4.0, its WDSI, winget (HELD).

### §0.1 Pickup table — one row per open item

| # | Item | Status | Resume from EXACTLY here | Closed when |
|---|---|---|---|---|
| 16 | **v4.5.0** | **10 of 10 waves BUILT; BUMPED (this commit); NOT tagged** | wait for CI 6/6 on the bump sha (`gh run list --limit 1`); then the owner says **"tag"**; then the `release` skill §4–§9 (build, `release:verify --deep`, client ZIP, install off the build machine, WDSI form — the steward's checklist is in SESSION-LOG.md) | `releases/latest` → v4.5.0; deep verify 3/3; client ZIP rebuilt; WDSI handed over |
| 16c | Release copy still at 4.4.0 | DONE in this commit — docs/client/* (README what's-new table, INSTALLATION_GUIDE 0074 + auto-close, deck chips, TERMS/PRIVACY/REFUND applies-to), docs/sales/* (landing pill + chip, brochure pill), README's v4.5.0 quote block, CHANGELOG.md v4.5.0; the client ZIP is NOT rebuilt (the tag session, after the build) | the steward's list in SESSION-LOG.md; fix-list-wave.md §C names every 4.5.0 item the copy must carry | every client/sales/README surface says 4.5.0 and its items |
| 2b | v4.4.0 release | PUBLISHED 2026-09-21 (`8cc6ac6`); owner-owed: the non-build-machine install, WDSI; winget HELD | archive §0.1 rows 2, 2b, 3, 4 | — |
| 17 | Follow-ups recorded THIS session, none scheduled | the AIS PURCHASE side of a staged ladder still states all entries in the first FY (3b-ii decision 7); `/reports/harvest` `sttSplit` counts N realisations per ladder (not ruled on); the FMV editor lists one row per trade id; the ~₹1 Upstox statutory-rounding residual | DECISIONS `^## 2026-09-22 — v4.5.0 wave 3b-ii` items 7–8 | each gets an owner "build" |
| 6 | §3 Positions-tab redesign | NOT built; no "build" on the round-3 canvas | archive row 6; `vyuha-live-desk-v4-inputs` memory fires FIRST | owner says "build" |
| 8 / 9 / 10 | The corrected backlog ledger; §8's corrected list; Q52's sha256 on `/instruments` | unscheduled / unscheduled / DISCHARGED for the ETF list, open for the index map | archive rows 8, 9, 10 | each gets an owner "build" |
| 11 / 13 | Hook proposal (SENTINEL README repoint); `fleet-tune` | operator-owed / **DUE, not run** | `~/.claude/coord/learnings/HOOK-PROPOSALS.md`; its own session | applied; weekly |

### §0.2 Decisions taken this session under the decision policy (recorded in DECISIONS; the owner may overrule)
Per-fill tax rows are per (fill × consumed tranche): the tranche gives the DATE, the moving average the BASIS; add-backs apportioned by take ÷ totalEntryQty (null stays null); entry-leg charges realise with their quantity (A1STG 196.67, not 194); the capital summary routes through the same helper (counted-once in EVERY consumer); the two pure `isOpen` guards stay; the AIS purchase side is unchanged (follow-up); A4's stated bill lives in a `stated-bill` note marker, not a column (pre-4.5.0 rows still re-price — stated); A3 carries no rupee figure; the calculator's plan is derived, never effect-synced; `daysToLongTerm` counts by calendar months; the revocation pem resolves by the mint rule in the test AND the publish script; of B2's three warnings only the strategies key was live — the other two are "not reproduced", and the console audit spec runs last in every e2e pass; a vitest 5 s timeout that moves between files with free memory is the whole-tree-scan CLASS, raised once from a JSON-reporter sweep with both measured times per case.

### §0.3 Verification — PASS and FAIL signatures (V3–V6 of the v4.4.0 era are in the archive)

**V1 — CI on the newest CODE sha.** `gh run list --limit 5`; judge only the run whose `workflowName` is `CI` with 6 jobs. **PASS:** `success`, 6/6. **FAIL-B** — a job red with a timeout / network / cold-runner signature (this session: ubuntu Playwright dead at the webServer start with `next/font/google` resolve errors = a Google Fonts fetch on the runner): re-run that job; never re-tag. **FAIL-G** — macOS Playwright red with ONE spec dead on `ECONNREFUSED :3100` and `[WebServer] ⚠ Server is approaching the used memory threshold, restarting...`: the dev-server heap watchdog; `playwright.config.ts` sets `--max-old-space-size=6144`; if it returns the source grew — raise the ceiling. **FAIL-H** — BOTH Playwright jobs red on ONE spec with `strict mode violation: … resolved to 2 elements`: pin both, never `.first()`. **FAIL-A / C / D / E / F** as before (archive).

**V2 — the gate.** `rtk proxy npm run verify` (heavy; coord serialises it; a builder NEVER runs it — `vyuha-verifier` does). **PASS:** exit 0 and the RAW vitest line `Test Files ≥ 473 passed`, `Tests ≥ 10,894 passed | 40 skipped`, no `expected fail` (`47088f7`); 40 skipped = the 35 owner-file cases + 5 in `tests/revocation-roundtrip.test.ts` when `VYUHA_LICENSE_PEM` is unset (with it: 10 passed); ~9,7xx on CI where the owner-file parity cases skip — re-derive from the run log. **FAIL-J (REFINED this session)** — `Test timed out in 5000ms` in a DIFFERENT file on each run: FIRST `Get-CimInstance Win32_OperatingSystem` — under ~4 GB free it is memory pressure (the owner's browser), not code; with memory to spare (8–10 GB this session) it is the whole-tree source-scan CLASS (each ~0.5 s alone, 3–6 s when every worker walks the tree; the suite's `Duration` climbs with the tree, 105 → 131 s) — do NOT raise one file per gate run (that cost three runs): `npx vitest run --reporter=json --outputFile=<scratch>.json`, list every case over 2.5 s under load without a raised timeout, raise them ALL to 20 s with both measured times in the comment. **FAIL-E** (a doc guard: README's file / spec / flow counts — fix the prose; this session 468 → 473 files, 33 → 35 specs, 115 → 120 flows), **FAIL-I** (a stale production `.next` under the dev server → `rm -rf .next`), **FAIL-H** (the Live Desk vocabulary gate), **FAIL-A** (counts SHRANK), **FAIL-B** (rtk summary green, raw line red), **FAIL-C** (build red on a server-only import), **FAIL-D** as before.

**V7 — the release-gate Playwright pass: DONE on the fix-list tree** (120 / 120 in 35 specs): `z-auto-close` green; `z-tax-person` (NEW) green — two persons → the picker and zero `₹` in `<main>`, one person → the header line; `zz-console-audit` (NEW, sorts last) 0 messages. **PASS signature for the next run:** `120 passed`, `PROBE-END console-audit (0 message(s))`. A red `zz-console-audit` is a product regression, never a flake.

### §0.4 Questions the owner still has to answer
1. **"tag" for v4.5.0** — after CI 6/6 on the bump sha. 2. Whether the A4 / A7 decisions and this session's §0.2 stand (in particular the `stated-bill` note marker rather than a column). 3. "build" on the Positions-tab round-3 canvas (row 6). 4. Row 5 / the derivative-marks schema release (archive §0.4 item 3). 5. The ~₹1 Upstox statutory-rounding residual — a finding to size, not a question yet. 6. Whether the AIS purchase side should split per entry leg (row 17).

## ARCHIVED 2026-09-23 by the eighteenth session — the §0 block of 2026-09-22 (v4.5.0 PUBLISHED, `/fleet-tune` DUE), byte-for-byte; replaced when the fleet-tune pass ran (no release change)

## §0 START HERE — reconciled 2026-09-22 IST by the seventeenth session: **v4.5.0 is PUBLISHED** (tag `v4.5.0` = `b6b8863`, annotated, the owner's "proceed to tag" 2026-09-22; CI 6/6 on that sha; release run 35758523118 3/3; deep verify 3/3; `releases/latest` → v4.5.0 published 17:47:49 UTC) — owner-owed: the non-build-machine install and the WDSI submission (the form was handed over in the tag session's final message)

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** and rots; it carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/21-V450-BUILD/SESSION-LOG.md` — the v4.5.0 wave-by-wave record and the tag session's ledger (each wave's sha, gate line, what is unproven); `21-V450-BUILD/fix-list-wave.md` is the fix-list ledger.
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` (**v4.5.0**) · `package.json` `"version"` (**4.5.0**) · `gh run list --limit 3` · `gh release list --limit 2` (v4.5.0 Latest) · the RAW vitest line (§0.3 V2) · `npm run drift:close-out` (0 FAIL before `/clear`; a FAIL is fixed in the DOC, never in the check).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: `06-ANSWERS.md` newest tables LAST; the design review `21-V450-BUILD/design-review.md`; `docs/DECISIONS.md` newest entries at the END (grep `^## 2026-09-22`, never read it whole — the newest is the v4.5.0 PUBLISHED entry) — binding, never re-ask one.
5. §3 of this file for where any other answer lives. Every earlier §0 block is in `VYUHA-STATE-ARCHIVE.md`, byte-for-byte (the bumped-not-tagged block of 2026-09-22 archived by this session).

**The one-line state (verified 2026-09-22, every number re-derived in the tag session):** published = **v4.5.0** (`b6b8863`, on the bump `90036e2`, on the fix-list wave `47088f7`). **Tag:** annotated, pushed only after CI run 35751084451 was 6/6 on `b6b8863` (the docs-only changelog commit was HEAD, so it was tagged rather than the bump — the v4.4.0 precedent). **Local build:** `npm run desktop:build` EXIT 0, `desktop-dist/.next/BUILD_ID` = `Q-QEnZ86fxSne9cCZ0gV1` written 22:30:49 IST this session, "stated-bill" present in 4 server chunks, the installer's `.sig` decoded to key id `4FF85F3BBE1DA21D` (Tauri base64-wraps the whole minisign file — decode twice). **Release run 35758523118:** Windows x64 / macOS Apple silicon / macOS Intel all success. **`npm run release:verify v4.5.0 -- --deep`:** 3 signatures, key id `4FF85F3BBE1DA21D`, each verified over the published bytes (34.0 / 61.0 / 63.4 MB, prehashed), "Safe to publish". **Published `--latest`** 2026-09-22 17:47:49 UTC; `latest.json` serves 4.5.0 for six platforms (darwin-aarch64 ×2, darwin-x86_64 ×2, windows-x86_64 ×2); `revocations` still `isPrerelease=true`; the Pages landing fetched live carries `4.5.0` ×2, `4.4.0` ×0, ₹1,499 ×3. **Client ZIP:** release-packages/Vyuha_4.5.0_Client_Package.zip (36.5 MB, 15 files incl. both setup guides), installer SHA-256 `86A73A21D6D8AD9B9679AB4BA7A523B3B009C3EC98E99BEC9A0DA9B0940B9865` (certutil agrees; the GitHub asset is a DIFFERENT build, sha `1EB01D33…D48B606`). **winget manifest** generated at release-packages/winget/4.5.0 from the GitHub asset sha — HELD, not submitted. **Gate on the tagged tree (unchanged since `47088f7`): 473 files / 10,894 passed / 40 skipped**, typecheck 0, lint 6 pre-existing warnings, `next build` ok; Playwright 120 / 120 in 35 specs; `npm run drift` 10 PASS / 0 FAIL / 1 SKIP at the session's start. **The live journal file on the build machine is still the installed 4.4.0** (73 migrations; 0074 lands with the owner's install): account #3 = 42 rows, gross ₹78,084.38, charges ₹2,951.63, net **₹75,132.75** — the baseline the post-install re-check must reproduce.

- **What v4.5.0 contains** (every wave two builders + `vyuha-verifier`; each red-on-revert; the owner's rulings 2026-09-22): Upstox is plan-dependent (0074, `resolvePlan`, ONE door `ratesForTrade`, Plus seeded; D2/D3 rate corrections); ONE trade identity `executionIdentity()`; the FIFO auto-close REBUILT and ON with the toggle, the result card, Un-close, the refusals; tax per TAX PERSON (`lib/queries/tax-scope.ts`, the ONE widening of invariant 8); the bundled NSE ETF list + STT from `etf_*` rows; CG heads by transfer date, calendar months, P3/P4; a partly-sold staged ladder's fills realised in their OWN year per (fill × consumed FIFO tranche) from ONE pure module `lib/analytics/realised-rows.ts`, six consumers incl. the capital summary (oracle A1STG 196.67, AIS sale 33100); the ETF × tax seam pinned (`tests/seams-v45-etf-tax.test.ts`); the fix-list wave A1 A2 A3 A4 A7 A8 A9 A11 A12 B1 B2 B6 B7 (A4 = a `stated-bill` marker in `import_notes`; A3 names no rupee figure; `e2e/zz-console-audit.spec.ts` runs LAST every pass; `e2e/z-tax-person.spec.ts` = the two-tax-person browser proof).
- **What is NOT done (owner-owed):** the non-build-machine install of 4.5.0 and its six-item smoke test (account #3 net ₹75,132.75 after migration 0074; a re-tag leaving a stated bill alone; the auto-close card; the tax-person picker under two persons; a gold ETF outside the ₹1.25 L exemption; the updater check from an installed 4.4.0 offering 4.5.0); the WDSI submission with the CLIENT ZIP hash above; winget (HELD). Also owner-owed and unchanged: the 4.4.0 install/WDSI never reported.
- **What WRONG looks like:** account #3 with net ≠ ₹75,132.75 or any moved stored column after 0074; a stored broker-stated charge moving on a re-tag (only ESTIMATES move — and only rows WITHOUT the `stated-bill` marker); a staged ladder's realised fill absent from every tax year, or counted again when the ladder closes; a tax figure that changes when a DIFFERENT person's account gains a trade; a gold ETF in the ₹1.25 L exemption; an ITR-2 export emitting A3; a blank head printed as ₹0; an installed copy refusing the 4.5.0 update (the `.sig` key id is the first thing to decode — `8FFAF1B491EAD2F0` means the stale key came back); `releases/latest` pointing anywhere but v4.5.0 (the revocation list published as a normal release would steal it); `zz-console-audit` red; a CI red that is a Windows 5 s timeout (re-run the job, never re-tag).
- **Owed outside the release:** `/fleet-tune` (DUE — 13.3 days at this session's start; NOT run, needs its own session) and one hook proposal awaiting the owner in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`.

### §0.1 Pickup table — one row per open item

| # | Item | Status | Resume from EXACTLY here | Closed when |
|---|---|---|---|---|
| 16 | **v4.5.0** | **PUBLISHED 2026-09-22** (`b6b8863`); owner-owed: the non-build-machine install + smoke test, WDSI; winget HELD | the WDSI form in the tag session's final message (SESSION-LOG.md carries a copy); the installer is release-packages/Vyuha_4.5.0_Client_Package.zip | the owner reports the install working and WDSI submitted |
| 18 | v4.6.0 scope | NOT opened; no owner ask | row 17's follow-ups + `06-ANSWERS.md` open questions; open ONLY on an owner "build" | owner names the scope |
| 17 | Follow-ups recorded in v4.5.0, none scheduled | the AIS PURCHASE side of a staged ladder still states all entries in the first FY (3b-ii decision 7); `/reports/harvest` `sttSplit` counts N realisations per ladder (not ruled on); the FMV editor lists one row per trade id; the ~₹1 Upstox statutory-rounding residual; pre-4.5.0 rows without the `stated-bill` marker still re-price on a re-tag (stated) | DECISIONS `^## 2026-09-22 — v4.5.0 wave 3b-ii` items 7–8; the fix-list entry A4 | each gets an owner "build" |
| 6 | §3 Positions-tab redesign | NOT built; no "build" on the round-3 canvas | archive row 6; `vyuha-live-desk-v4-inputs` memory fires FIRST | owner says "build" |
| 8 / 9 / 10 | The corrected backlog ledger; §8's corrected list; Q52's sha256 on `/instruments` | unscheduled / unscheduled / DISCHARGED for the ETF list, open for the index map | archive rows 8, 9, 10 | each gets an owner "build" |
| 11 / 13 | Hook proposal (SENTINEL README repoint); `fleet-tune` | operator-owed / **DUE, not run** | `~/.claude/coord/learnings/HOOK-PROPOSALS.md`; its own session | applied; weekly |

### §0.2 Decisions taken this session under the decision policy (recorded in DECISIONS; the owner may overrule)
The owner's "read, analyze, plan and then code: after reading and analyzing proceed to tag" was read as the standing "tag" — the tag, the release run, the publish `--latest` and the client ZIP followed without a further ask; the tag went on `b6b8863` (HEAD, CI-green) not the bump commit, per the v4.4.0 precedent; no 4.5.0 client ZIP existed, so nothing was renamed aside (the two 4.4.0 ZIPs stay); the winget manifest was generated (not submitted) from the GitHub asset digest; the account #3 baseline was read from a COPY of the live journal file (never the live file), and the post-0074 figure is the owner's install step, not this session's; the WDSI form quotes the CLIENT ZIP hash, category pre-emptive, detection "N/A - no detection", definition version blank. The earlier §0.2 rulings of the sixteenth session (per-fill tax rows, the `stated-bill` marker, A3 without a rupee figure, the derived calculator plan, calendar months, the FAIL-J class rule) stand unchanged. **Owner ruling 2026-09-23 (after this session): from v4.6.0 the OWNER publishes the release — a session stops after `npm run release:verify <tag> -- --deep` says "Safe to publish" and hands over; it never runs `gh release edit --draft=false --latest` again** (DECISIONS 2026-09-23, the owner publishes releases by hand).

### §0.3 Verification — PASS and FAIL signatures (V3–V6 of the v4.4.0 era are in the archive)

**V1 — CI on the newest CODE sha.** `gh run list --limit 5`; judge only the run whose `workflowName` is `CI` with 6 jobs. **PASS:** `success`, 6/6. **FAIL-B** — a job red with a timeout / network / cold-runner signature (ubuntu Playwright dead at the webServer start with `next/font/google` resolve errors = a Google Fonts fetch on the runner): re-run that job; never re-tag. **FAIL-G** — macOS Playwright red with ONE spec dead on `ECONNREFUSED :3100` and the dev server's memory-threshold restart line: the heap watchdog; `playwright.config.ts` sets `--max-old-space-size=6144`; if it returns the source grew — raise the ceiling. **FAIL-H** — BOTH Playwright jobs red on ONE spec with `strict mode violation: … resolved to 2 elements`: pin both, never `.first()`. **FAIL-A / C / D / E / F** as before (archive).

**V2 — the gate.** `rtk proxy npm run verify` (heavy; coord serialises it; a builder NEVER runs it — `vyuha-verifier` does). **PASS:** exit 0 and the RAW vitest line `Test Files ≥ 473 passed`, `Tests ≥ 10,894 passed | 40 skipped`, no `expected fail` (`47088f7`, the tagged tree); 40 skipped = the 35 owner-file cases + 5 in `tests/revocation-roundtrip.test.ts` when `VYUHA_LICENSE_PEM` is unset (with it: 10 passed); ~9,7xx on CI where the owner-file parity cases skip — re-derive from the run log. **FAIL-J** — `Test timed out in 5000ms` in a DIFFERENT file on each run: FIRST `Get-CimInstance Win32_OperatingSystem` — under ~4 GB free it is memory pressure, not code; with memory to spare it is the whole-tree source-scan CLASS — `npx vitest run --reporter=json --outputFile=<scratch>.json`, list every case over 2.5 s, raise them ALL to 20 s with both measured times in the comment, never one per gate run. **FAIL-E** (a doc guard: README's file / spec / flow counts — fix the prose), **FAIL-I** (a stale production `.next` under the dev server → `rm -rf .next`), **FAIL-H** (the Live Desk vocabulary gate), **FAIL-A** (counts SHRANK), **FAIL-B** (rtk summary green, raw line red), **FAIL-C** (build red on a server-only import), **FAIL-D** as before.

**V7 — the release-gate Playwright pass:** 120 / 120 in 35 specs on the tagged tree. **PASS signature:** `120 passed`, `PROBE-END console-audit (0 message(s))`. A red `zz-console-audit` is a product regression, never a flake.

**V8 — the release itself (NEW, the v4.5.0 tag session's signatures).** `npm run release:verify <tag> -- --deep` must print N signatures ALL `4FF85F3BBE1DA21D` and ALL "verifies over" — a key id that passes with a deep red means an asset was re-uploaded after signing: delete the draft, re-run the workflow, never re-upload by hand. `gh release list --limit 2` must show the new tag as `Latest` and `gh release view revocations --json isPrerelease` must stay `true`. A background Bash `until` loop on `gh run view --json status` is the reliable wait; a `Monitor` with a `comm`/`jq` filter emitted nothing for 30 minutes while two jobs went green (FAIL signature: silence — check the run directly). `$CLAUDE_SCRATCHPAD` is NOT set inside the Bash tool's shell here — write the absolute scratchpad path.

### §0.4 Questions the owner still has to answer
1. Did the 4.5.0 install on a non-build machine work, and is WDSI submitted (the form is in the tag session's final message)? 2. Whether the A4 / A7 decisions and the sixteenth session's §0.2 stand (in particular the `stated-bill` note marker rather than a column). 3. "build" on the Positions-tab round-3 canvas (row 6). 4. Row 5 / the derivative-marks schema release (archive §0.4 item 3). 5. The ~₹1 Upstox statutory-rounding residual — a finding to size, not a question yet. 6. Whether the AIS purchase side should split per entry leg (row 17). 7. What v4.6.0 is (row 18) — nothing is scheduled.

## ARCHIVED 2026-09-24 by the nineteenth session — the §0 block of 2026-09-23 (v4.5.0 PUBLISHED, install + WDSI owner-owed, fleet-tune done), byte-for-byte; replaced when the owner reported the install and WDSI done

## §0 START HERE — reconciled 2026-09-23 IST by the eighteenth session (the `/fleet-tune` session; no product change): **v4.5.0 is PUBLISHED** (tag `v4.5.0` = `b6b8863`, annotated, the owner's "proceed to tag" 2026-09-22; CI 6/6 on that sha; release run 35758523118 3/3; deep verify 3/3; `releases/latest` → v4.5.0 published 17:47:49 UTC) — owner-owed: the non-build-machine install and the WDSI submission (the form was handed over in the tag session's final message); `/fleet-tune` DONE 2026-09-23, TWO hook proposals await the owner

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** and rots; it carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/21-V450-BUILD/SESSION-LOG.md` — the v4.5.0 wave-by-wave record and the tag session's ledger (each wave's sha, gate line, what is unproven); `21-V450-BUILD/fix-list-wave.md` is the fix-list ledger.
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` (**v4.5.0**) · `package.json` `"version"` (**4.5.0**) · `gh run list --limit 3` · `gh release list --limit 2` (v4.5.0 Latest) · the RAW vitest line (§0.3 V2) · `npm run drift:close-out` (0 FAIL before `/clear`; a FAIL is fixed in the DOC, never in the check).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: `06-ANSWERS.md` newest tables LAST; the design review `21-V450-BUILD/design-review.md`; `docs/DECISIONS.md` newest entries at the END (grep `^## 2026-09-22`, never read it whole — the newest is the v4.5.0 PUBLISHED entry) — binding, never re-ask one.
5. §3 of this file for where any other answer lives. Every earlier §0 block is in `VYUHA-STATE-ARCHIVE.md`, byte-for-byte (the bumped-not-tagged block of 2026-09-22 archived by this session).

**The one-line state (re-verified 2026-09-23 by the eighteenth session: HEAD `06d02a9` on `a9d5c59` on `86bcd06` on `b6b8863`; CI run 35780535565 SUCCESS 6/6 on `06d02a9` — the run on `a9d5c59` shows `cancelled`, superseded by the next push seven minutes later, not a red; tag v4.5.0; `releases/latest` v4.5.0; `npm run drift` 10 PASS / 0 FAIL / 1 SKIP; `npm run verify` NOT re-run — no code changed; the tag session's numbers follow unchanged):** published = **v4.5.0** (`b6b8863`, on the bump `90036e2`, on the fix-list wave `47088f7`). **Tag:** annotated, pushed only after CI run 35751084451 was 6/6 on `b6b8863` (the docs-only changelog commit was HEAD, so it was tagged rather than the bump — the v4.4.0 precedent). **Local build:** `npm run desktop:build` EXIT 0, `desktop-dist/.next/BUILD_ID` = `Q-QEnZ86fxSne9cCZ0gV1` written 22:30:49 IST this session, "stated-bill" present in 4 server chunks, the installer's `.sig` decoded to key id `4FF85F3BBE1DA21D` (Tauri base64-wraps the whole minisign file — decode twice). **Release run 35758523118:** Windows x64 / macOS Apple silicon / macOS Intel all success. **`npm run release:verify v4.5.0 -- --deep`:** 3 signatures, key id `4FF85F3BBE1DA21D`, each verified over the published bytes (34.0 / 61.0 / 63.4 MB, prehashed), "Safe to publish". **Published `--latest`** 2026-09-22 17:47:49 UTC; `latest.json` serves 4.5.0 for six platforms (darwin-aarch64 ×2, darwin-x86_64 ×2, windows-x86_64 ×2); `revocations` still `isPrerelease=true`; the Pages landing fetched live carries `4.5.0` ×2, `4.4.0` ×0, ₹1,499 ×3. **Client ZIP:** release-packages/Vyuha_4.5.0_Client_Package.zip (36.5 MB, 15 files incl. both setup guides), installer SHA-256 `86A73A21D6D8AD9B9679AB4BA7A523B3B009C3EC98E99BEC9A0DA9B0940B9865` (certutil agrees; the GitHub asset is a DIFFERENT build, sha `1EB01D33…D48B606`). **winget manifest** generated at release-packages/winget/4.5.0 from the GitHub asset sha — HELD, not submitted. **Gate on the tagged tree (unchanged since `47088f7`): 473 files / 10,894 passed / 40 skipped**, typecheck 0, lint 6 pre-existing warnings, `next build` ok; Playwright 120 / 120 in 35 specs; `npm run drift` 10 PASS / 0 FAIL / 1 SKIP at the session's start. **The live journal file on the build machine is still the installed 4.4.0** (73 migrations; 0074 lands with the owner's install): account #3 = 42 rows, gross ₹78,084.38, charges ₹2,951.63, net **₹75,132.75** — the baseline the post-install re-check must reproduce.

- **What v4.5.0 contains** (every wave two builders + `vyuha-verifier`; each red-on-revert; the owner's rulings 2026-09-22): Upstox is plan-dependent (0074, `resolvePlan`, ONE door `ratesForTrade`, Plus seeded; D2/D3 rate corrections); ONE trade identity `executionIdentity()`; the FIFO auto-close REBUILT and ON with the toggle, the result card, Un-close, the refusals; tax per TAX PERSON (`lib/queries/tax-scope.ts`, the ONE widening of invariant 8); the bundled NSE ETF list + STT from `etf_*` rows; CG heads by transfer date, calendar months, P3/P4; a partly-sold staged ladder's fills realised in their OWN year per (fill × consumed FIFO tranche) from ONE pure module `lib/analytics/realised-rows.ts`, six consumers incl. the capital summary (oracle A1STG 196.67, AIS sale 33100); the ETF × tax seam pinned (`tests/seams-v45-etf-tax.test.ts`); the fix-list wave A1 A2 A3 A4 A7 A8 A9 A11 A12 B1 B2 B6 B7 (A4 = a `stated-bill` marker in `import_notes`; A3 names no rupee figure; `e2e/zz-console-audit.spec.ts` runs LAST every pass; `e2e/z-tax-person.spec.ts` = the two-tax-person browser proof).
- **What is NOT done (owner-owed):** the non-build-machine install of 4.5.0 and its six-item smoke test (account #3 net ₹75,132.75 after migration 0074; a re-tag leaving a stated bill alone; the auto-close card; the tax-person picker under two persons; a gold ETF outside the ₹1.25 L exemption; the updater check from an installed 4.4.0 offering 4.5.0); the WDSI submission with the CLIENT ZIP hash above; winget (HELD). Also owner-owed and unchanged: the 4.4.0 install/WDSI never reported.
- **What WRONG looks like:** account #3 with net ≠ ₹75,132.75 or any moved stored column after 0074; a stored broker-stated charge moving on a re-tag (only ESTIMATES move — and only rows WITHOUT the `stated-bill` marker); a staged ladder's realised fill absent from every tax year, or counted again when the ladder closes; a tax figure that changes when a DIFFERENT person's account gains a trade; a gold ETF in the ₹1.25 L exemption; an ITR-2 export emitting A3; a blank head printed as ₹0; an installed copy refusing the 4.5.0 update (the `.sig` key id is the first thing to decode — `8FFAF1B491EAD2F0` means the stale key came back); `releases/latest` pointing anywhere but v4.5.0 (the revocation list published as a normal release would steal it); `zz-console-audit` red; a CI red that is a Windows 5 s timeout (re-run the job, never re-tag).
- **Owed outside the release:** `/fleet-tune` RAN 2026-09-23 in this session (14.3 days since the last tune; 45 sessions / 274 hook decisions read; report at `~/.claude/coord/learnings/reports/2026-09-23.md`; prompts only — six global agent files edited, a `researcher` agent added, the two VYUHA agent prompts told HOW to read DECISIONS and never to heredoc an interpreter; committed in the `~/.claude` repo; window marked tuned). **TWO hook proposals now await the owner** in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`: the SENTINEL hooks-README repoint (2026-09-10) and, new, `coord/config.json` still registering SENTINEL at the pre-move `TRADE-SENTINAL` path (2026-09-23 — until it is applied the stale-path detector reports every SENTINEL citation stale and the repo-ownership rule cannot match `SENTINEL/bot`). Next tune due ~2026-09-30.

### §0.1 Pickup table — one row per open item

| # | Item | Status | Resume from EXACTLY here | Closed when |
|---|---|---|---|---|
| 16 | **v4.5.0** | **PUBLISHED 2026-09-22** (`b6b8863`); owner-owed: the non-build-machine install + smoke test, WDSI; winget HELD | the WDSI form in the tag session's final message (SESSION-LOG.md carries a copy); the installer is release-packages/Vyuha_4.5.0_Client_Package.zip | the owner reports the install working and WDSI submitted |
| 18 | v4.6.0 scope | NOT opened; no owner ask | row 17's follow-ups + `06-ANSWERS.md` open questions; open ONLY on an owner "build" | owner names the scope |
| 17 | Follow-ups recorded in v4.5.0, none scheduled | the AIS PURCHASE side of a staged ladder still states all entries in the first FY (3b-ii decision 7); `/reports/harvest` `sttSplit` counts N realisations per ladder (not ruled on); the FMV editor lists one row per trade id; the ~₹1 Upstox statutory-rounding residual; pre-4.5.0 rows without the `stated-bill` marker still re-price on a re-tag (stated) | DECISIONS `^## 2026-09-22 — v4.5.0 wave 3b-ii` items 7–8; the fix-list entry A4 | each gets an owner "build" |
| 6 | §3 Positions-tab redesign | NOT built; no "build" on the round-3 canvas | archive row 6; `vyuha-live-desk-v4-inputs` memory fires FIRST | owner says "build" |
| 8 / 9 / 10 | The corrected backlog ledger; §8's corrected list; Q52's sha256 on `/instruments` | unscheduled / unscheduled / DISCHARGED for the ETF list, open for the index map | archive rows 8, 9, 10 | each gets an owner "build" |
| 11 / 13 | Hook proposals ×2 (SENTINEL README repoint; `coord/config.json` SENTINEL root → `SENTINEL/bot`); `fleet-tune` | operator-owed ×2 / **DONE 2026-09-23** | `~/.claude/coord/learnings/HOOK-PROPOSALS.md` (both `[PENDING]`); next tune ~2026-09-30, its own session | both markers flipped to `[APPLIED …]` or `[REJECTED …]`; weekly |

### §0.2 Decisions taken this session under the decision policy (recorded in DECISIONS; the owner may overrule)
The owner's "read, analyze, plan and then code: after reading and analyzing proceed to tag" was read as the standing "tag" — the tag, the release run, the publish `--latest` and the client ZIP followed without a further ask; the tag went on `b6b8863` (HEAD, CI-green) not the bump commit, per the v4.4.0 precedent; no 4.5.0 client ZIP existed, so nothing was renamed aside (the two 4.4.0 ZIPs stay); the winget manifest was generated (not submitted) from the GitHub asset digest; the account #3 baseline was read from a COPY of the live journal file (never the live file), and the post-0074 figure is the owner's install step, not this session's; the WDSI form quotes the CLIENT ZIP hash, category pre-emptive, detection "N/A - no detection", definition version blank. The earlier §0.2 rulings of the sixteenth session (per-fill tax rows, the `stated-bill` marker, A3 without a rupee figure, the derived calculator plan, calendar months, the FAIL-J class rule) stand unchanged. **Owner ruling 2026-09-23 (after this session): from v4.6.0 the OWNER publishes the release — a session stops after `npm run release:verify <tag> -- --deep` says "Safe to publish" and hands over; it never runs `gh release edit --draft=false --latest` again** (DECISIONS 2026-09-23, the owner publishes releases by hand). **Eighteenth session (2026-09-23), under the decision policy:** the fleet-tune pass edited prompts only and wrote the coord config root as a PROPOSAL (never an unlock); the new `researcher` agent runs on Opus per the standing model default; the `a9d5c59` CI run's `cancelled` was read as superseded, not red (the next push's run is 6/6); nothing else was decided (DECISIONS 2026-09-23, the fleet-tune pass).

### §0.3 Verification — PASS and FAIL signatures (V3–V6 of the v4.4.0 era are in the archive)

**V1 — CI on the newest CODE sha.** `gh run list --limit 5`; judge only the run whose `workflowName` is `CI` with 6 jobs. **PASS:** `success`, 6/6. **FAIL-B** — a job red with a timeout / network / cold-runner signature (ubuntu Playwright dead at the webServer start with `next/font/google` resolve errors = a Google Fonts fetch on the runner): re-run that job; never re-tag. **FAIL-G** — macOS Playwright red with ONE spec dead on `ECONNREFUSED :3100` and the dev server's memory-threshold restart line: the heap watchdog; `playwright.config.ts` sets `--max-old-space-size=6144`; if it returns the source grew — raise the ceiling. **FAIL-H** — BOTH Playwright jobs red on ONE spec with `strict mode violation: … resolved to 2 elements`: pin both, never `.first()`. **FAIL-A / C / D / E / F** as before (archive).

**V2 — the gate.** `rtk proxy npm run verify` (heavy; coord serialises it; a builder NEVER runs it — `vyuha-verifier` does). **PASS:** exit 0 and the RAW vitest line `Test Files ≥ 473 passed`, `Tests ≥ 10,894 passed | 40 skipped`, no `expected fail` (`47088f7`, the tagged tree); 40 skipped = the 35 owner-file cases + 5 in `tests/revocation-roundtrip.test.ts` when `VYUHA_LICENSE_PEM` is unset (with it: 10 passed); ~9,7xx on CI where the owner-file parity cases skip — re-derive from the run log. **FAIL-J** — `Test timed out in 5000ms` in a DIFFERENT file on each run: FIRST `Get-CimInstance Win32_OperatingSystem` — under ~4 GB free it is memory pressure, not code; with memory to spare it is the whole-tree source-scan CLASS — `npx vitest run --reporter=json --outputFile=<scratch>.json`, list every case over 2.5 s, raise them ALL to 20 s with both measured times in the comment, never one per gate run. **FAIL-E** (a doc guard: README's file / spec / flow counts — fix the prose), **FAIL-I** (a stale production `.next` under the dev server → `rm -rf .next`), **FAIL-H** (the Live Desk vocabulary gate), **FAIL-A** (counts SHRANK), **FAIL-B** (rtk summary green, raw line red), **FAIL-C** (build red on a server-only import), **FAIL-D** as before.

**V7 — the release-gate Playwright pass:** 120 / 120 in 35 specs on the tagged tree. **PASS signature:** `120 passed`, `PROBE-END console-audit (0 message(s))`. A red `zz-console-audit` is a product regression, never a flake.

**V8 — the release itself (NEW, the v4.5.0 tag session's signatures).** `npm run release:verify <tag> -- --deep` must print N signatures ALL `4FF85F3BBE1DA21D` and ALL "verifies over" — a key id that passes with a deep red means an asset was re-uploaded after signing: delete the draft, re-run the workflow, never re-upload by hand. `gh release list --limit 2` must show the new tag as `Latest` and `gh release view revocations --json isPrerelease` must stay `true`. A background Bash `until` loop on `gh run view --json status` is the reliable wait; a `Monitor` with a `comm`/`jq` filter emitted nothing for 30 minutes while two jobs went green (FAIL signature: silence — check the run directly). `$CLAUDE_SCRATCHPAD` is NOT set inside the Bash tool's shell here — write the absolute scratchpad path.

### §0.4 Questions the owner still has to answer
1. Did the 4.5.0 install on a non-build machine work, and is WDSI submitted (the form is in the tag session's final message)? 2. Whether the A4 / A7 decisions and the sixteenth session's §0.2 stand (in particular the `stated-bill` note marker rather than a column). 3. "build" on the Positions-tab round-3 canvas (row 6). 4. Row 5 / the derivative-marks schema release (archive §0.4 item 3). 5. The ~₹1 Upstox statutory-rounding residual — a finding to size, not a question yet. 6. Whether the AIS purchase side should split per entry leg (row 17). 7. What v4.6.0 is (row 18) — nothing is scheduled.

## ARCHIVED 2026-09-24 by the twentieth session — the §0 block of 2026-09-24 (v4.5.0 CLOSED by the owner, nothing scheduled), byte-for-byte; replaced when the owner opened v4.6.0/v4.7.0 and the spec was written

## §0 START HERE — reconciled 2026-09-24 IST by the nineteenth session (the owner's report; no product change): **v4.5.0 is PUBLISHED, INSTALLED off the build machine and WDSI-SUBMITTED — the owner, 2026-09-24: "I have finished my steps and all are working and correct"; NO REWORK on any completed item, in this or any later session** (tag `v4.5.0` = `b6b8863`, annotated, the owner's "proceed to tag" 2026-09-22; CI 6/6 on that sha; release run 35758523118 3/3; deep verify 3/3; `releases/latest` → v4.5.0 published 17:47:49 UTC) — nothing release-side is owner-owed any more; winget stays HELD by choice; `/fleet-tune` DONE 2026-09-23; TWO hook proposals still `[PENDING]` on disk (verified 2026-09-24, NOT part of the owner's report)

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** and rots; it carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/21-V450-BUILD/SESSION-LOG.md` — the v4.5.0 wave-by-wave record and the tag session's ledger (each wave's sha, gate line, what is unproven); `21-V450-BUILD/fix-list-wave.md` is the fix-list ledger.
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` (**v4.5.0**) · `package.json` `"version"` (**4.5.0**) · `gh run list --limit 3` · `gh release list --limit 2` (v4.5.0 Latest) · the RAW vitest line (§0.3 V2) · `npm run drift:close-out` (0 FAIL before `/clear`; a FAIL is fixed in the DOC, never in the check).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: `06-ANSWERS.md` newest tables LAST; the design review `21-V450-BUILD/design-review.md`; `docs/DECISIONS.md` newest entries at the END (grep `^## 2026-09-22`, never read it whole — the newest is the v4.5.0 PUBLISHED entry) — binding, never re-ask one.
5. §3 of this file for where any other answer lives. Every earlier §0 block is in `VYUHA-STATE-ARCHIVE.md`, byte-for-byte (the bumped-not-tagged block of 2026-09-22 archived by this session).

**The one-line state (re-verified 2026-09-24 by the nineteenth session: HEAD `16ea079` on `06d02a9` on `a9d5c59`; CI run 35894541800 SUCCESS 6/6 on `16ea079`; tag v4.5.0 = `b6b8863`; `releases/latest` v4.5.0; `npm run drift` 0 FAIL; `npm run verify` NOT re-run — no code changed since the tagged tree; the tag session's numbers follow unchanged):** published = **v4.5.0** (`b6b8863`, on the bump `90036e2`, on the fix-list wave `47088f7`). **Tag:** annotated, pushed only after CI run 35751084451 was 6/6 on `b6b8863` (the docs-only changelog commit was HEAD, so it was tagged rather than the bump — the v4.4.0 precedent). **Local build:** `npm run desktop:build` EXIT 0, `desktop-dist/.next/BUILD_ID` = `Q-QEnZ86fxSne9cCZ0gV1` written 22:30:49 IST this session, "stated-bill" present in 4 server chunks, the installer's `.sig` decoded to key id `4FF85F3BBE1DA21D` (Tauri base64-wraps the whole minisign file — decode twice). **Release run 35758523118:** Windows x64 / macOS Apple silicon / macOS Intel all success. **`npm run release:verify v4.5.0 -- --deep`:** 3 signatures, key id `4FF85F3BBE1DA21D`, each verified over the published bytes (34.0 / 61.0 / 63.4 MB, prehashed), "Safe to publish". **Published `--latest`** 2026-09-22 17:47:49 UTC; `latest.json` serves 4.5.0 for six platforms (darwin-aarch64 ×2, darwin-x86_64 ×2, windows-x86_64 ×2); `revocations` still `isPrerelease=true`; the Pages landing fetched live carries `4.5.0` ×2, `4.4.0` ×0, ₹1,499 ×3. **Client ZIP:** release-packages/Vyuha_4.5.0_Client_Package.zip (36.5 MB, 15 files incl. both setup guides), installer SHA-256 `86A73A21D6D8AD9B9679AB4BA7A523B3B009C3EC98E99BEC9A0DA9B0940B9865` (certutil agrees; the GitHub asset is a DIFFERENT build, sha `1EB01D33…D48B606`). **winget manifest** generated at release-packages/winget/4.5.0 from the GitHub asset sha — HELD, not submitted. **Gate on the tagged tree (unchanged since `47088f7`): 473 files / 10,894 passed / 40 skipped**, typecheck 0, lint 6 pre-existing warnings, `next build` ok; Playwright 120 / 120 in 35 specs; `npm run drift` 10 PASS / 0 FAIL / 1 SKIP at the session's start. **The live journal file on the build machine is still the installed 4.4.0** (73 migrations; 0074 lands with the owner's install): account #3 = 42 rows, gross ₹78,084.38, charges ₹2,951.63, net **₹75,132.75** — the baseline the post-install re-check must reproduce.

- **What v4.5.0 contains** (every wave two builders + `vyuha-verifier`; each red-on-revert; the owner's rulings 2026-09-22): Upstox is plan-dependent (0074, `resolvePlan`, ONE door `ratesForTrade`, Plus seeded; D2/D3 rate corrections); ONE trade identity `executionIdentity()`; the FIFO auto-close REBUILT and ON with the toggle, the result card, Un-close, the refusals; tax per TAX PERSON (`lib/queries/tax-scope.ts`, the ONE widening of invariant 8); the bundled NSE ETF list + STT from `etf_*` rows; CG heads by transfer date, calendar months, P3/P4; a partly-sold staged ladder's fills realised in their OWN year per (fill × consumed FIFO tranche) from ONE pure module `lib/analytics/realised-rows.ts`, six consumers incl. the capital summary (oracle A1STG 196.67, AIS sale 33100); the ETF × tax seam pinned (`tests/seams-v45-etf-tax.test.ts`); the fix-list wave A1 A2 A3 A4 A7 A8 A9 A11 A12 B1 B2 B6 B7 (A4 = a `stated-bill` marker in `import_notes`; A3 names no rupee figure; `e2e/zz-console-audit.spec.ts` runs LAST every pass; `e2e/z-tax-person.spec.ts` = the two-tax-person browser proof).
- **DONE by the owner (reported 2026-09-24, verified on the owner's machine, not by a session):** the non-build-machine install of 4.5.0 and its six-item smoke test (account #3 net ₹75,132.75 after migration 0074; a re-tag leaving a stated bill alone; the auto-close card; the tax-person picker under two persons; a gold ETF outside the ₹1.25 L exemption; the updater check from an installed 4.4.0 offering 4.5.0) — "all are working and correct"; the WDSI submission with the CLIENT ZIP hash above. The 4.4.0 install/WDSI question is subsumed (4.5.0 is installed over it). **winget stays HELD** (the owner's standing choice, not an owed step). **Do not re-run, re-check or rebuild any of these — the owner has closed them.**
- **What WRONG looks like:** account #3 with net ≠ ₹75,132.75 or any moved stored column after 0074; a stored broker-stated charge moving on a re-tag (only ESTIMATES move — and only rows WITHOUT the `stated-bill` marker); a staged ladder's realised fill absent from every tax year, or counted again when the ladder closes; a tax figure that changes when a DIFFERENT person's account gains a trade; a gold ETF in the ₹1.25 L exemption; an ITR-2 export emitting A3; a blank head printed as ₹0; an installed copy refusing the 4.5.0 update (the `.sig` key id is the first thing to decode — `8FFAF1B491EAD2F0` means the stale key came back); `releases/latest` pointing anywhere but v4.5.0 (the revocation list published as a normal release would steal it); `zz-console-audit` red; a CI red that is a Windows 5 s timeout (re-run the job, never re-tag).
- **Owed outside the release:** `/fleet-tune` RAN 2026-09-23 in this session (14.3 days since the last tune; 45 sessions / 274 hook decisions read; report at `~/.claude/coord/learnings/reports/2026-09-23.md`; prompts only — six global agent files edited, a `researcher` agent added, the two VYUHA agent prompts told HOW to read DECISIONS and never to heredoc an interpreter; committed in the `~/.claude` repo; window marked tuned). **TWO hook proposals now await the owner** in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`: the SENTINEL hooks-README repoint (2026-09-10) and, new, `coord/config.json` still registering SENTINEL at the pre-move `TRADE-SENTINAL` path (2026-09-23 — until it is applied the stale-path detector reports every SENTINEL citation stale and the repo-ownership rule cannot match `SENTINEL/bot`). Next tune due ~2026-09-30.

### §0.1 Pickup table — one row per open item

| # | Item | Status | Resume from EXACTLY here | Closed when |
|---|---|---|---|---|
| 16 | **v4.5.0** | **CLOSED 2026-09-24** — PUBLISHED 2026-09-22 (`b6b8863`), installed off the build machine, smoke test passed, WDSI submitted (owner report); winget HELD by choice | nothing — no rework | closed |
| 18 | v4.6.0 scope | NOT opened; no owner ask | row 17's follow-ups + `06-ANSWERS.md` open questions; open ONLY on an owner "build" | owner names the scope |
| 17 | Follow-ups recorded in v4.5.0, none scheduled | the AIS PURCHASE side of a staged ladder still states all entries in the first FY (3b-ii decision 7); `/reports/harvest` `sttSplit` counts N realisations per ladder (not ruled on); the FMV editor lists one row per trade id; the ~₹1 Upstox statutory-rounding residual; pre-4.5.0 rows without the `stated-bill` marker still re-price on a re-tag (stated) | DECISIONS `^## 2026-09-22 — v4.5.0 wave 3b-ii` items 7–8; the fix-list entry A4 | each gets an owner "build" |
| 6 | §3 Positions-tab redesign | NOT built; no "build" on the round-3 canvas | archive row 6; `vyuha-live-desk-v4-inputs` memory fires FIRST | owner says "build" |
| 8 / 9 / 10 | The corrected backlog ledger; §8's corrected list; Q52's sha256 on `/instruments` | unscheduled / unscheduled / DISCHARGED for the ETF list, open for the index map | archive rows 8, 9, 10 | each gets an owner "build" |
| 11 / 13 | Hook proposals ×2 (SENTINEL README repoint; `coord/config.json` SENTINEL root → `SENTINEL/bot`); `fleet-tune` | operator-owed ×2 / **DONE 2026-09-23** | `~/.claude/coord/learnings/HOOK-PROPOSALS.md` (both `[PENDING]`); next tune ~2026-09-30, its own session | both markers flipped to `[APPLIED …]` or `[REJECTED …]`; weekly |

### §0.2 Decisions taken this session under the decision policy (recorded in DECISIONS; the owner may overrule)
The owner's "read, analyze, plan and then code: after reading and analyzing proceed to tag" was read as the standing "tag" — the tag, the release run, the publish `--latest` and the client ZIP followed without a further ask; the tag went on `b6b8863` (HEAD, CI-green) not the bump commit, per the v4.4.0 precedent; no 4.5.0 client ZIP existed, so nothing was renamed aside (the two 4.4.0 ZIPs stay); the winget manifest was generated (not submitted) from the GitHub asset digest; the account #3 baseline was read from a COPY of the live journal file (never the live file), and the post-0074 figure is the owner's install step, not this session's; the WDSI form quotes the CLIENT ZIP hash, category pre-emptive, detection "N/A - no detection", definition version blank. The earlier §0.2 rulings of the sixteenth session (per-fill tax rows, the `stated-bill` marker, A3 without a rupee figure, the derived calculator plan, calendar months, the FAIL-J class rule) stand unchanged. **Owner ruling 2026-09-23 (after this session): from v4.6.0 the OWNER publishes the release — a session stops after `npm run release:verify <tag> -- --deep` says "Safe to publish" and hands over; it never runs `gh release edit --draft=false --latest` again** (DECISIONS 2026-09-23, the owner publishes releases by hand). **Eighteenth session (2026-09-23), under the decision policy:** the fleet-tune pass edited prompts only and wrote the coord config root as a PROPOSAL (never an unlock); the new `researcher` agent runs on Opus per the standing model default; the `a9d5c59` CI run's `cancelled` was read as superseded, not red (the next push's run is 6/6); nothing else was decided (DECISIONS 2026-09-23, the fleet-tune pass). **Nineteenth session (2026-09-24):** the owner's "I have finished my steps and all are working and correct" was read as the items row 16 listed as owner-owed (the install + six-item smoke test, WDSI) — the two hook proposals were checked on disk and are still `[PENDING]` and coord's SENTINEL root is unchanged, so they were NOT read as done; no number was re-derived from the owner's machine (the post-0074 account #3 figure is owner-verified); "no rework after clear" is recorded here, in row 16 and in DECISIONS, because STATE §0 is what a cleared session reads (DECISIONS 2026-09-24).

### §0.3 Verification — PASS and FAIL signatures (V3–V6 of the v4.4.0 era are in the archive)

**V1 — CI on the newest CODE sha.** `gh run list --limit 5`; judge only the run whose `workflowName` is `CI` with 6 jobs. **PASS:** `success`, 6/6. **FAIL-B** — a job red with a timeout / network / cold-runner signature (ubuntu Playwright dead at the webServer start with `next/font/google` resolve errors = a Google Fonts fetch on the runner): re-run that job; never re-tag. **FAIL-G** — macOS Playwright red with ONE spec dead on `ECONNREFUSED :3100` and the dev server's memory-threshold restart line: the heap watchdog; `playwright.config.ts` sets `--max-old-space-size=6144`; if it returns the source grew — raise the ceiling. **FAIL-H** — BOTH Playwright jobs red on ONE spec with `strict mode violation: … resolved to 2 elements`: pin both, never `.first()`. **FAIL-A / C / D / E / F** as before (archive).

**V2 — the gate.** `rtk proxy npm run verify` (heavy; coord serialises it; a builder NEVER runs it — `vyuha-verifier` does). **PASS:** exit 0 and the RAW vitest line `Test Files ≥ 473 passed`, `Tests ≥ 10,894 passed | 40 skipped`, no `expected fail` (`47088f7`, the tagged tree); 40 skipped = the 35 owner-file cases + 5 in `tests/revocation-roundtrip.test.ts` when `VYUHA_LICENSE_PEM` is unset (with it: 10 passed); ~9,7xx on CI where the owner-file parity cases skip — re-derive from the run log. **FAIL-J** — `Test timed out in 5000ms` in a DIFFERENT file on each run: FIRST `Get-CimInstance Win32_OperatingSystem` — under ~4 GB free it is memory pressure, not code; with memory to spare it is the whole-tree source-scan CLASS — `npx vitest run --reporter=json --outputFile=<scratch>.json`, list every case over 2.5 s, raise them ALL to 20 s with both measured times in the comment, never one per gate run. **FAIL-E** (a doc guard: README's file / spec / flow counts — fix the prose), **FAIL-I** (a stale production `.next` under the dev server → `rm -rf .next`), **FAIL-H** (the Live Desk vocabulary gate), **FAIL-A** (counts SHRANK), **FAIL-B** (rtk summary green, raw line red), **FAIL-C** (build red on a server-only import), **FAIL-D** as before.

**V7 — the release-gate Playwright pass:** 120 / 120 in 35 specs on the tagged tree. **PASS signature:** `120 passed`, `PROBE-END console-audit (0 message(s))`. A red `zz-console-audit` is a product regression, never a flake.

**V8 — the release itself (NEW, the v4.5.0 tag session's signatures).** `npm run release:verify <tag> -- --deep` must print N signatures ALL `4FF85F3BBE1DA21D` and ALL "verifies over" — a key id that passes with a deep red means an asset was re-uploaded after signing: delete the draft, re-run the workflow, never re-upload by hand. `gh release list --limit 2` must show the new tag as `Latest` and `gh release view revocations --json isPrerelease` must stay `true`. A background Bash `until` loop on `gh run view --json status` is the reliable wait; a `Monitor` with a `comm`/`jq` filter emitted nothing for 30 minutes while two jobs went green (FAIL signature: silence — check the run directly). `$CLAUDE_SCRATCHPAD` is NOT set inside the Bash tool's shell here — write the absolute scratchpad path.

### §0.4 Questions the owner still has to answer
1. ~~Did the 4.5.0 install work / is WDSI submitted~~ — ANSWERED 2026-09-24: yes, both. 2. Whether the A4 / A7 decisions and the sixteenth session's §0.2 stand (in particular the `stated-bill` note marker rather than a column). 3. "build" on the Positions-tab round-3 canvas (row 6). 4. Row 5 / the derivative-marks schema release (archive §0.4 item 3). 5. The ~₹1 Upstox statutory-rounding residual — a finding to size, not a question yet. 6. Whether the AIS purchase side should split per entry leg (row 17). 7. What v4.6.0 is (row 18) — nothing is scheduled.

## ARCHIVED 2026-09-25 by the twenty-first session — the §0 block of 2026-09-24 (v4.6.0 SPECIFIED, W1 next), byte-for-byte; replaced when W1 (market calendar) was built

## §0 START HERE — reconciled 2026-09-24 IST by the twentieth session (the v4.6.0/v4.7.0 SCOPE session; research + rulings + spec, no product code): **v4.5.0 is the published release and is CLOSED** (tag `v4.5.0` = `b6b8863`; installed off the build machine, smoke-tested, WDSI submitted — owner, 2026-09-24; NO REWORK on any closed item). **v4.6.0 is OPENED and SPECIFIED, not built**: `VYUHA/LIVE-DESK-RESEARCH/22-V460-BUILD/00-SPEC-PLAN.md` holds the owner's rulings (§0), the seven waves W1–W7, and v4.7.0's five. **A LIVE correctness defect found:** SEBI's Closing Auction Session (effective 2026-08-03) is unknown to the code — marks persist before the official close and post-15:30 fills read as off-session (W1 fixes it). Two hook proposals still `[PENDING]`.

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** and rots; it carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/22-V460-BUILD/00-SPEC-PLAN.md` — the v4.6.0/v4.7.0 spec and wave plan (rulings table §0, waves §2–§3, questions owed §4, the owner's download list §6); its `research/R0…R4` are the evidence (never re-run them). `docs/LEDGER.md` is the tagged record ([LESSON] [FIX] [UPGRADE] [RESEARCH] [DECISION]) — grep it before building anything. `22-V460-BUILD/SESSION-LOG.md` is the per-session ledger and carries the ONE paste block; `21-V450-BUILD/SESSION-LOG.md` is v4.5.0 history only. Research is OUTSIDE the repo — cite it as `VYUHA/LIVE-DESK-RESEARCH/…`, never as a repo-relative path.
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` (**v4.5.0**) · `package.json` `"version"` (**4.5.0**) · `gh run list --limit 3` (a plain list, never `--commit`) · `gh release list --limit 2` (v4.5.0 Latest) · the RAW vitest line (§0.3 V2) · `npm run drift:close-out` (0 FAIL before `/clear`; a FAIL is fixed in the DOC, never in the check).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: the spec's §0 table; `06-ANSWERS.md` for older ones; `docs/DECISIONS.md` newest entries at the END (grep `^## 2026-09-24`, never read it whole — the newest two are the v4.6.0/v4.7.0 scope entry and the alignment entry) — binding, never re-ask one.
5. §3 of this file for where any other answer lives. Every earlier §0 block is in `VYUHA-STATE-ARCHIVE.md`, byte-for-byte (the nineteenth session's block archived by this session).

**The one-line state (re-verified 2026-09-24 by the twentieth session at its close: HEAD `b3d71c4` (the scope commit; CI run 35915435942 SUCCESS 6/6) + the alignment-audit commit on top, on `ea3b369`; newest migration **0074** (the next, 0075, is W6's `side`); tag v4.5.0 = `b6b8863`; `releases/latest` v4.5.0; `npm run drift` 10 PASS / 0 FAIL / 1 SKIP; `npm run verify` NOT re-run — no PRODUCT code changed since the tagged tree; the alignment commit edits only `scripts/state-drift-check.mjs`, which now reads the session log §0 names instead of a hard-coded `21-V450-BUILD` path, and its `tests/state-drift.test.ts` was run):** published = **v4.5.0**. **Gate on the tagged tree (unchanged since `47088f7`): 473 files / 10,894 passed / 40 skipped**; Playwright 120 / 120 in 35 specs. The tag session's release evidence (release run 35758523118, deep verify 3/3 key id `4FF85F3BBE1DA21D`, client ZIP sha `86A73A21…0B9865`, winget HELD) is in the archived blocks and DECISIONS 2026-09-22 — v4.5.0 is closed; nothing there is re-checked.

- **What the twentieth session did:** five research reports (R0 inventory; R1 prescriptive analytics; R2 help desk; R3 stock universe; R4 market calendar — subagents wrote them to disk, ≤ 450-word summaries came back); three owner question groups (12 questions, all answered); the spec; `docs/LEDGER.md` (5 [RESEARCH], 3 [LESSON], 2 [DECISION]); DECISIONS 2026-09-24 scope entry; then an alignment audit at the owner's request (stale STATE §3 routes and §8.00 status, a moved path in AGENTS.md, two ledgers, the Clinic route, the paste block moved to `22-V460-BUILD/SESSION-LOG.md`, memory repointed — DECISIONS 2026-09-24 alignment entry). No code, no migration, no dependency.
- **What WRONG looks like from here:** a builder starting W1's DATA file before the owner's downloads arrive (the module and its tests may start; the JSON waits); a wave tag in git (breaks every newest-tag gate — ruling R1); a user's own sector overwritten by the universe; a band on an NSE Emerge stock; a daily mark written before 15:35 on a CAS stock after W1; an old analytics URL 404ing after W3; Shepherd.js or any AGPL dependency; the app contacting classification hosts at runtime; the Bailey/López de Prado formulas coded before they are checked against the paper.
- **Owed outside the build:** the owner's calendar source files — the exact list with file names is `VYUHA/LIVE-DESK-RESEARCH/_data/market-calendar-2026-09-24/DOWNLOAD-LIST.md` (10 items; save them into that folder); two hook proposals in `~/.claude/coord/learnings/HOOK-PROPOSALS.md` (SENTINEL README repoint; `coord/config.json` SENTINEL root → `SENTINEL/bot`), both `[PENDING]`; next `/fleet-tune` ~2026-09-30.

### §0.1 Pickup table — one row per open item

| # | Item | Status | Resume from EXACTLY here | Closed when |
|---|---|---|---|---|
| 18 | **v4.6.0** (W1 calendar → W2 universe + row 10 → W3 hubs → W4 Help + strip → W5 Q51/Atlas v3 → W6 row 9 → W7 row 17) | **OPENED + SPECIFIED 2026-09-24; not built** | spec §2 W1; W1's JSON needs the owner's files (`_data/market-calendar-2026-09-24/DOWNLOAD-LIST.md`); W5 needs the 20 06a AQ answers; W6 `side` and W7 need `vyuha-design-reviewer` first | owner publishes v4.6.0 |
| 19 | **v4.7.0** (Edge Clinic, journal Kelly, row 6 Positions tab, Q18 Telegram) | SPECIFIED; opens after 4.6.0 is published | spec §3 | owner publishes v4.7.0 |
| 6 / 8 / 9 / 10 / 17 | Rows folded into 18/19 | row 6 → v4.7.0 C4; row 8 → Q51+Atlas v3 (W5), Kelly (C3), Q18 (C5) — Q42b + openalgo facade NOT chosen; row 9 → W4 strip + W6; row 10 → W2; row 17 → W7 | the spec | with their release |
| 11 / 13 | Hook proposals ×2; `fleet-tune` | operator-owed ×2 / DONE 2026-09-23 | `~/.claude/coord/learnings/HOOK-PROPOSALS.md` (both `[PENDING]`); next tune ~2026-09-30 | markers flipped; weekly |

### §0.2 Decisions taken this session under the decision policy (recorded in DECISIONS; the owner may overrule)
**Twentieth session (2026-09-24):** no `cmdk` — the existing command palette gains a help source; one new dependency for Help (`@radix-ui/react-accordion`, lock spliced by hand); weekdays past the calendar's `coversThrough` are trading days FLAGGED "calendar not verified" with a DQ warning (not silently closed); the three hubs ship in 4.6.0 with existing tabs and the Clinic tab lands in 4.7.0; the ledger lives in the repo (`docs/LEDGER.md`) so the drift check can read it; wave tags rejected (they break the newest-tag gates). Earlier sessions' rulings (the owner publishes from v4.6.0; per-fill tax rows; the `stated-bill` marker; calendar months) stand unchanged — see the archived blocks and DECISIONS.

### §0.3 Verification — PASS and FAIL signatures (V3–V6 of the v4.4.0 era are in the archive)

**V1 — CI on the newest CODE sha.** `gh run list --limit 5`; judge only the run whose `workflowName` is `CI` with 6 jobs. **PASS:** `success`, 6/6. **FAIL-B** — a job red with a timeout / network / cold-runner signature (ubuntu Playwright dead at the webServer start with `next/font/google` resolve errors = a Google Fonts fetch on the runner): re-run that job; never re-tag. **FAIL-G** — macOS Playwright red with ONE spec dead on `ECONNREFUSED :3100` and the dev server's memory-threshold restart line: the heap watchdog; `playwright.config.ts` sets `--max-old-space-size=6144`; if it returns the source grew — raise the ceiling. **FAIL-H** — BOTH Playwright jobs red on ONE spec with `strict mode violation: … resolved to 2 elements`: pin both, never `.first()`. **FAIL-A / C / D / E / F** as before (archive).

**V2 — the gate.** `rtk proxy npm run verify` (heavy; coord serialises it; a builder NEVER runs it — `vyuha-verifier` does). **PASS:** exit 0 and the RAW vitest line `Test Files ≥ 473 passed`, `Tests ≥ 10,894 passed | 40 skipped`, no `expected fail` (`47088f7`, the tagged tree); 40 skipped = the 35 owner-file cases + 5 in `tests/revocation-roundtrip.test.ts` when `VYUHA_LICENSE_PEM` is unset (with it: 10 passed); ~9,7xx on CI where the owner-file parity cases skip — re-derive from the run log. **FAIL-J** — `Test timed out in 5000ms` in a DIFFERENT file on each run: FIRST `Get-CimInstance Win32_OperatingSystem` — under ~4 GB free it is memory pressure, not code; with memory to spare it is the whole-tree source-scan CLASS — `npx vitest run --reporter=json --outputFile=<scratch>.json`, list every case over 2.5 s, raise them ALL to 20 s with both measured times in the comment, never one per gate run. **FAIL-E** (a doc guard: README's file / spec / flow counts — fix the prose), **FAIL-I** (a stale production `.next` under the dev server → `rm -rf .next`), **FAIL-H** (the Live Desk vocabulary gate), **FAIL-A** (counts SHRANK), **FAIL-B** (rtk summary green, raw line red), **FAIL-C** (build red on a server-only import), **FAIL-D** as before.

**V7 — the release-gate Playwright pass:** 120 / 120 in 35 specs on the tagged tree. **PASS signature:** `120 passed`, `PROBE-END console-audit (0 message(s))`. A red `zz-console-audit` is a product regression, never a flake.

**V8 — the release itself (NEW, the v4.5.0 tag session's signatures).** `npm run release:verify <tag> -- --deep` must print N signatures ALL `4FF85F3BBE1DA21D` and ALL "verifies over" — a key id that passes with a deep red means an asset was re-uploaded after signing: delete the draft, re-run the workflow, never re-upload by hand. `gh release list --limit 2` must show the new tag as `Latest` and `gh release view revocations --json isPrerelease` must stay `true`. A background Bash `until` loop on `gh run view --json status` is the reliable wait; a `Monitor` with a `comm`/`jq` filter emitted nothing for 30 minutes while two jobs went green (FAIL signature: silence — check the run directly). `$CLAUDE_SCRATCHPAD` is NOT set inside the Bash tool's shell here — write the absolute scratchpad path.

### §0.4 Questions the owner still has to answer
1. Before W5: the 20 unanswered 06a AQ questions (Atlas panel v3). 2. Before W7: harvest `sttSplit` counting (recommended: one per ladder, fills listed beneath) and what "accepted" means for the ~₹1 Upstox residual. 3. Before C2 (v4.7.0): the setup-grade labels, and whether intra-trade high/low is typed or later auto-filled. 4. Still open from before: whether the A4 / A7 decisions and the sixteenth session's §0.2 stand (the `stated-bill` note marker rather than a column); row 5 / the derivative-marks schema release (archive §0.4 item 3). Everything else was answered 2026-09-24 (spec §0).

## ARCHIVED 2026-09-25 by the twenty-second session — the §0 block of 2026-09-25 (v4.6.0 W1 BUILT, W8 next), byte-for-byte; replaced when W8 (OpenAlgo safety) was built

## §0 START HERE — reconciled 2026-09-25 IST by the twenty-first session (v4.6.0 wave W1 BUILT — the market calendar): **v4.5.0 is the published release and is CLOSED** (tag `v4.5.0` = `b6b8863`; NO REWORK on any closed item). **v4.6.0 is IN BUILD: W1 (market calendar, rulings K1–K3) is DONE and committed; W8 (OpenAlgo safety — sandbox fills, Groww ÷100, MCX units, `NCO`/`NCDEX`, minimum 2.0.2.6) is NEXT, then W2.** Scope extended the same day (owner rulings B1–B6): **only v4.6.0 and v4.7.0 carry everything left** — v4.6.0 gains W8 + W9 (Fyers + Nuvama broker list, rate cards, file parsers from the owner's real exports), v4.7.0 gains C6 (native pulls Fyers, Kotak Neo, Nuvama) + C7 (OpenAlgo WebSocket feed). The LIVE defect of 2026-09-24 is FIXED in the tree (not yet released): the day's mark now waits for the official close (CAS stock 15:36, other stock 15:31), fills 15:30–16:00 since 2026-08-03 are in session, and every timing reads one calendar built from the owner's downloads. Two hook proposals still `[PENDING]`.

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** and rots; it carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/22-V460-BUILD/00-SPEC-PLAN.md` — the v4.6.0/v4.7.0 spec and wave plan (rulings table §0, waves §2–§3, questions owed §4); its `research/R0…R4` and `VYUHA/LIVE-DESK-RESEARCH/23-BROKERS-OPENALGO-2026-09-25/R5…R9` are the evidence (never re-run them). `docs/LEDGER.md` is the tagged record ([LESSON] [FIX] [UPGRADE] [RESEARCH] [DECISION]) — grep it before building anything. `22-V460-BUILD/SESSION-LOG.md` is the per-session ledger and carries the ONE paste block. Research is OUTSIDE the repo — cite it as `VYUHA/LIVE-DESK-RESEARCH/…`, never as a repo-relative path.
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` (**v4.5.0**) · `package.json` `"version"` (**4.5.0**) · `gh run list --limit 3` (a plain list, never `--commit`) · `gh release list --limit 2` (v4.5.0 Latest) · the RAW vitest line (§0.3 V2) · `npm run drift:close-out` (0 FAIL before `/clear`; a FAIL is fixed in the DOC, never in the check).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both) — it now has a "Bundled market calendar" section. Owner rulings: the spec's §0 table; `docs/DECISIONS.md` newest entries at the END (grep `^## 2026-09-2`, never read it whole — the newest two are the 2026-09-25 W1 entry and the 2026-09-25 brokers + OpenAlgo scope entry) — binding, never re-ask one.
5. §3 of this file for where any other answer lives. Every earlier §0 block is in `VYUHA-STATE-ARCHIVE.md`, byte-for-byte (the twentieth session's block archived by this session).

**The one-line state (re-verified 2026-09-25 by the twenty-first session at its close: HEAD `e8d8f58` (the W1 commit; CI 36049396558 SUCCESS 6/6, both Playwright jobs green) + the brokers/OpenAlgo scope docs commit on top (its CI is in the session log); newest migration **0074** (W1 adds none; the next, 0075, is W6's `side`); tag v4.5.0 = `b6b8863`; `releases/latest` v4.5.0; `npm run drift` 11 PASS / 0 FAIL / 1 SKIP):** published = **v4.5.0**. **Gate on the W1 tree (full vitest after the review fixes): 474 files / 10,932 passed / 35 skipped**, exit 0; `npm run verify` (vyuha-verifier) EXIT 0 on the tree just before those fixes (10,930 passed; lint 0 errors, `next build` compiled; the fixes added 2 tests and were typechecked and linted). 35 skipped = the owner-file cases; `VYUHA_LICENSE_PEM` was set, so the 5 revocation cases ran (unset: 40 skipped). Playwright last ran on the v4.5.0 tagged tree (120 / 120 in 35 specs) — W1 has not been through e2e; the release gate runs it.

- **What the twenty-first session did:** built W1 in full — `lib/domain/market-calendar.ts` (pure), `lib/data/market-calendar.json` (from the owner's 10 files in `VYUHA/LIVE-DESK-RESEARCH/_data/market-calendar-2026-09-24/`: 6 required primary, `fo_mktlots.csv` accepted as item 7, 4 extra NSE circulars as supporting; 11 anchor checks), `scripts/build-market-calendar.mjs`; the 18 R4 call sites migrated; `nse-holidays.json` retired; OpenAlgo disclosure "3" → "4" (its 15:31 sentence became false); a calendar-coverage DQ warning; `/instruments` shows the calendar's provenance; `ledger-fix-tests` added to the drift check. Independent skeptic review: 8 findings, 5 fixed, 3 recorded owner-owed (DECISIONS 2026-09-25). LEDGER U-1, F-2…F-7, D-4, L-6, L-7.
- **What WRONG looks like from here:** a SANDBOX fill (`mode: "analyze"`) or a Groww fill at 1/100th of its price imported from OpenAlgo (W8 not yet built — the risk is live in v4.5.0); a broker SDK or any AGPL code added as a dependency; a Fyers/Nuvama parser built from anything but the owner's real exports; a Kotak Neo pull shipped without its "documented, not yet verified" label; a mark row before 15:36 on an F&O stock (or before 15:31 on any stock); a `"15:30"`, `15 * 60` or `+05:30` literal outside `lib/domain/market-calendar.ts` / `lib/domain/trading-day.ts` (the scan in `tests/market-calendar.test.ts` fails); the sidebar dot green on a listed holiday; a hand-edited `market-calendar.json` (rebuild it); a W2 builder overwriting a user's own sector; a band on an NSE Emerge stock; a wave tag in git; the app contacting a classification host at runtime.
- **Owed outside the build:** the two W1 owner questions (§0.4 item 4: the stored Telegram 15:35; the band width) in DECISIONS 2026-09-25; Q24's citation (NSE/INVG/67858 A.8, found by R9) is ready for the release claims audit; the 2027 NSE holiday list when NSE publishes it (usually December) + the Muhurat 2026 circular — save into a new `_data/market-calendar-<date>/` folder and re-run the build; two hook proposals in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`, both `[PENDING]`; next `/fleet-tune` ~2026-09-30.

### §0.1 Pickup table — one row per open item

| # | Item | Status | Resume from EXACTLY here | Closed when |
|---|---|---|---|---|
| 18 | **v4.6.0** (W1 calendar ✓ → **W8 OpenAlgo safety** → W2 universe + row 10 → **W9 Fyers + Nuvama list/rates/parsers** → W3 hubs → W4 Help + strip → W5 Q51/Atlas v3 → W6 row 9 → W7 row 17) | **W1 DONE 2026-09-25; W8 next** | spec §2 W8 (`23-BROKERS-OPENALGO-2026-09-25/R6-OPENALGO-UPSTREAM.md`); then W2 (`research/R3-STOCK-UNIVERSE.md`; the crawl is a ~100-min unattended run on the owner's machine); W9 reads the owner's files in `VYUHA/BROKER-FILES-FOR-TESTING/`; W5 needs the 20 06a AQ answers; W6 `side` and W7 need `vyuha-design-reviewer` first | owner publishes v4.6.0 |
| 19 | **v4.7.0** (C1–C2 Edge Clinic, C3 journal Kelly, C4 row 6 Positions tab, C5 Q18 Telegram, **C6 native pulls Fyers / Kotak Neo / Nuvama**, **C7 OpenAlgo WebSocket feed**) | SPECIFIED; opens after 4.6.0 is published | spec §3; C6 needs `vyuha-design-reviewer` first | owner publishes v4.7.0 |
| 6 / 8 / 9 / 10 / 17 | Rows folded into 18/19 | row 6 → v4.7.0 C4; row 8 → Q51+Atlas v3 (W5), Kelly (C3), Q18 (C5); row 9 → W4 strip + W6; row 10 → W2; row 17 → W7 | the spec | with their release |
| 11 / 13 | Hook proposals ×2; `fleet-tune` | operator-owed ×2 / DONE 2026-09-23 | `~/.claude/coord/learnings/HOOK-PROPOSALS.md` (both `[PENDING]`); next tune ~2026-09-30 | markers flipped; weekly |

### §0.2 Decisions taken this session under the decision policy (recorded in DECISIONS 2026-09-25; the owner may overrule)
**Twenty-first session (2026-09-25):** `fo_mktlots.csv` accepted as DOWNLOAD-LIST item 7 and four extra NSE circulars as supporting evidence; CAS membership UNKNOWN 2026-08-03..2026-09-23 (marks take the later minute); one union `auction` band 15:30–15:40 + `postclose` 15:50–16:00, 15:30 exactly stays "close"; 2026-11-08 a special session (no bundled hours → automatic marks and the digest refuse; the button works), special sessions stay out of the 245-day annualisation; the desk reconnect 15:36 and the live window to 15:45; OpenAlgo disclosure "4"; digest fallback 15:45 with the column default untouched; `nse-holidays.json` retired; MCX rows and BSE holidays secondary; W1 built by the session itself (one wave, one owner). **Brokers + OpenAlgo (same day):** W8 before W2 (live correctness risks); minimum OpenAlgo 2.0.2.6 with a documented version source or a labelled capability check; `NCO`/`NCDEX` refused, never mapped; C6 as REST from TypeScript with no SDK dependency, each host through the egress guard + PRIVACY + its own consent; the no-AGPL rule written into AGENTS.md. The twentieth session's decisions (no `cmdk`; one Help dependency; weekdays past `coversThrough` flagged; hubs in 4.6.0; the ledger in the repo; no wave tags) stand.

### §0.3 Verification — PASS and FAIL signatures (V3–V6 of the v4.4.0 era are in the archive)

**V1 — CI on the newest CODE sha.** `gh run list --limit 5`; judge only the run whose `workflowName` is `CI` with 6 jobs. **PASS:** `success`, 6/6. **FAIL-B** — a job red with a timeout / network / cold-runner signature (ubuntu Playwright dead at the webServer start with `next/font/google` resolve errors = a Google Fonts fetch on the runner): re-run that job; never re-tag. **FAIL-G** — macOS Playwright red with ONE spec dead on `ECONNREFUSED :3100` and the dev server's memory-threshold restart line: the heap watchdog; `playwright.config.ts` sets `--max-old-space-size=6144`. **FAIL-H** — BOTH Playwright jobs red on ONE spec with `strict mode violation: … resolved to 2 elements`: pin both, never `.first()`. **FAIL-K (new)** — a Playwright spec that asserts the market is open/closed, a session label, or a mark written "after 15:30": W1 moved those minutes (open to 15:40, marks 15:31/15:36, window 15:45) — re-pin to the calendar's answer, never to a literal. **FAIL-A / C / D / E / F** as before (archive).

**V2 — the gate.** `rtk proxy npm run verify` (heavy; coord serialises it; a builder NEVER runs it — `vyuha-verifier` does). **PASS:** exit 0 and the RAW vitest line `Test Files ≥ 474 passed`, `Tests ≥ 10,932 passed | 35 skipped` with `VYUHA_LICENSE_PEM` set (unset: 5 fewer passed, 40 skipped), no `expected fail`; ~9,7xx on CI where the owner-file parity cases skip — re-derive from the run log. **FAIL-J** — `Test timed out in 5000ms` in a DIFFERENT file on each run: FIRST `Get-CimInstance Win32_OperatingSystem` — under ~4 GB free it is memory pressure, not code; with memory to spare it is the whole-tree source-scan CLASS — raise every case over 2.5 s to 20 s with both measured times in the comment. **FAIL-L (new)** — `tests/market-calendar.test.ts` "no timing literal" red: a file spelled a timing or the IST offset — route it through `lib/domain/market-calendar.ts`, never add it to `ALLOWED`. **FAIL-E** (README's file / spec / flow counts — fix the prose), **FAIL-I** (a stale production `.next` under the dev server → `rm -rf .next`), **FAIL-H** (the Live Desk vocabulary gate), **FAIL-A** (counts SHRANK), **FAIL-B** (rtk summary green, raw line red), **FAIL-C** (build red on a server-only import), **FAIL-D** as before.

**V7 — the release-gate Playwright pass:** 120 / 120 in 35 specs on the v4.5.0 tagged tree (W1 not yet through it). **PASS signature:** `120 passed`, `PROBE-END console-audit (0 message(s))`. A red `zz-console-audit` is a product regression, never a flake.

**V8 — the release itself.** `npm run release:verify <tag> -- --deep` must print N signatures ALL `4FF85F3BBE1DA21D` and ALL "verifies over"; `gh release list --limit 2` must show the new tag as `Latest` and `gh release view revocations --json isPrerelease` must stay `true`. A background Bash `until` loop on `gh run view --json status` is the reliable wait. `$CLAUDE_SCRATCHPAD` is NOT set inside the Bash tool's shell here — write the absolute scratchpad path. The OWNER publishes (stop after `release:verify --deep`).

### §0.4 Questions the owner still has to answer
1. Before W5: the 20 unanswered 06a AQ questions (Atlas panel v3). 2. Before W7: harvest `sttSplit` counting (recommended: one per ladder, fills listed beneath) and what "accepted" means for the ~₹1 Upstox residual. 3. Before C2 (v4.7.0): the setup-grade labels, and whether intra-trade high/low is typed or later auto-filled. 4. **Answered 2026-09-25 (B1–B6, spec §0):** the release slot, the native-pull brokers, the OpenAlgo scope, the broker list, live quotes via OpenAlgo, Nuvama data. **Still open (W1):** should existing installs' stored Telegram send time 15:35 (the column default) move to the derived 15:45 — a migration or treating '15:35' as unset (recommended: a migration that moves only rows still equal to the default, before the release)? And is the 15:30–15:40 union band enough, or should unknown-membership stocks get the spec's full 15:15–15:40 window? 5. Still open from before: whether the A4 / A7 decisions and the sixteenth session's §0.2 stand (the `stated-bill` note marker rather than a column); row 5 / the derivative-marks schema release (archive §0.4 item 3).

## ARCHIVED 2026-09-25 by the twenty-third session — the §0 block of 2026-09-25 (v4.6.0 W8 BUILT, W2 next), byte-for-byte; replaced when W2 (stock universe + row 10) was built

## §0 START HERE — reconciled 2026-09-25 IST by the twenty-second session (v4.6.0 wave W8 BUILT — OpenAlgo safety; the W1 follow-ups answered): **v4.5.0 is the published release and is CLOSED** (tag `v4.5.0` = `b6b8863`; NO REWORK on any closed item). **v4.6.0 is IN BUILD: W1 (market calendar) and W8 (OpenAlgo safety) are DONE and committed; W2 (full stock universe + row 10) is NEXT, then W9 (Fyers + Nuvama).** Only v4.6.0 and v4.7.0 carry everything left (rulings B1–B6). Two live defects of the shipped v4.5.0 are fixed in the tree (not yet released): the day's mark waits for the official close (W1), and an OpenAlgo pull now refuses a sandbox tradebook, an OpenAlgo older than 2.0.2.6 (every broker), rows on `NCO`/`NCDEX`, and a zero-quantity MCX row (W8). Two hook proposals still `[PENDING]`.

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** and rots; it carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/22-V460-BUILD/00-SPEC-PLAN.md` — the v4.6.0/v4.7.0 spec and wave plan (rulings table §0, waves §2–§3, questions owed §4); its `research/R0…R4` and `VYUHA/LIVE-DESK-RESEARCH/23-BROKERS-OPENALGO-2026-09-25/R5…R9` are the evidence (never re-run them). `docs/LEDGER.md` is the tagged record ([LESSON] [FIX] [UPGRADE] [RESEARCH] [DECISION]) — grep it before building anything. `22-V460-BUILD/SESSION-LOG.md` is the per-session ledger and carries the ONE paste block. Research is OUTSIDE the repo — cite it as `VYUHA/LIVE-DESK-RESEARCH/…`, never as a repo-relative path.
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` (**v4.5.0**) · `package.json` `"version"` (**4.5.0**) · `gh run list --limit 3` (a plain list, never `--commit`) · `gh release list --limit 2` (v4.5.0 Latest) · the RAW vitest line (§0.3 V2) · `npm run drift:close-out` (0 FAIL before `/clear`; a FAIL is fixed in the DOC, never in the check).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: the spec's §0 table; `docs/DECISIONS.md` newest entries at the END (grep `^## 2026-09-2`, never read it whole — the newest three are the 2026-09-25 W1 entry, the brokers + OpenAlgo scope entry and the **W8 entry**) — binding, never re-ask one.
5. §3 of this file for where any other answer lives. Every earlier §0 block is in `VYUHA-STATE-ARCHIVE.md`, byte-for-byte (the twenty-first session's block archived by this session).

**The one-line state (re-verified 2026-09-25 by the twenty-second session at its close: the W8 commit sits on base HEAD `1d4d4f8` — its sha and CI are in the session log; newest migration **0075** (W8's Telegram send time; W6's `side` is now 0076+); tag v4.5.0 = `b6b8863`; `releases/latest` v4.5.0):** published = **v4.5.0**. **Gate on the W8 tree: `npm run verify` (vyuha-verifier) EXIT 0 — 476 files / 10,967 passed / 35 skipped**, lint 0 errors, `next build` compiled; 35 skipped = the owner-file cases with `VYUHA_LICENSE_PEM` set (unset: 40 skipped). Playwright last ran on CI for the W1 commit (both jobs green); W8 changes no screen a spec drives except the Settings feed line.

- **What the twenty-second session did:** asked the owed W1 follow-ups + the W8 group (4 answers, all recommended — DECISIONS 2026-09-25 W8 entry); built migration **0075** (stored Telegram send time '15:35' → '15:45' only where still the default; schema default '15:45'); built W8 in full — the version gate on OpenAlgo's documented `GET /auth/app-info` (present since 2.0.0.0), sandbox (`mode: "analyze"`) refusal, unpriceable-exchange refusal, MCX no-repair + trade-value note, the feed's version warning beside "Feed OK", the upgrade guide in `docs/OPENALGO_SETUP.md` Part 4 / the client guide / Help, disclosure "4" amended (never shipped), PRIVACY item 3; removed the dead `verifyOpenAlgoConnection`; corrected the false "saving probes /funds / fires a live check" copy (three surfaces). Red-on-revert 8/8. Independent skeptic: 0 blocking; 1 medium fixed (a stopped OpenAlgo was told to upgrade); 3 low recorded. CI on the W8 commit was 5/6: a doc-copy test that had passed vacuously on every Windows run (a CRLF-blind paragraph split) caught a new card sentence on Linux — both fixed in a follow-up commit. LEDGER U-2, F-8…F-12, D-6, L-10.
- **What WRONG looks like from here:** an OpenAlgo pull that imports while `/auth/app-info` says < 2.0.2.6, answers 404, or is unreachable; a sandbox (`mode: "analyze"`) fill in the journal; an `NCO`/`NCDEX` row filed under NSE or MCX; a zero-quantity MCX row "repaired"; a refusal telling a user whose OpenAlgo is simply stopped to upgrade; any surface again saying the save step makes a network call; `OPENALGO_DISCLOSURE_VERSION` bumped to "5" before v4.6.0 ships (amend "4"); a fresh install whose `telegram_send_time` reads 15:35; a broker SDK or AGPL code added as a dependency; a Fyers/Nuvama parser built from anything but the owner's real exports; a mark row before 15:36 on an F&O stock; a `"15:30"` / `15 * 60` / `+05:30` literal outside the calendar; a W2 builder overwriting a user's own sector; a band on an NSE Emerge stock; a wave tag in git.
- **Owed outside the build:** a LIVE OpenAlgo 2.0.2.6 preview pull (owner, before the v4.6.0 release — record the version in `docs/OPENALGO_SETUP.md` Part 4 and DECISIONS); Q24's citation (NSE/INVG/67858 A.8) for the release claims audit; the 2027 NSE holiday list + the Muhurat 2026 circular when NSE publishes them; two hook proposals in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`, both `[PENDING]`; next `/fleet-tune` ~2026-09-30.

### §0.1 Pickup table — one row per open item

| # | Item | Status | Resume from EXACTLY here | Closed when |
|---|---|---|---|---|
| 18 | **v4.6.0** (W1 calendar ✓ → W8 OpenAlgo safety ✓ → **W2 universe + row 10** → **W9 Fyers + Nuvama list/rates/parsers** → W3 hubs → W4 Help + strip → W5 Q51/Atlas v3 → W6 row 9 → W7 row 17) | **W1 + W8 DONE 2026-09-25; W2 next** | spec §2 W2 (`research/R3-STOCK-UNIVERSE.md`; the crawl is a ~100-min unattended run on the owner's machine; ask W2's question group at its start); W9 reads the owner's files in `VYUHA/BROKER-FILES-FOR-TESTING/`; W5 needs the 20 06a AQ answers; W6 `side` and W7 need `vyuha-design-reviewer` first | owner publishes v4.6.0 |
| 19 | **v4.7.0** (C1–C2 Edge Clinic, C3 journal Kelly, C4 row 6 Positions tab, C5 Q18 Telegram, **C6 native pulls Fyers / Kotak Neo / Nuvama**, **C7 OpenAlgo WebSocket feed**) | SPECIFIED; opens after 4.6.0 is published | spec §3; C6 needs `vyuha-design-reviewer` first | owner publishes v4.7.0 |
| 6 / 8 / 9 / 10 / 17 | Rows folded into 18/19 | row 6 → v4.7.0 C4; row 8 → Q51+Atlas v3 (W5), Kelly (C3), Q18 (C5); row 9 → W4 strip + W6; row 10 → W2; row 17 → W7 | the spec | with their release |
| 11 / 13 | Hook proposals ×2; `fleet-tune` | operator-owed ×2 / DONE 2026-09-23 | `~/.claude/coord/learnings/HOOK-PROPOSALS.md` (both `[PENDING]`); next tune ~2026-09-30 | markers flipped; weekly |

### §0.2 Decisions taken this session under the decision policy (recorded in DECISIONS 2026-09-25 W8 entry; the owner may overrule)
**Twenty-second session (2026-09-25):** the version source is `GET /auth/app-info` (documented by OpenAlgo's own upgrade procedure; `/api/v1/ping` names only the broker); a 404 = older than 2.0.0.0 or not OpenAlgo, a network failure = not running (never a version fact); a mode other than absent/"live" is refused, not only "analyze"; any STATED unmappable exchange is refused (an absent one keeps the old null hint); the spec's "Data Quality line" for refused rows is the preview warning (refused rows never reach the database); MCX stays valued at qty × price with a note where OpenAlgo's trade value disagrees; the feed warns, never refuses; the save step stays offline (Preview pull is the live check) and the copy was corrected instead; disclosure "4" amended rather than bumped ("4" never shipped); the version number kept out of PRIVACY (its IPv4 scan reads it as an address); W8 and 0075 built by the session itself. The twenty-first session's decisions (DECISIONS W1 + brokers entries) stand.

### §0.3 Verification — PASS and FAIL signatures (V3–V6 of the v4.4.0 era are in the archive)

**V1 — CI on the newest CODE sha.** `gh run list --limit 5`; judge only the run whose `workflowName` is `CI` with 6 jobs. **PASS:** `success`, 6/6. **FAIL-B** — a job red with a timeout / network / cold-runner signature (ubuntu Playwright dead at the webServer start with `next/font/google` resolve errors): re-run that job; never re-tag. **FAIL-G** — macOS Playwright red with ONE spec dead on `ECONNREFUSED :3100` and the dev server's memory-threshold restart line: the heap watchdog. **FAIL-H** — BOTH Playwright jobs red on ONE spec with `strict mode violation`: pin both, never `.first()`. **FAIL-K** — a Playwright spec asserting a market-open/closed label or a mark minute: re-pin to the calendar's answer, never to a literal. **FAIL-A / C / D / E / F** as before (archive).

**V2 — the gate.** `rtk proxy npm run verify` (heavy; coord serialises it; a builder NEVER runs it — `vyuha-verifier` does). **PASS:** exit 0 and the RAW vitest line `Test Files ≥ 476 passed`, `Tests ≥ 10,967 passed | 35 skipped` with `VYUHA_LICENSE_PEM` set (unset: 5 fewer passed, 40 skipped), no `expected fail`. **FAIL-M (new)** — `tests/quotes-egress-guard.test.ts` / `tests/seams-v41.test.ts` red on a request list: the OpenAlgo feed gained or lost a request — the pins name `/api/v1/multiquotes`, `/api/v1/funds` and the keyless `GET /auth/app-info`; a fourth endpoint needs the disclosure, PRIVACY item 3 and those pins changed together. **FAIL-N (new)** — `seams-v41` "PRIVACY item 3 … IPv4" red: a dotted version number ("2.0.2.6") in PRIVACY reads as an address; keep version numbers out of that item. **FAIL-O (new)** — a doc-copy test green on Windows and red on Linux CI: a CRLF-blind regex (`\n[ \t]*\n` never splits the CRLF checkout); make it `\r?`-aware and fix the copy it then catches — never the reverse. **FAIL-J** (a 5 s timeout in a different file each run: check free memory first), **FAIL-L** (a timing literal outside the calendar), **FAIL-E** (README's counts — fix the prose), **FAIL-I**, **FAIL-A**, **FAIL-B**, **FAIL-C**, **FAIL-D** as before.

**V7 — the release-gate Playwright pass:** 120 / 120 in 35 specs on the v4.5.0 tagged tree; CI ran both Playwright jobs green on the W1 commit. **PASS signature:** `120 passed`, `PROBE-END console-audit (0 message(s))`. A red `zz-console-audit` is a product regression, never a flake.

**V8 — the release itself.** `npm run release:verify <tag> -- --deep` must print N signatures ALL `4FF85F3BBE1DA21D` and ALL "verifies over"; `gh release list --limit 2` must show the new tag as `Latest` and `gh release view revocations --json isPrerelease` must stay `true`. A background Bash `until` loop on `gh run view --json status` is the reliable wait. `$CLAUDE_SCRATCHPAD` is NOT set inside the Bash tool's shell here — write the absolute scratchpad path. The OWNER publishes (stop after `release:verify --deep`).

### §0.4 Questions the owner still has to answer
1. Before W2 (asked at its start): W2's question group, if the crawl or the band rules leave anything open (spec §2 W2). 2. Before W5: the 20 unanswered 06a AQ questions (Atlas panel v3). 3. Before W7: harvest `sttSplit` counting (recommended: one per ladder, fills listed beneath) and what "accepted" means for the ~₹1 Upstox residual. 4. Before C2 (v4.7.0): the setup-grade labels, and whether intra-trade high/low is typed or later auto-filled. 5. **Answered 2026-09-25 (twenty-second session):** the stored Telegram 15:35 → migration 0075; the 15:30–15:40 band stands; below OpenAlgo 2.0.2.6 every broker refuses; the live 2.0.2.6 pull is owner-owed (not a question — a task). 6. Still open from before: whether the A4 / A7 decisions and the sixteenth session's §0.2 stand (the `stated-bill` note marker rather than a column); row 5 / the derivative-marks schema release (archive §0.4 item 3).

## ARCHIVED 2026-09-25 by the twenty-fourth session — the §0 block of 2026-09-25 (v4.6.0 W2 BUILT, W9 next), byte-for-byte; replaced when W9 (Fyers + Nuvama) was built

## §0 START HERE — reconciled 2026-09-25 IST by the twenty-third session (v4.6.0 wave W2 BUILT — the full stock universe + row 10): **v4.5.0 is the published release and is CLOSED** (tag `v4.5.0` = `b6b8863`; NO REWORK on any closed item). **v4.6.0 is IN BUILD: W1 (market calendar), W8 (OpenAlgo safety) and W2 (stock universe) are DONE and committed; W9 (Fyers + Nuvama list, rate cards, parsers) is NEXT, then W3…W7.** Only v4.6.0 and v4.7.0 carry everything left (rulings B1–B6). Fixed in the tree, not yet released: the day's mark waits for the official close (W1); an OpenAlgo pull refuses sandbox, pre-2.0.2.6, `NCO`/`NCDEX` and zero-quantity MCX rows (W8); sectors now come from the exchanges' own classification for 5,450 of 5,482 listed equities and the cap band is AMFI's (W2). Two hook proposals still `[PENDING]`.

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** and rots; it carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/22-V460-BUILD/00-SPEC-PLAN.md` — the v4.6.0/v4.7.0 spec and wave plan (rulings table §0, waves §2–§3, questions owed §4); its `research/R0…R4` and `VYUHA/LIVE-DESK-RESEARCH/23-BROKERS-OPENALGO-2026-09-25/R5…R9` are the evidence (never re-run them). `docs/LEDGER.md` is the tagged record ([LESSON] [FIX] [UPGRADE] [RESEARCH] [DECISION]) — grep it before building anything. `22-V460-BUILD/SESSION-LOG.md` is the per-session ledger and carries the ONE paste block. Research is OUTSIDE the repo — cite it as `VYUHA/LIVE-DESK-RESEARCH/…`, never as a repo-relative path.
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` (**v4.5.0**) · `package.json` `"version"` (**4.5.0**) · `gh run list --limit 3` (a plain list, never `--commit`) · `gh release list --limit 2` (v4.5.0 Latest) · the RAW vitest line (§0.3 V2) · `npm run drift:close-out` (0 FAIL before `/clear`; a FAIL is fixed in the DOC, never in the check).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: the spec's §0 table; `docs/DECISIONS.md` newest entries at the END (grep `^## 2026-09-2`, never read it whole — the newest four are the 2026-09-25 W1 entry, the brokers + OpenAlgo scope entry, the W8 entry and the **W2 entry**) — binding, never re-ask one.
5. §3 of this file for where any other answer lives. Every earlier §0 block is in `VYUHA-STATE-ARCHIVE.md`, byte-for-byte (the twenty-second session's block archived by this session).

**The one-line state (re-verified 2026-09-25 by the twenty-third session at its close: the W2 commit sits on base HEAD `4c04e4e` — its sha and CI are in the session log; newest migration **0075** (W2 added none; W6's `side` is 0076+); tag v4.5.0 = `b6b8863`; `releases/latest` v4.5.0):** published = **v4.5.0**. **Gate on the W2 tree: `npm run verify` (vyuha-verifier) EXIT 0 — 477 files / 11,005 passed / 35 skipped**, lint 0 errors, `next build` compiled; 35 skipped = the owner-file cases with `VYUHA_LICENSE_PEM` set (unset: 40 skipped). Playwright last ran on CI for the W8 follow-up commit (both jobs green); W2 changes `/instruments` copy and the Atlas cap tab, which no spec asserts on.

- **What the twenty-third session did:** asked W2's question group (2 answers, both recommended — DECISIONS 2026-09-25 W2 entry): the session ran the crawl itself; the universe first, `sector-map.json` only as fallback. Refreshed `lib/data/isin-symbols.json` (5,691 → 5,745, as of 2026-09-25; it now records `superseded` ISINs); extracted NSE Indices' structure (12 / 22 / 59 / 197) to `scripts/ics-structure-2023-07.json`; ran the polite crawl (NSE 6,360 + BSE 5,287 calls, 0 refusals, 57 min); built `lib/data/stock-universe.json` (99.4% classified, NSE/BSE agree 98.4% of 2,429, AMFI large 100 / mid 147 / small 4,521, Emerge blank + reason); wired the sector chain (user → universe → sector-map → index map; a ticker takes only its owner's sector), `getCapBandMap()` = AMFI, `getIndexBandMap()` = the Nifty size-index lens, the Atlas's two tables, row 10 on `/instruments`; superseded ISINs resolve to their successor. Red-on-revert 12/12. Independent skeptic: 2 medium (a ticker shared across boards took another company's sector on 5 symbols; no issuer guard on fallback joins) + 1 low-medium (reissues outside AMFI missed) + 3 low — all fixed before commit. LEDGER U-3, U-4, F-13, F-14, L-11, L-12, D-7.
- **What WRONG looks like from here:** a ticker shared across boards (MAL, GSTL, SEL, ZEAL, RAJPUTANA, FOCUS) showing the OTHER company's sector; an alias or a `superseded` pair whose two ISINs differ in characters 1–7; a band on an NSE Emerge stock; "micro" from `getCapBandMap()`; a user's own sector overwritten; the app dialling `nseindia.com/api`, `api.bseindia.com` or `amfiindia.com`; a scrip code showing as a number for a trade stated under a superseded ISIN; a universe build accepted below 95% coverage / 90% agreement or with a label outside the structure; a Fyers/Nuvama parser built from anything but the owner's real exports; a broker SDK or AGPL dependency; `OPENALGO_DISCLOSURE_VERSION` bumped to "5" before v4.6.0 ships; a `"15:30"` / `15 * 60` / `+05:30` literal outside the calendar; a wave tag in git.
- **Owed outside the build:** a LIVE OpenAlgo 2.0.2.6 preview pull (owner, before the v4.6.0 release — record the version in `docs/OPENALGO_SETUP.md` Part 4 and DECISIONS); Q24's citation (NSE/INVG/67858 A.8) for the release claims audit; the 2027 NSE holiday list + the Muhurat 2026 circular when NSE publishes them; two hook proposals in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`, both `[PENDING]`; next `/fleet-tune` ~2026-09-30.

### §0.1 Pickup table — one row per open item

| # | Item | Status | Resume from EXACTLY here | Closed when |
|---|---|---|---|---|
| 18 | **v4.6.0** (W1 calendar ✓ → W8 OpenAlgo safety ✓ → W2 universe + row 10 ✓ → **W9 Fyers + Nuvama list/rates/parsers** → W3 hubs → W4 Help + strip → W5 Q51/Atlas v3 → W6 row 9 → W7 row 17) | **W1 + W8 + W2 DONE 2026-09-25; W9 next** | spec §2 W9 (`23-BROKERS-OPENALGO-2026-09-25/R7-FYERS-NUVAMA.md` §2; the owner's files in `VYUHA/BROKER-FILES-FOR-TESTING/`; redact the Nuvama header's personal data); W5 needs the 20 06a AQ answers; W6 `side` and W7 need `vyuha-design-reviewer` first | owner publishes v4.6.0 |
| 19 | **v4.7.0** (C1–C2 Edge Clinic, C3 journal Kelly, C4 row 6 Positions tab, C5 Q18 Telegram, **C6 native pulls Fyers / Kotak Neo / Nuvama**, **C7 OpenAlgo WebSocket feed**) | SPECIFIED; opens after 4.6.0 is published | spec §3; C6 needs `vyuha-design-reviewer` first; retire `sector-map.json` there if its fallback-only count (8 on 2026-09-25) is still about zero | owner publishes v4.7.0 |
| 6 / 8 / 9 / 10 / 17 | Rows folded into 18/19 | row 6 → v4.7.0 C4; row 8 → Q51+Atlas v3 (W5), Kelly (C3), Q18 (C5); row 9 → W4 strip + W6; **row 10 → DONE in W2**; row 17 → W7 | the spec | with their release |
| 11 / 13 | Hook proposals ×2; `fleet-tune` | operator-owed ×2 / DONE 2026-09-23 | `~/.claude/coord/learnings/HOOK-PROPOSALS.md` (both `[PENDING]`); next tune ~2026-09-30 | markers flipped; weekly |

### §0.2 Decisions taken this session under the decision policy (recorded in DECISIONS 2026-09-25 W2 entry; the owner may overrule)
**Twenty-third session (2026-09-25):** the listing snapshot refreshed in the same run (one as-of; the universe carries no symbols, so the two cannot disagree); BSE crawled whole (the cross-validation); structure codes + a code → label table as storage, no market-cap figures; structure extracted by column position with one asserted repair; ALL-CAPS NSE labels never used, NSE wins a disagreement, one allowlisted label (BATLIBOI); universe rows at the `high` tier ("from official sources"); the index-map field keeps its name `capBand` (code: `IndexBand`); U3's "Data Quality line" = the `/instruments` report, not a scored issue; row 10 digests = canonical JSON (`getMapDigests()`); two Atlas tables; fallback joins and `superseded` pairs stay inside one issuer; a ticker takes only its owner's sector. The earlier sessions' decisions (DECISIONS W1, brokers, W8 entries) stand.

### §0.3 Verification — PASS and FAIL signatures (V3–V6 of the v4.4.0 era are in the archive)

**V1 — CI on the newest CODE sha.** `gh run list --limit 5`; judge only the run whose `workflowName` is `CI` with 6 jobs. **PASS:** `success`, 6/6. **FAIL-B** — a job red with a timeout / network / cold-runner signature (ubuntu Playwright dead at the webServer start with `next/font/google` resolve errors): re-run that job; never re-tag. **FAIL-G** — macOS Playwright red with ONE spec dead on `ECONNREFUSED :3100` and the dev server's memory-threshold restart line: the heap watchdog. **FAIL-H** — BOTH Playwright jobs red on ONE spec with `strict mode violation`: pin both, never `.first()`. **FAIL-K** — a Playwright spec asserting a market-open/closed label or a mark minute: re-pin to the calendar's answer, never to a literal. **FAIL-A / C / D / E / F** as before (archive).

**V2 — the gate.** `rtk proxy npm run verify` (heavy; coord serialises it; a builder NEVER runs it — `vyuha-verifier` does, and NOT while another agent is loading the machine: a concurrent skeptic made `seams-v42-fix5`'s 30 s hook time out once — FAIL-J). **PASS:** exit 0 and the RAW vitest line `Test Files ≥ 477 passed`, `Tests ≥ 11,005 passed | 35 skipped` with `VYUHA_LICENSE_PEM` set (unset: 5 fewer passed, 40 skipped), no `expected fail`. **FAIL-P (new)** — `tests/stock-universe.test.ts` "the bundled snapshot" red after a refresh: the snapshot and `isin-symbols.json` were built from different listing dates (rebuild both, in the AGENTS order) or a floor failed (never loosen a floor; fix the crawl). **FAIL-M** (the OpenAlgo request-list pins), **FAIL-N** (a dotted version in PRIVACY item 3), **FAIL-O** (a CRLF-blind doc-copy regex), **FAIL-J** (a 5 s / 30 s timeout under load: check free memory and concurrent agents first), **FAIL-L** (a timing literal outside the calendar), **FAIL-E** (README's counts — fix the prose), **FAIL-I**, **FAIL-A**, **FAIL-B**, **FAIL-C**, **FAIL-D** as before.

**V7 — the release-gate Playwright pass:** 120 / 120 in 35 specs on the v4.5.0 tagged tree; CI ran both Playwright jobs green on the W8 follow-up commit. **PASS signature:** `120 passed`, `PROBE-END console-audit (0 message(s))`. A red `zz-console-audit` is a product regression, never a flake.

**V8 — the release itself.** `npm run release:verify <tag> -- --deep` must print N signatures ALL `4FF85F3BBE1DA21D` and ALL "verifies over"; `gh release list --limit 2` must show the new tag as `Latest` and `gh release view revocations --json isPrerelease` must stay `true`. A background Bash `until` loop on `gh run view --json status` is the reliable wait. `$CLAUDE_SCRATCHPAD` is NOT set inside the Bash tool's shell here — write the absolute scratchpad path. The OWNER publishes (stop after `release:verify --deep`).

### §0.4 Questions the owner still has to answer
1. Before W9 (asked at its start): anything the owner's Fyers/Nuvama files leave open (plan per account, the Nuvama sheets to import). 2. Before W5: the 20 unanswered 06a AQ questions (Atlas panel v3). 3. Before W7: harvest `sttSplit` counting (recommended: one per ladder, fills listed beneath) and what "accepted" means for the ~₹1 Upstox residual. 4. Before C2 (v4.7.0): the setup-grade labels, and whether intra-trade high/low is typed or later auto-filled. 5. **Answered 2026-09-25 (twenty-third session):** the crawl run by the session; the universe first, `sector-map.json` as fallback. 6. Still open from before: whether the A4 / A7 decisions and the sixteenth session's §0.2 stand (the `stated-bill` note marker rather than a column); row 5 / the derivative-marks schema release (archive §0.4 item 3). 7. Noted, not asked: BSE SME companies (486) DO get an AMFI band (small) — U3 names NSE Emerge only, and AMFI ranks BSE SME; say so if Emerge-style blanking should extend to them.
---


## ARCHIVED 2026-09-25 by the twenty-fifth session — the §0 block of 2026-09-25 (v4.6.0 W9 BUILT, W3 next), byte-for-byte; replaced when W3 (analytics hubs) was built

## §0 START HERE — reconciled 2026-09-25 IST by the twenty-fourth session (v4.6.0 wave W9 BUILT — Fyers + Nuvama: broker list, rate cards, file parsers): **v4.5.0 is the published release and is CLOSED** (tag `v4.5.0` = `b6b8863`; NO REWORK on any closed item). **v4.6.0 is IN BUILD: W1 (market calendar), W8 (OpenAlgo safety), W2 (stock universe) and W9 (Fyers + Nuvama) are DONE and committed; W3 (analytics hubs) is NEXT, then W4…W7.** Only v4.6.0 and v4.7.0 carry everything left (rulings B1–B6). Fixed in the tree, not yet released: the day's mark waits for the official close (W1); an OpenAlgo pull refuses sandbox, pre-2.0.2.6, `NCO`/`NCDEX` and zero-quantity MCX rows (W8); sectors come from the exchanges' own classification and the cap band is AMFI's (W2); Fyers and Nuvama are brokers with rate cards and native file parsers, and execution ladders now sum to their parent on split lots for Zerodha too (W9). Two hook proposals still `[PENDING]`.

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** and rots; it carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/22-V460-BUILD/00-SPEC-PLAN.md` — the v4.6.0/v4.7.0 spec and wave plan (rulings table §0, waves §2–§3, questions owed §4); its `research/R0…R4` and `VYUHA/LIVE-DESK-RESEARCH/23-BROKERS-OPENALGO-2026-09-25/R5…R9` are the evidence (never re-run them). `docs/LEDGER.md` is the tagged record ([LESSON] [FIX] [UPGRADE] [RESEARCH] [DECISION]) — grep it before building anything. `22-V460-BUILD/SESSION-LOG.md` is the per-session ledger and carries the ONE paste block. Research is OUTSIDE the repo — cite it as `VYUHA/LIVE-DESK-RESEARCH/…`, never as a repo-relative path.
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` (**v4.5.0**) · `package.json` `"version"` (**4.5.0**) · `gh run list --limit 3` (a plain list, never `--commit`) · `gh release list --limit 2` (v4.5.0 Latest) · the RAW vitest line (§0.3 V2) · `npm run drift:close-out` (0 FAIL before `/clear`; a FAIL is fixed in the DOC, never in the check).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: the spec's §0 table; `docs/DECISIONS.md` newest entries at the END (grep `^## 2026-09-2`, never read it whole — the newest five are the 2026-09-25 W1 entry, the brokers + OpenAlgo scope entry, the W8 entry, the W2 entry and the **W9 entry**) — binding, never re-ask one.
5. §3 of this file for where any other answer lives. Every earlier §0 block is in `VYUHA-STATE-ARCHIVE.md`, byte-for-byte (the twenty-third session's block archived by this session).

**The one-line state (re-verified 2026-09-25 by the twenty-fourth session at its close: the W9 commit sits on `0c40e86` — its sha and CI are in the session log; newest migration **0075** (W9 added none — the rate card is TypeScript seed, refreshed from the template on every desktop start; W6's `side` is 0076+); tag v4.5.0 = `b6b8863`; `releases/latest` v4.5.0):** published = **v4.5.0**. **Gate on the W9 tree: `npm run verify` (vyuha-verifier) EXIT 0 — 477 files / 11,328 passed / 35 skipped**, lint 0 errors (6 pre-existing warnings), `next build` "Compiled successfully"; 35 skipped = the owner-file cases with `VYUHA_LICENSE_PEM` set (unset: 40 skipped). Playwright last ran on CI for the W2 commit (both jobs green); W9 changes no page a spec asserts on (import copy, the MTF report section's two new rows, Help text).

- **What the twenty-fourth session did:** asked W9's question group (4 answers, all recommended — DECISIONS 2026-09-25 W9 entry): Nuvama legs import as trades, negotiated Nuvama plan → published plans only, Fyers default Standard, no equity export yet. Measured the owner's three real files before building (81 Fyers mirror rows; 11 byte-identical fills proven separate by Fyers' own P&L; the Nuvama report carries 63 trade legs with billed charge heads, contrary to ruling B6's description; both files show options STT at 0.15% — the repo already has that epoch). Read both brokers' rate pages (primary; no effective date anywhere). ONE Opus builder: `fyers` + `nuvama` in `BROKERS` (10), Standard/Prime and Lite Plus/Elite in `seed-data.ts` (14 broker-plans, 238 keys / 980 rows — no migration), parsers `fyers-tradebook` / `fyers-realised-pnl` (reference only) / `nuvama-pnl-report`, redacted fixtures, BROKER_FORMATS sections, `not-bundled` MTF kind, OpenAlgo Fyers supported / Nuvama not, 27 test pins re-derived by running the seed. Independent skeptic: 2 REFUTED (execution ladders over-counted on split lots — fixed for Fyers AND Zerodha via the new `lib/import/leg-allocation.ts`; Nuvama per-position charge heads pro-rated — now the consumed legs' own heads) + 4 test gaps, all fixed before commit. Verifier run 1 EXIT 1 (two stale multi-plan pins), run 2 EXIT 0. LEDGER R-11, L-13…L-15, F-15, F-16, U-5, D-8.
- **What WRONG looks like from here:** a Fyers import whose `sourceRows` is 179 or whose ANGELONE position nets to 0 (mirror rows imported) or is 2,500 (identical fills collapsed); a `trade_legs` ladder that does not sum to its parent; a Nuvama position whose brokerage head differs from the billed lines it consumed; a Fyers or Nuvama rate that is not on the broker's own page (only labelled precedents are allowed: Standard commodity = its F&O card, MTF brokerage = delivery, per-lot fees seeded per order); a Nuvama plan seeded from the owner's billed ₹10/lot; `no-mtf` on Fyers or Nuvama; `OPENALGO_DISCLOSURE_VERSION` bumped to "5" before v4.6.0 ships; personal data in `tests/fixtures/redacted/`; a `"15:30"` / `15 * 60` / `+05:30` literal outside the calendar; a wave tag in git; a ticker shared across boards showing the other company's sector (W2).
- **Owed outside the build:** a LIVE OpenAlgo 2.0.2.6 preview pull (owner, before the v4.6.0 release — record the version in `docs/OPENALGO_SETUP.md` Part 4 and DECISIONS); a Fyers tradebook and a Nuvama report WITH equity rows whenever the owner has them (the equity layouts are UNVERIFIED — `docs/BROKER_FORMATS.md`); Q24's citation (NSE/INVG/67858 A.8) for the release claims audit; the 2027 NSE holiday list + the Muhurat 2026 circular when NSE publishes them; two hook proposals in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`, both `[PENDING]`; next `/fleet-tune` ~2026-09-30.

### §0.1 Pickup table — one row per open item

| # | Item | Status | Resume from EXACTLY here | Closed when |
|---|---|---|---|---|
| 18 | **v4.6.0** (W1 calendar ✓ → W8 OpenAlgo safety ✓ → W2 universe + row 10 ✓ → W9 Fyers + Nuvama ✓ → **W3 hubs** → W4 Help + strip → W5 Q51/Atlas v3 → W6 row 9 → W7 row 17) | **W1 + W8 + W2 + W9 DONE 2026-09-25; W3 next** | spec §2 W3 (nav + routes, no maths; old URLs redirect; the F&O workspace map must hide the Expiry TAB, not the hub; Pro gates per tab); W5 needs the 20 06a AQ answers; W6 `side` and W7 need `vyuha-design-reviewer` first — W6 also owns overnight F&O shorts, which `pairLegs` still files as `opening-sell` + `open` for every broker (LEDGER D-8) | owner publishes v4.6.0 |
| 19 | **v4.7.0** (C1–C2 Edge Clinic, C3 journal Kelly, C4 row 6 Positions tab, C5 Q18 Telegram, **C6 native pulls Fyers / Kotak Neo / Nuvama**, **C7 OpenAlgo WebSocket feed**) | SPECIFIED; opens after 4.6.0 is published | spec §3; C6 needs `vyuha-design-reviewer` first (identity across file, OpenAlgo and native sources — Fyers' `/trade-history` fields are unpinned until a first real pull); retire `sector-map.json` there if its fallback-only count (8 on 2026-09-25) is still about zero | owner publishes v4.7.0 |
| 6 / 8 / 9 / 10 / 17 | Rows folded into 18/19 | row 6 → v4.7.0 C4; row 8 → Q51+Atlas v3 (W5), Kelly (C3), Q18 (C5); row 9 → W4 strip + W6; row 10 → DONE in W2; row 17 → W7 | the spec | with their release |
| 11 / 13 | Hook proposals ×2; `fleet-tune` | operator-owed ×2 / DONE 2026-09-23 | `~/.claude/coord/learnings/HOOK-PROPOSALS.md` (both `[PENDING]`); next tune ~2026-09-30 | markers flipped; weekly |

### §0.2 Decisions taken this session under the decision policy (recorded in DECISIONS 2026-09-25 W9 entry; the owner may overrule)
**Twenty-fourth session (2026-09-25):** no SQL migration for the rate rows (the code wins over the spec's wording); unpublished figures follow the seed's labelled precedents; per-lot fees seeded per order (stated approximation); Elite's delivery floor a flat ₹25; Fyers Prime ₹499 ex-GST (the Kotak Neo Pro precedent); Fyers detected by filename only (the content never names the broker); Nuvama identity = the file's own instrument string, tradingsymbol rendered Dhan-style with the full expiry, legs ordered by the CumulativeQuantity chain; a position's charge heads = the legs it consumed; the Zerodha window filter fixed in the same commit (same defect class); `ALUMINI` added to the commodity set; overnight F&O shorts left to W6; MTF `not-bundled` kind; OpenAlgo Nuvama "not available" in Sahi's shape; disclosure sentence amended, version "4" kept. The earlier sessions' decisions (DECISIONS W1, brokers, W8, W2 entries) stand.

### §0.3 Verification — PASS and FAIL signatures (V3–V6 of the v4.4.0 era are in the archive)

**V1 — CI on the newest CODE sha.** `gh run list --limit 5`; judge only the run whose `workflowName` is `CI` with 6 jobs. **PASS:** `success`, 6/6. **FAIL-B** — a job red with a timeout / network / cold-runner signature (ubuntu Playwright dead at the webServer start with `next/font/google` resolve errors): re-run that job; never re-tag. **FAIL-G** — macOS Playwright red with ONE spec dead on `ECONNREFUSED :3100` and the dev server's memory-threshold restart line: the heap watchdog. **FAIL-H** — BOTH Playwright jobs red on ONE spec with `strict mode violation`: pin both, never `.first()`. **FAIL-K** — a Playwright spec asserting a market-open/closed label or a mark minute: re-pin to the calendar's answer, never to a literal. **FAIL-A / C / D / E / F** as before (archive).

**V2 — the gate.** `rtk proxy npm run verify` (heavy; coord serialises it; a builder NEVER runs it — `vyuha-verifier` does, and NOT while another agent is loading the machine: a concurrent skeptic made `seams-v42-fix5`'s 30 s hook time out once — FAIL-J). **PASS:** exit 0 and the RAW vitest line `Test Files ≥ 477 passed`, `Tests ≥ 11,328 passed | 35 skipped` with `VYUHA_LICENSE_PEM` set (unset: 5 fewer passed, 40 skipped), no `expected fail`. **FAIL-Q (new)** — a test pinning the SET of brokers with more than one plan (`broker-plan*.test.ts`), a seed-row count (170/700-era numbers) or a broker hand-list red after a broker or plan is added: re-derive the number by RUNNING the seed and fix the pin, never the seed; the scout's list of such pins is in DECISIONS W9 (27 pins). **FAIL-P** (`tests/stock-universe.test.ts` after a refresh: rebuild both snapshots in the AGENTS order, never loosen a floor), **FAIL-M** (the OpenAlgo request-list pins), **FAIL-N** (a dotted version in PRIVACY item 3), **FAIL-O** (a CRLF-blind doc-copy regex), **FAIL-J** (a 5 s / 30 s timeout under load: check free memory and concurrent agents first), **FAIL-L** (a timing literal outside the calendar), **FAIL-E** (README's counts — fix the prose), **FAIL-I**, **FAIL-A**, **FAIL-B**, **FAIL-C**, **FAIL-D** as before.

**V7 — the release-gate Playwright pass:** 120 / 120 in 35 specs on the v4.5.0 tagged tree; CI ran both Playwright jobs green on the W2 commit. **PASS signature:** `120 passed`, `PROBE-END console-audit (0 message(s))`. A red `zz-console-audit` is a product regression, never a flake.

**V8 — the release itself.** `npm run release:verify <tag> -- --deep` must print N signatures ALL `4FF85F3BBE1DA21D` and ALL "verifies over"; `gh release list --limit 2` must show the new tag as `Latest` and `gh release view revocations --json isPrerelease` must stay `true`. A background Bash `until` loop on `gh run view --json status` is the reliable wait. `$CLAUDE_SCRATCHPAD` is NOT set inside the Bash tool's shell here — write the absolute scratchpad path. The OWNER publishes (stop after `release:verify --deep`).

### §0.4 Questions the owner still has to answer
1. **Answered 2026-09-25 (twenty-fourth session, W9):** Nuvama legs as trades; negotiated Nuvama plan → published plans only; Fyers default Standard; no equity export yet. 2. Before W5: the 20 unanswered 06a AQ questions (Atlas panel v3). 3. Before W7: harvest `sttSplit` counting (recommended: one per ladder, fills listed beneath) and what "accepted" means for the ~₹1 Upstox residual. 4. Before C2 (v4.7.0): the setup-grade labels, and whether intra-trade high/low is typed or later auto-filled. 5. Before C6: the first real Fyers `/trade-history` pull pins its fields; Kotak Neo stays "documented, not verified" (B2). 6. Still open from before: whether the A4 / A7 decisions and the sixteenth session's §0.2 stand (the `stated-bill` note marker rather than a column); row 5 / the derivative-marks schema release (archive §0.4 item 3). 7. Noted, not asked: BSE SME companies (486) DO get an AMFI band (small) — U3 names NSE Emerge only; say so if Emerge-style blanking should extend to them. 8. Noted, not asked: a Fyers tradebook or a Nuvama report WITH equity rows, whenever one exists — it pins the two UNVERIFIED equity layouts.
---


## ARCHIVED 2026-09-25 by the twenty-sixth session — the §0 block of 2026-09-25 (v4.6.0 W3 BUILT, W4 next), byte-for-byte; replaced when W4 (Help revamp + getting-started strip) was built

## §0 START HERE — reconciled 2026-09-25 IST by the twenty-fifth session (v4.6.0 wave W3 BUILT — the three analytics hubs: Edge Clinic · Capital & Expiry · Costs): **v4.5.0 is the published release and is CLOSED** (tag `v4.5.0` = `b6b8863`; NO REWORK on any closed item). **v4.6.0 is IN BUILD: W1 (market calendar), W8 (OpenAlgo safety), W2 (stock universe), W9 (Fyers + Nuvama) and W3 (analytics hubs) are DONE and committed; W4 (Help revamp + getting-started strip) is NEXT, then W5…W7.** Only v4.6.0 and v4.7.0 carry everything left (rulings B1–B6). Fixed in the tree, not yet released: the day's mark waits for the official close (W1); an OpenAlgo pull refuses sandbox, pre-2.0.2.6, `NCO`/`NCDEX` and zero-quantity MCX rows (W8); sectors come from the exchanges' own classification and the cap band is AMFI's (W2); Fyers and Nuvama are brokers with rate cards and native file parsers (W9); seven analytics screens are tabs of three hubs and their old URLs answer a real 307 (W3). Two hook proposals still `[PENDING]`.

**Read order, every session, in this order and no other:**

1. **This §0.** It outranks every other document. *If any document disagrees with §0, §0 wins and the other document is the bug — fix it in the same turn.* **Memory is not authoritative; it forks by cwd** and rots; it carries pointers and durable traps only.
2. `VYUHA/LIVE-DESK-RESEARCH/22-V460-BUILD/00-SPEC-PLAN.md` — the v4.6.0/v4.7.0 spec and wave plan (rulings table §0, waves §2–§3, questions owed §4); its `research/R0…R4` and `VYUHA/LIVE-DESK-RESEARCH/23-BROKERS-OPENALGO-2026-09-25/R5…R9` are the evidence (never re-run them). `docs/LEDGER.md` is the tagged record ([LESSON] [FIX] [UPGRADE] [RESEARCH] [DECISION]) — grep it before building anything. `22-V460-BUILD/SESSION-LOG.md` is the per-session ledger and carries the ONE paste block. Research is OUTSIDE the repo — cite it as `VYUHA/LIVE-DESK-RESEARCH/…`, never as a repo-relative path (LEDGER L-4).
3. Re-derive before asserting: `git log --oneline -3` · `git tag --sort=-creatordate | head -1` (**v4.5.0**) · `package.json` `"version"` (**4.5.0**) · `gh run list --limit 3` (a plain list, never `--commit`) · `gh release list --limit 2` (v4.5.0 Latest) · the RAW vitest line (§0.3 V2) · `npm run drift:close-out` (0 FAIL before `/clear`; a FAIL is fixed in the DOC, never in the check).
4. `AGENTS.md` before any code change (on conflict AGENTS.md wins over this file; the code wins over both). Owner rulings: the spec's §0 table; `docs/DECISIONS.md` newest entries at the END (grep `^## 2026-09-2`, never read it whole — the newest five are the 2026-09-25 W8 entry, the W2 entry, the W9 entry and the **W3 entry**, with the W1 and brokers entries before them) — binding, never re-ask one.
5. §3 of this file for where any other answer lives. Every earlier §0 block is in `VYUHA-STATE-ARCHIVE.md`, byte-for-byte (the twenty-fourth session's block archived by this session).

**The one-line state (re-verified 2026-09-25 by the twenty-fifth session at its close: the W3 commit sits on `2907833` (W9) — its sha and CI are in the session log; newest migration **0075** (W3 added none — nav, routes and redirects only; W6's `side` is 0076+); tag v4.5.0 = `b6b8863`; `releases/latest` v4.5.0):** published = **v4.5.0**. **Gate on the W3 tree: `npm run verify` (vyuha-verifier) EXIT 0 — 478 files / 11,373 passed / 35 skipped**, lint 0 errors (6 pre-existing warnings), `next build` "Compiled successfully"; 35 skipped = the owner-file cases with `VYUHA_LICENSE_PEM` set (unset: 40 skipped). Playwright: 128 flows in 36 specs by `--list` (z-hubs adds 8); CI on the W3 commit runs both jobs — W3 changes the sidebar, three new pages and the seven old URLs, so a red Playwright job there is a product signal, never a flake, unless it carries a FAIL-B/G signature.

- **What the twenty-fifth session did:** W3 from a written contract (scratchpad `W3-CONTRACT.md`) after a scout mapped the surface: all seven routes are `/reports/*`; no redirect or `?tab=` precedent existed; the Pro-gating test and `lockFor` would both have lost their guard on a `?tab=` href. ONE Opus builder (150 turns + a resume): `lib/domain/hubs.ts` (the one registry), three hub pages + `_tabs` bodies + `hub-tabs.tsx`, seven redirect stubs, nav 10 → 6, `PRO_FEATURES` / help / palette / `migrateNavHrefs` / `tabVisible` derived, 20 tests re-pointed, `tests/hubs.test.ts` (44) + `e2e/z-hubs.spec.ts` (8); 18 red-on-revert proofs. The session fixed what the builder left: `/help` would have dropped the seven tab-keyed entries (`navGroupHrefs`, pinned), README counts, the demo shot list. Measured on the dev server: a page `redirect()` streams as a 200 (`NEXT_REDIRECT`, no meta refresh) under the root loading boundary — added `next.config.ts redirects()` for a real 307 (curl-verified on all seven); a stale `.next/dev` cache 404ed UNCHANGED routes (L-18). Skeptic: 12 CONFIRMED / 0 REFUTED / 1 UNVERIFIABLE (the 307, then measured); three small findings fixed (dangling `aria-controls`, a per-tab gate pin, the e2e wire assertion). LEDGER U-6, F-17, L-16…L-18, D-9.
- **What WRONG looks like from here:** `/reports/edge` (or any of the seven) answering 200 or 404 to `curl -o /dev/null -w %{http_code}` (must be 307 + `Location`); a hub page with two `<ProGate>` literals or a tab rendered outside the one gate; the Expiry tab in the strip for an equity-only workspace when it is not the current tab, or `/reports/capital` itself hidden; a user's saved `vyuha-nav-order` that had `/reports/edge` fold-visible now folding the Edge Clinic away; `/help` showing fewer than 3 hub cards + 7 tab cards under Analytics; a `permanentRedirect` or `permanent: true` anywhere; a second literal list of the seven old hrefs outside comments and the stubs; a `_tabs` body whose diff against the old page is more than imports, wrapper, export name and the two badges; `OPENALGO_DISCLOSURE_VERSION` bumped to "5" before v4.6.0 ships; a `"15:30"` / `15 * 60` / `+05:30` literal outside the calendar; a wave tag in git; a ticker shared across boards showing the other company's sector (W2); a Fyers import whose `sourceRows` is 179 (W9 mirror rows imported).
- **Owed outside the build:** a LIVE OpenAlgo 2.0.2.6 preview pull (owner, before the v4.6.0 release — record the version in `docs/OPENALGO_SETUP.md` Part 4 and DECISIONS); a Fyers tradebook and a Nuvama report WITH equity rows whenever the owner has them (the equity layouts are UNVERIFIED — `docs/BROKER_FORMATS.md`); Q24's citation (NSE/INVG/67858 A.8) for the release claims audit; the 2027 NSE holiday list + the Muhurat 2026 circular when NSE publishes them; two hook proposals in `~/.claude/coord/learnings/HOOK-PROPOSALS.md`, both `[PENDING]`; next `/fleet-tune` ~2026-09-30; eight stray `grep.exe` "feed started / warmup" watchers from earlier sessions today (parent pid 13944, 08:27–10:13) still alive on the machine — not this session's, not killed.

### §0.1 Pickup table — one row per open item

| # | Item | Status | Resume from EXACTLY here | Closed when |
|---|---|---|---|---|
| 18 | **v4.6.0** (W1 calendar ✓ → W8 OpenAlgo safety ✓ → W2 universe + row 10 ✓ → W9 Fyers + Nuvama ✓ → W3 hubs ✓ → **W4 Help + strip** → W5 Q51/Atlas v3 → W6 row 9 → W7 row 17) | **W1 + W8 + W2 + W9 + W3 DONE 2026-09-25; W4 next** | spec §2 W4 (H1–H4 + row 9.5: help content reshaped categories → topics, `components/help/*`, the palette's Help source, Playwright screenshot capture, the getting-started strip from existing data; ONE new dep `@radix-ui/react-accordion` spliced per AGENTS "Adding a dependency" — never `npm install`; the `HELP_ENTRIES ↔ NAV_ITEMS` join test extends to hubs/tabs — `navGroupHrefs` is the hook; the global-search "screens" source still lists `NAV_ITEMS` only, D-9); W5 needs the 20 06a AQ answers; W6 `side` and W7 need `vyuha-design-reviewer` first — W6 also owns overnight F&O shorts (`pairLegs` files them `opening-sell` + `open`, LEDGER D-8) | owner publishes v4.6.0 |
| 19 | **v4.7.0** (C1–C2 Edge Clinic → the Clinic TAB of `/reports/edge-clinic` becomes the default, C3 journal Kelly, C4 row 6 Positions tab, C5 Q18 Telegram, **C6 native pulls Fyers / Kotak Neo / Nuvama**, **C7 OpenAlgo WebSocket feed**) | SPECIFIED; opens after 4.6.0 is published | spec §3; C1 adds a `clinic` tab to `HUBS` and flips `defaultTab` (the 307s stay temporary for exactly this); C6 needs `vyuha-design-reviewer` first (identity across file, OpenAlgo and native sources — Fyers' `/trade-history` fields are unpinned until a first real pull); retire `sector-map.json` there if its fallback-only count (8 on 2026-09-25) is still about zero | owner publishes v4.7.0 |
| 6 / 8 / 9 / 10 / 17 | Rows folded into 18/19 | row 6 → v4.7.0 C4; row 8 → Q51+Atlas v3 (W5), Kelly (C3), Q18 (C5); row 9 → W4 strip + W6; row 10 → DONE in W2; row 17 → W7 | the spec | with their release |
| 11 / 13 | Hook proposals ×2; `fleet-tune` | operator-owed ×2 / DONE 2026-09-23 | `~/.claude/coord/learnings/HOOK-PROPOSALS.md` (both `[PENDING]`); next tune ~2026-09-30 | markers flipped; weekly |

### §0.2 Decisions taken this session under the decision policy (recorded in DECISIONS 2026-09-25 W3 entry; the owner may overrule)
**Twenty-fifth session (2026-09-25):** the seven routes are `/reports/*` (the code wins over the spec's shorthand); ONE registry `lib/domain/hubs.ts`; a 307 from `next.config.ts redirects()` PLUS the page stubs, never a 308; only the active tab's body renders; a hidden Expiry tab still opens by deep link; help = 7 tab entries + 3 hub entries and `/help` groups through `navGroupHrefs`; palette rows "Hub › Tab"; the saved nav order migrates at the first legacy member's position, fold-visible if any member was; the two header badges moved into their tab bodies; `aria-controls` dropped from link-triggers; `readme-claims` counts a redirect-only `page.tsx` as no screen (45); the global-search screens gap left to W4. The earlier sessions' decisions (DECISIONS W1, brokers, W8, W2, W9 entries) stand.

### §0.3 Verification — PASS and FAIL signatures (V3–V6 of the v4.4.0 era are in the archive)

**V1 — CI on the newest CODE sha.** `gh run list --limit 5`; judge only the run whose `workflowName` is `CI` with 6 jobs. **PASS:** `success`, 6/6. **FAIL-B** — a job red with a timeout / network / cold-runner signature (ubuntu Playwright dead at the webServer start with `next/font/google` resolve errors; a spec stalled 90 s on a cold macOS runner with no ECONNREFUSED or memory line): re-run that job; never re-tag. **FAIL-G** — macOS Playwright red with ONE spec dead on `ECONNREFUSED :3100` and the dev server's memory-threshold restart line: the heap watchdog. **FAIL-H** — BOTH Playwright jobs red on ONE spec with `strict mode violation`: pin both, never `.first()`. **FAIL-K** — a Playwright spec asserting a market-open/closed label or a mark minute: re-pin to the calendar's answer, never to a literal. **FAIL-R (new)** — `z-hubs.spec.ts` red on `config-level redirect: expected 307` — the `redirects()` block in `next.config.ts` was removed or no longer derives every legacy href; fix the config, never the spec. **FAIL-A / C / D / E / F** as before (archive).

**V2 — the gate.** `rtk proxy npm run verify` (heavy; coord serialises it; a builder NEVER runs it — `vyuha-verifier` does, and NOT while another agent is loading the machine: a concurrent skeptic made `seams-v42-fix5`'s 30 s hook time out once — FAIL-J). **PASS:** exit 0 and the RAW vitest line `Test Files ≥ 478 passed`, `Tests ≥ 11,373 passed | 35 skipped` with `VYUHA_LICENSE_PEM` set (unset: 5 fewer passed, 40 skipped), no `expected fail`. **FAIL-S (new)** — `tests/hubs.test.ts` or `tests/pro-gating.test.ts` red after a screen is added, moved or re-keyed: every surface that keys by href (nav, `PRO_FEATURES`, help, palette, `SCREEN_DOMAIN`, the saved nav order, `/help`'s grouping) is a READER to re-point — derive from `HUBS` / `navGroupHrefs`, never add a second literal list. **FAIL-Q** — a test pinning the SET of brokers with more than one plan (`broker-plan*.test.ts`), a seed-row count or a broker hand-list red after a broker or plan is added: re-derive the number by RUNNING the seed and fix the pin, never the seed (DECISIONS W9, 27 pins). **FAIL-P** (`tests/stock-universe.test.ts` after a refresh: rebuild both snapshots in the AGENTS order, never loosen a floor), **FAIL-M** (the OpenAlgo request-list pins), **FAIL-N** (a dotted version in PRIVACY item 3), **FAIL-O** (a CRLF-blind doc-copy regex), **FAIL-J** (a 5 s / 30 s timeout under load: check free memory and concurrent agents first), **FAIL-L** (a timing literal outside the calendar), **FAIL-E** (README's counts — fix the prose; a redirect-only `page.tsx` is not a screen), **FAIL-I**, **FAIL-A**, **FAIL-B**, **FAIL-C**, **FAIL-D** as before.

**V7 — the release-gate Playwright pass:** 120 / 120 in 35 specs on the v4.5.0 tagged tree; 128 in 36 by `--list` on the W3 tree (not yet run locally; CI runs both jobs on the W3 commit). **PASS signature:** `128 passed`, `PROBE-END console-audit (0 message(s))`. A red `zz-console-audit` is a product regression, never a flake.

**V8 — the release itself.** `npm run release:verify <tag> -- --deep` must print N signatures ALL `4FF85F3BBE1DA21D` and ALL "verifies over"; `gh release list --limit 2` must show the new tag as `Latest` and `gh release view revocations --json isPrerelease` must stay `true`. A background Bash `until` loop on `gh run view --json status` is the reliable wait. `$CLAUDE_SCRATCHPAD` is NOT set inside the Bash tool's shell here — write the absolute scratchpad path. The OWNER publishes (stop after `release:verify --deep`).

**V9 — a dev-server probe (new, W3).** Before blaming a wave for a 404, probe an UNCHANGED sibling route; if it 404s too with nothing in the log, the Turbopack cache is stale: stop the server, delete `.next/dev`, restart (L-18). Prove a redirect with `curl -s -o /dev/null -w '%{http_code} %{redirect_url}'`, never by reading `redirect()`.

### §0.4 Questions the owner still has to answer
1. **Answered 2026-09-25 (twenty-fourth session, W9):** Nuvama legs as trades; negotiated Nuvama plan → published plans only; Fyers default Standard; no equity export yet. 2. Before W5: the 20 unanswered 06a AQ questions (Atlas panel v3) — asked at W5's start, in two groups of ten. 3. Before W7: harvest `sttSplit` counting (recommended: one per ladder, fills listed beneath) and what "accepted" means for the ~₹1 Upstox residual. 4. Before C2 (v4.7.0): the setup-grade labels, and whether intra-trade high/low is typed or later auto-filled. 5. Before C6: the first real Fyers `/trade-history` pull pins its fields; Kotak Neo stays "documented, not verified" (B2). 6. Still open from before: whether the A4 / A7 decisions and the sixteenth session's §0.2 stand (the `stated-bill` note marker rather than a column); row 5 / the derivative-marks schema release (archive §0.4 item 3). 7. Noted, not asked: BSE SME companies (486) DO get an AMFI band (small) — U3 names NSE Emerge only; say so if Emerge-style blanking should extend to them. 8. Noted, not asked: a Fyers tradebook or a Nuvama report WITH equity rows, whenever one exists — it pins the two UNVERIFIED equity layouts. 9. W3 and W4 owe no group (spec §4); the owner asked this session WHEN the groups get asked — at the named wave's start, never mid-build (W5's twenty at W5's start).
---
