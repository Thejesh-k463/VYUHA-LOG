# v4.6.0 / v4.7.0 LEDGER — the tagged record (owner ruling R1, 2026-09-24)

One line per entry, APPEND-ONLY, newest last. Tags: **[LESSON]** a trap and how to avoid it · **[FIX]** a defect fixed
(MUST name the test that goes red on revert) · **[UPGRADE]** a feature shipped · **[RESEARCH]** a report and what it
settled · **[DECISION]** a ruling or a session decision (the WHY lives in `docs/DECISIONS.md`).
Format: `ID | date | tag | what | evidence (sha / test / file)`. Ids: `L-`, `F-`, `U-`, `R-`, `D-` + a number.
Before building anything, grep this file and DECISIONS for the topic — a [LESSON] here is the reason it is not re-learnt.

| ID | Date | Tag | What | Evidence |
|---|---|---|---|---|
| R-1 | 2026-09-24 | [RESEARCH] | Inventory: rows 6/8/9/10/17 in full; 71 analytics modules (lib/analytics; R0 said 74 — corrected by the prose pass), all DESCRIPTIVE by their own headers; Help = 3 TS data files (~1,600 lines, 48 entries); 5,691 ISINs resolved but only 1,379 classified | `research/R0-INVENTORY.md` |
| R-2 | 2026-09-24 | [RESEARCH] | Prescriptive analytics: 18 techniques with formulas + sample guardrails; no competitor publishes a significance method; the Clinic design | `research/R1-PRESCRIPTIVE-ANALYTICS.md` (Bailey/LdP formulas UNVERIFIED — §F) |
| R-3 | 2026-09-24 | [RESEARCH] | Help UX: search-first IA, Radix/shadcn, MiniSearch, cmdk, driver.js; **Shepherd.js is AGPL — never use** | `research/R2-HELP-DESK.md` |
| R-4 | 2026-09-24 | [RESEARCH] | Universe: NSE `getSymbolData` + BSE `ComHeadernew` give the same 4-level taxonomy (28/29 agree); AMFI 5,427 rows; NSE Emerge absent from AMFI; ~98% classifiable | `research/R3-STOCK-UNIVERSE.md` |
| R-5 | 2026-09-24 | [RESEARCH] | Market calendar: CAS effective 2026-08-03 (F&O stocks' continuous session ends 15:15, auction to 15:35, F&O to 15:40, post-close 15:50–16:00); pre-open revised 2026-09-07; 21 hard-coded call sites | `research/R4-MARKET-CALENDAR.md` (only the SEBI circular primary) |
| L-1 | 2026-09-24 | [LESSON] | A calendar fact with no expiry date is a silent defect: CAS went live 2026-08-03 and nothing in the repo knew for 52 days; the holiday list ends 2026-12-31 with no warning. Every dated rule gets `effectiveFrom` + a `coversThrough` DQ warning. | R-5; W1 scan test |
| L-2 | 2026-09-24 | [LESSON] | NSE archives time out and BSE/MCX PDFs return 403 to an agent; BSE `ComHeadernew` answers only Node `fetch` (curl 403). Primary exchange files come from the OWNER's browser download, recorded with sha256. | R-4, R-5 |
| L-3 | 2026-09-24 | [LESSON] | "1,379 stocks" was never a symbol-coverage gap (5,691 ISINs resolve) — it was a CLASSIFICATION gap. Measure what a count counts before scoping a fix. | R-1 Part D |
| D-1 | 2026-09-24 | [DECISION] | Scope + two-release split + every ruling S1–S4, U1–U4, H1–H4, T1, K1–K3, C1–C4, Q18, P1–P2, R1 | `00-SPEC-PLAN.md` §0; DECISIONS 2026-09-24 scope entry |
| D-2 | 2026-09-24 | [DECISION] | Session decisions: no cmdk (extend the existing palette); one new dep `@radix-ui/react-accordion`; weekdays past `coversThrough` = trading days FLAGGED; hubs ship in 4.6.0, Clinic tab in 4.7.0; openalgo facade stays unbuilt | `00-SPEC-PLAN.md` §0 last paragraph |
| L-4 | 2026-09-24 | [LESSON] | A research path written repo-relative (`LIVE-DESK-RESEARCH/…`) opened as `TRADE-JOURNAL/LIVE-DESK-RESEARCH/…` and 404ed for the owner. Research is OUTSIDE the repo: cite `VYUHA/LIVE-DESK-RESEARCH/…` (or `../LIVE-DESK-RESEARCH/…` in a clickable link). | STATE §0 read order item 2 |
| L-5 | 2026-09-24 | [LESSON] | A folder move leaves stale paths in prose: AGENTS.md still cited `TRADE-SENTINAL/sentinel/NIFTY INDICES` 14 days after the move to `SENTINEL/bot`. `ls` every cited external path before a wave depends on it (W2 rebuilds the index map from it). | AGENTS.md "Bundled NSE index map" |
| D-3 | 2026-09-24 | [DECISION] | Alignment audit: ONE paste block (`22-V460-BUILD/SESSION-LOG.md`); ONE ledger from v4.6.0 (this file; `09-BUILD-LEDGER.md` is history); spec §0 > research > older packs; the Clinic is a hub tab (`/reports/edge-clinic?tab=clinic`); v4.6.0/v4.7.0 rulings live in the spec §0 table, and 06-ANSWERS points to it | DECISIONS 2026-09-24 alignment entry |
| F-1 | 2026-09-24 | [FIX] | `scripts/state-drift-check.mjs` read a hard-coded `21-V450-BUILD/SESSION-LOG.md`, so its cited-sha check would never see the v4.6.0 log; it now reads the highest-numbered `NN-VNNN-BUILD/SESSION-LOG.md` STATE §0 names. STATE §0 wording restored so `state-head-ancestor` and `migration-vs-state` run again (close-out 19 PASS / 4 SKIP → 21 PASS / 2 SKIP) | `tests/state-drift.test.ts` (3 passed); `npm run drift:close-out` |
| U-1 | 2026-09-25 | [UPGRADE] | W1 market calendar: `lib/domain/market-calendar.ts` (pure) + `lib/data/market-calendar.json` (18 effective-dated session rows, 19 NSE holidays, Budget Sunday + Muhurat, 210 CAS stocks) built by `scripts/build-market-calendar.mjs` from the owner's downloads with 11 anchor checks; `/instruments` shows as-of + sources sha256; DQ warns 60 days before `coversThrough` | `tests/market-calendar.test.ts` |
| F-2 | 2026-09-25 | [FIX] | The day's mark was saved from 15:30 — an F&O stock's PRE-AUCTION price since CAS (2026-08-03). Now per key: CAS stock 15:36, other stock 15:31 (K3); the desk's reconnect 15:31 → 15:36 | `tests/market-calendar.test.ts`, `tests/live-mark-persist.test.ts`, `tests/seams-v41-fix2.test.ts` |
| F-3 | 2026-09-25 | [FIX] | Fills 15:30–15:40 (CAS prints, F&O extension) and 15:50–16:00 (post-close) were filed "outside the session … worth checking the import"; bands now come from the calendar by the fill's own date | `tests/market-calendar.test.ts` |
| F-4 | 2026-09-25 | [FIX] | The sidebar dot kept its own Mon–Fri 09:15–15:30 clock (green on Republic Day, shut during the F&O 15:30–15:40 extension); desk and sidebar now share `isMarketOpen()` | `tests/market-calendar.test.ts`, `tests/live-market-hours.test.ts` |
| F-5 | 2026-09-25 | [FIX] | Three extra IST offsets (data-quality, Angel One session expiry, desk date format) and a typed `T15:30:00+05:30`; a scan now fails on any timing/offset literal outside the calendar and `trading-day.ts` | `tests/market-calendar.test.ts` |
| F-6 | 2026-09-25 | [FIX] | `previousTradingDay` / `latestBhavcopyDate` walked INTO holidays and skipped the Budget Sunday; the calendar walk skips holidays and visits special sessions | `tests/market-calendar.test.ts`, `tests/trading-day.test.ts` |
| F-7 | 2026-09-25 | [FIX] | The live window ended at a typed 15:40 — the new F&O CLOSE, no margin for its last print; now the F&O mark minute, 15:45 | `tests/quotes-mapping.test.ts` |
| D-4 | 2026-09-25 | [DECISION] | W1 session decisions: `fo_mktlots.csv` accepted as item 7 + 4 supporting NSE circulars; one union `auction` band 15:30–15:40 (not the spec's 15:15–15:40 "closing window"); 2026-11-08 a special session, not a holiday; `nse-holidays.json` retired; OpenAlgo disclosure "3" → "4"; digest fallback 15:45 with the column default untouched | DECISIONS 2026-09-25 W1 entry |
| L-6 | 2026-09-25 | [LESSON] | PDF tables do not extract into facts a parser can trust, and a hand-typed timing with no link to its file ships a typo silently. Type the fact once, ANCHOR it to a phrase the file must contain, and refuse the build when it is missing | `scripts/build-market-calendar.mjs` |
| L-7 | 2026-09-25 | [LESSON] | A consent text that names an exact minute ("15:31 IST") is a promise about timing: when the rule moved, the accepted sentence became false and the disclosure version had to bump (every user re-accepts). The new text states the rule and the minutes together | `lib/domain/openalgo-disclosure.ts` |
| R-6 | 2026-09-25 | [RESEARCH] | VYUHA broker inventory: 8 brokers in `BROKERS`; Fyers + Nuvama had no support of any kind; OpenAlgo calls exactly `tradebook`, `funds` (probe), `multiquotes`; no OpenAlgo version ever recorded | `VYUHA/LIVE-DESK-RESEARCH/23-BROKERS-OPENALGO-2026-09-25/R5-VYUHA-BROKER-INVENTORY.md` |
| R-7 | 2026-09-25 | [RESEARCH] | OpenAlgo 2.0.2.6: sandbox fills via analyzer mode, Groww ÷100 (< 2.0.2.4), Zerodha MCX units, `NCO`/`NCDEX`, 3 security fixes, WebSocket `:8765`, MIT SDKs / AGPL platform | `…/R6-OPENALGO-UPSTREAM.md` |
| R-8 | 2026-09-25 | [RESEARCH] | Fyers (free API, reads exempt from static IP, date-range history reports, Prime/Standard plans) and Nuvama (static IP stated mandatory, untyped fields, no OpenAlgo plugin — "Nubra" is Zanskar) | `…/R7-FYERS-NUVAMA.md` |
| R-9 | 2026-09-25 | [RESEARCH] | Kotak Neo: TOTP → MPIN, static IP orders-only, today-only trade book, MIT SDK; OpenAlgo's `kotak` adapter IS Neo | `…/R8-KOTAK-NEO.md` |
| R-10 | 2026-09-25 | [RESEARCH] | Landscape: static IP binds order calls only (NSE FAQ 3 Nov 2025); Q24's citation NSE/INVG/67858 A.8; no current pull broken; no "any broker" API exists | `…/R9-BROKER-API-LANDSCAPE.md` |
| D-5 | 2026-09-25 | [DECISION] | Rulings B1–B6: only v4.6.0 + v4.7.0; W8 OpenAlgo safety (next), W9 Fyers + Nuvama list/rates/parsers, C6 native pulls Fyers/Kotak Neo/Nuvama, C7 OpenAlgo WebSocket; no-AGPL rule written into AGENTS.md | `00-SPEC-PLAN.md` §0; DECISIONS 2026-09-25 brokers entry |
| L-8 | 2026-09-25 | [LESSON] | A bridge you do not control changes under you: OpenAlgo shipped 27 releases in 2026, several changing tradebook semantics (units, prices, sandbox routing) with no API version bump. Record the tested upstream version and gate on it | W8 (spec §2) |
| L-9 | 2026-09-25 | [LESSON] | The researcher / Explore agents cannot write files here (read-only guard) — the session saved each report verbatim from `subagents/agent-<id>.jsonl`; the task output file is empty | memory `planner-agent-cannot-write-files` |
| U-2 | 2026-09-25 | [UPGRADE] | W8 OpenAlgo version gate: `GET /auth/app-info` (OpenAlgo's documented check, since 2.0.0.0); a pull below 2.0.2.6, unreadable or pre-2.0 is refused for every broker with the upgrade steps; the feed warns beside "Feed OK"; setup/upgrade guide in `docs/OPENALGO_SETUP.md`, the client guide and Help | `tests/openalgo-w8-safety.test.ts`, `tests/openalgo-gate.test.ts` |
| F-8 | 2026-09-25 | [FIX] | A tradebook OpenAlgo answered from Analyzer (sandbox) mode would import simulated fills as real — `openAlgoPost` dropped the envelope's `mode: "analyze"`; now refused whole | `tests/openalgo-w8-safety.test.ts`, `tests/openalgo-gate.test.ts` |
| F-9 | 2026-09-25 | [FIX] | A row on an exchange VYUHA cannot price (`NCO`, `NCDEX`, any unmapped stated code) imported with a null exchange hint for the classifier to guess; now refused and named in the preview warnings | `tests/openalgo-w8-safety.test.ts` |
| F-10 | 2026-09-25 | [FIX] | A zero-quantity MCX row was "repaired" from trade_value ÷ price, which on MCX is contracts × multiplier × price — an invented size; now refused, and a disagreeing trade value on a positive row is noted | `tests/openalgo-w8-safety.test.ts` |
| F-11 | 2026-09-25 | [FIX] | Installs on the stored default sent the "end of day" Telegram digest at 15:35, before the F&O close (15:40); migration 0075 moves rows still at '15:35' to '15:45' and the schema default is '15:45' (owner ruling) | `tests/migration-0075-telegram-send-time.test.ts` |
| D-6 | 2026-09-25 | [DECISION] | W1 follow-ups answered (Telegram 15:35 → 15:45 by migration; 15:30–15:40 band stands); below 2.0.2.6 every broker refuses; live 2.0.2.6 pull owner-owed; disclosure "4" amended (never shipped), not bumped; save stays offline | DECISIONS 2026-09-25 W8 entry |
| L-10 | 2026-09-25 | [LESSON] | A "stale statements corrected" commit wrote a NEW false claim ("saving also makes a /funds check") from a function's name (`verifyOpenAlgoConnection`) without reading the save branch — the function had no caller. Check a behaviour claim against the code path that runs, never a helper's name | DECISIONS 2026-09-25 W8 entry |
| F-12 | 2026-09-25 | [FIX] | `privacy-feed-disclosure`'s paragraph split (`\n[ \t]*\n`) never matched CRLF, so every Windows run judged each client doc as ONE paragraph and passed vacuously; Linux CI caught a W8 card sentence ("the Live Desk's feed makes a /funds check") that named the feed without the loopback default. Split is now `\r?\n[ \t]*\r?\n` (red on Windows with the old card, as on CI) and the card reworded | `tests/privacy-feed-disclosure.test.ts` |
| U-3 | 2026-09-25 | [UPGRADE] | W2 stock universe: `lib/data/stock-universe.json` — 5,450 of 5,482 listed equities (99.4%) carry the exchanges' own 4-level classification (NSE+BSE agree 98.4% of 2,429), validated against NSE Indices' July-2023 structure; AMFI's half-yearly rank is THE cap band (Emerge blank + reason); built by `scripts/build-stock-universe.mjs` from a polite crawl (11,647 calls, 0 refusals); the sector chain reads it first, `sector-map.json` only for the 8 ISINs it leaves blank | `tests/stock-universe.test.ts`, `tests/cap-band.test.ts` |
| U-4 | 2026-09-25 | [UPGRADE] | Row 10: `/instruments` shows as-of + canonical-JSON sha256 for the stock universe and the NSE index map (Q52's sha256 half discharged), beside the calendar; the Atlas cap tab splits into "Cap bands (AMFI)" and "Index membership (Nifty size indices)" | `tests/stock-universe.test.ts`, `tests/atlas-query.test.ts` |
| F-13 | 2026-09-25 | [FIX] | (caught in W2 review, before commit) the sector chain keyed the universe by TICKER, last writer wins: five NSE Emerge companies (MAL, GSTL, SEL, ZEAL, RAJPUTANA) took a BSE-only company's sector; an entry now claims a ticker only if the listing ranks its ISIN the owner | `tests/stock-universe.test.ts` |
| F-14 | 2026-09-25 | [FIX] | (caught in W2 review) a reissued ISIN AMFI does not list (REMAGNET INE519N01014) resolved to nothing after the refresh; `build-isin-symbols.mjs` records same-issuer `superseded` pairs against the snapshot it replaces, and every fallback join is issuer-guarded | `tests/stock-universe.test.ts` |
| L-11 | 2026-09-25 | [LESSON] | A PDF table's text cannot say where a wrapped label ends — the Basic Industry column runs into Definition, a label crosses a page break with no code row, and one word wraps mid-word. Extract by column x-position, restrict to table pages, and ASSERT each hand repair's hit count so a revised PDF fails instead of keeping a stale fix | `scripts/extract-ics-structure.mjs` |
| L-12 | 2026-09-25 | [LESSON] | Refreshing a live-only listing snapshot drops SUPERSEDED ISINs (a face-value split reissues one) while the journal still holds trades stated under them — INE887D01016 vanished and the owner's demo book would show a bare scrip code again. A refresh needs an old → new alias step in the resolver | `tests/stock-universe.test.ts`, `tests/isin-bundle-coverage.test.ts` |
| D-7 | 2026-09-25 | [DECISION] | W2: the session ran the crawl; universe first, sector-map fallback (owner answers); listing snapshot refreshed in the same run; BSE crawled whole; code-only rename to `IndexBand`; U3's DQ line = the /instruments report, not a scored issue; one allowlisted label; universe rows at the `high` tier | DECISIONS 2026-09-25 W2 entry |
| R-11 | 2026-09-25 | [RESEARCH] | Fyers + Nuvama rate cards from the brokers' own pages (Prime is a paid ₹499/month plan; Fyers Standard commodity NOT STATED; Nuvama MTF brokerage/interest NOT STATED; Nuvama's pricing page and FAQ say "Lite Plus", its comparison page "Lite" with a 0.01% cap vs 2%); no page states an effective date | DECISIONS 2026-09-25 W9 entry (URLs + quotes) |
| L-13 | 2026-09-25 | [LESSON] | Ruling B6 called the Nuvama report "per-scrip realised P&L, no trades" from its NAME; the file carries 63 per-day trade legs with billed charge heads. Open the file before ruling on what it holds — a question group at the wave's start is where the difference surfaces. | `NUVAMA_PnL_Report_23-Sep-26.xlsx` sheet `Detail Realised`; DECISIONS W9 |
| L-14 | 2026-09-25 | [LESSON] | A tradebook can carry rows that are not trades: Fyers mirrors every fill with an opposite-side `NDIR` row at 12:00:00 AM (81 of 179). Only the broker's OWN P&L file, reconciled per symbol, says which rows are fills — a parser built from the tradebook alone would have imported a book that nets to zero. | `tests/golden-books.test.ts` "81 mirror rows are skipped" |
| L-15 | 2026-09-25 | [LESSON] | A pro-rata charge breakdown keeps every column sum intact while putting the WRONG head mix on each position (Nuvama CE-23750: billed brokerage 40 / STT 43, stored 36.9 / 46.97) — a Σ-per-head test cannot see it. Pin one position's exact heads, not just the totals. | `tests/golden-books.test.ts` (Nuvama per-position heads) |
| F-15 | 2026-09-25 | [FIX] | (caught by the skeptic before commit) Fyers execution ladders put MORE fills on a position than its quantity when FIFO split a fill across positions (ALKEM 125/125 with 250 execs) — `writeLadder` would have written legs that do not sum to the parent (invariant 5); the window filter was copied from `zerodha.ts`, which had the SAME defect on split lots — both now allocate fills per position through the new `lib/import/leg-allocation.ts` (same-day first, then FIFO; a shared fill split by qty at its own price) | `tests/golden-books.test.ts` (Σ executions per side = parent qty: Fyers + both Zerodha tradebook fixtures) |
| F-16 | 2026-09-25 | [FIX] | (caught by the skeptic before commit) Nuvama per-position `reportedCharges` were the INSTRUMENT's totals spread pro rata by NetCharges; now the heads of the legs the position consumed, a split day-leg pro rata by qty | `tests/golden-books.test.ts` (CE-23750 02-Sep brokerage 40 / sttCtt 43) |
| U-5 | 2026-09-25 | [UPGRADE] | W9: `fyers` + `nuvama` in `BROKERS` (10 brokers); rate cards for Fyers Standard/Prime and Nuvama Lite Plus/Elite in `seed-data.ts` (14 broker-plans, 238 keys / 980 rows; no migration — the desktop refreshes from the template); parsers `fyers-tradebook` (98 fills → 22 closed / 1 open / 1 opening-sell, per-symbol gross = Fyers' own P&L to the paisa), `fyers-realised-pnl` (reference only), `nuvama-pnl-report` (64 legs → 32 closed / 1 open, billed charges 3,859.52 stored as stated, Summary as reference); redacted fixtures; BROKER_FORMATS sections; MTF `not-bundled` kind; OpenAlgo Fyers supported, Nuvama not | `tests/golden-books.test.ts`, `tests/import-detection-matrix.test.ts`, `tests/broker-rates.test.ts`; DECISIONS W9 |
| D-8 | 2026-09-25 | [DECISION] | W9: owner answers (Nuvama legs imported as trades; negotiated Nuvama plan → published plans only; Fyers default Standard; equity layouts UNVERIFIED); session: no SQL migration for rates (the code wins over the spec), unpublished figures by seed precedent, per-lot fees seeded per order (stated approximation), Fyers detection by filename, Nuvama identity = the file's instrument string, overnight F&O shorts stay W6's, `ALUMINI` commodity, disclosure amended not bumped | DECISIONS 2026-09-25 W9 entry |
