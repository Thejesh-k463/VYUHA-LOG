# v4.6.0 / v4.7.0 LEDGER — the tagged record (owner ruling R1, 2026-09-24)

One line per entry, APPEND-ONLY, newest last. Tags: **[LESSON]** a trap and how to avoid it · **[FIX]** a defect fixed
(MUST name the test that goes red on revert) · **[UPGRADE]** a feature shipped · **[RESEARCH]** a report and what it
settled · **[DECISION]** a ruling or a session decision (the WHY lives in `docs/DECISIONS.md`).
Format: `ID | date | tag | what | evidence (sha / test / file)`. Ids: `L-`, `F-`, `U-`, `R-`, `D-` + a number.
Before building anything, grep this file and DECISIONS for the topic — a [LESSON] here is the reason it is not re-learnt.

| ID | Date | Tag | What | Evidence |
|---|---|---|---|---|
| R-1 | 2026-09-24 | [RESEARCH] | Inventory: rows 6/8/9/10/17 in full; 71 analytics modules (lib/analytics; R0 said 74 — corrected by the prose pass), all DESCRIPTIVE by their own headers; Help = 3 TS data files (~1,600 lines, 48 entries); 5,691 ISINs resolved but only 1,379 classified | `research/R0-INVENTORY.md` |
| R-2 | 2026-09-24 | [RESEARCH] | Prescriptive analytics: 18 techniques with formulas + sample guardrails; no competitor publishes a significance method; the Clinic design | `research/R1-PRESCRIPTIVE-ANALYTICS.md` (Bailey/LdP formulas UNVERIFIED — §F) |
| R-3 | 2026-09-24 | [RESEARCH] | Help UX: search-first IA, Radix/shadcn, MiniSearch, cmdk, driver.js; **Shepherd.js is AGPL — never use** | `research/R2-HELP-DESK.md` |
| R-4 | 2026-09-24 | [RESEARCH] | Universe: NSE `getSymbolData` + BSE `ComHeadernew` give the same 4-level taxonomy (28/29 agree); AMFI 5,427 rows; NSE Emerge absent from AMFI; ~98% classifiable | `research/R3-STOCK-UNIVERSE.md` |
| R-5 | 2026-09-24 | [RESEARCH] | Market calendar: CAS effective 2026-08-03 (F&O stocks' continuous session ends 15:15, auction to 15:35, F&O to 15:40, post-close 15:50–16:00); pre-open revised 2026-09-07; 21 hard-coded call sites | `research/R4-MARKET-CALENDAR.md` (only the SEBI circular primary) |
| L-1 | 2026-09-24 | [LESSON] | A calendar fact with no expiry date is a silent defect: CAS went live 2026-08-03 and nothing in the repo knew for 52 days; the holiday list ends 2026-12-31 with no warning. Every dated rule gets `effectiveFrom` + a `coversThrough` DQ warning. | R-5; W1 scan test |
| L-2 | 2026-09-24 | [LESSON] | NSE archives time out and BSE/MCX PDFs return 403 to an agent; BSE `ComHeadernew` answers only Node `fetch` (curl 403). Primary exchange files come from the OWNER's browser download, recorded with sha256. | R-4, R-5 |
| L-3 | 2026-09-24 | [LESSON] | "1,379 stocks" was never a symbol-coverage gap (5,691 ISINs resolve) — it was a CLASSIFICATION gap. Measure what a count counts before scoping a fix. | R-1 Part D |
| D-1 | 2026-09-24 | [DECISION] | Scope + two-release split + every ruling S1–S4, U1–U4, H1–H4, T1, K1–K3, C1–C4, Q18, P1–P2, R1 | `00-SPEC-PLAN.md` §0; DECISIONS 2026-09-24 scope entry |
| D-2 | 2026-09-24 | [DECISION] | Session decisions: no cmdk (extend the existing palette); one new dep `@radix-ui/react-accordion`; weekdays past `coversThrough` = trading days FLAGGED; hubs ship in 4.6.0, Clinic tab in 4.7.0; openalgo facade stays unbuilt | `00-SPEC-PLAN.md` §0 last paragraph |
| L-4 | 2026-09-24 | [LESSON] | A research path written repo-relative (`LIVE-DESK-RESEARCH/…`) opened as `TRADE-JOURNAL/LIVE-DESK-RESEARCH/…` and 404ed for the owner. Research is OUTSIDE the repo: cite `VYUHA/LIVE-DESK-RESEARCH/…` (or `../LIVE-DESK-RESEARCH/…` in a clickable link). | STATE §0 read order item 2 |
| L-5 | 2026-09-24 | [LESSON] | A folder move leaves stale paths in prose: AGENTS.md still cited `TRADE-SENTINAL/sentinel/NIFTY INDICES` 14 days after the move to `SENTINEL/bot`. `ls` every cited external path before a wave depends on it (W2 rebuilds the index map from it). | AGENTS.md "Bundled NSE index map" |
| D-3 | 2026-09-24 | [DECISION] | Alignment audit: ONE paste block (`22-V460-BUILD/SESSION-LOG.md`); ONE ledger from v4.6.0 (this file; `09-BUILD-LEDGER.md` is history); spec §0 > research > older packs; the Clinic is a hub tab (`/reports/edge-clinic?tab=clinic`); v4.6.0/v4.7.0 rulings live in the spec §0 table, and 06-ANSWERS points to it | DECISIONS 2026-09-24 alignment entry |
| F-1 | 2026-09-24 | [FIX] | `scripts/state-drift-check.mjs` read a hard-coded `21-V450-BUILD/SESSION-LOG.md`, so its cited-sha check would never see the v4.6.0 log; it now reads the highest-numbered `NN-VNNN-BUILD/SESSION-LOG.md` STATE §0 names. STATE §0 wording restored so `state-head-ancestor` and `migration-vs-state` run again (close-out 19 PASS / 4 SKIP → 21 PASS / 2 SKIP) | `tests/state-drift.test.ts` (3 passed); `npm run drift:close-out` |
| U-1 | 2026-09-25 | [UPGRADE] | W1 market calendar: `lib/domain/market-calendar.ts` (pure) + `lib/data/market-calendar.json` (18 effective-dated session rows, 19 NSE holidays, Budget Sunday + Muhurat, 210 CAS stocks) built by `scripts/build-market-calendar.mjs` from the owner's downloads with 11 anchor checks; `/instruments` shows as-of + sources sha256; DQ warns 60 days before `coversThrough` | `tests/market-calendar.test.ts` |
| F-2 | 2026-09-25 | [FIX] | The day's mark was saved from 15:30 — an F&O stock's PRE-AUCTION price since CAS (2026-08-03). Now per key: CAS stock 15:36, other stock 15:31 (K3); the desk's reconnect 15:31 → 15:36 | `tests/market-calendar.test.ts`, `tests/live-mark-persist.test.ts`, `tests/seams-v41-fix2.test.ts` |
| F-3 | 2026-09-25 | [FIX] | Fills 15:30–15:40 (CAS prints, F&O extension) and 15:50–16:00 (post-close) were filed "outside the session … worth checking the import"; bands now come from the calendar by the fill's own date | `tests/market-calendar.test.ts` |
| F-4 | 2026-09-25 | [FIX] | The sidebar dot kept its own Mon–Fri 09:15–15:30 clock (green on Republic Day, shut during the F&O 15:30–15:40 extension); desk and sidebar now share `isMarketOpen()` | `tests/market-calendar.test.ts`, `tests/live-market-hours.test.ts` |
| F-5 | 2026-09-25 | [FIX] | Three extra IST offsets (data-quality, Angel One session expiry, desk date format) and a typed `T15:30:00+05:30`; a scan now fails on any timing/offset literal outside the calendar and `trading-day.ts` | `tests/market-calendar.test.ts` |
| F-6 | 2026-09-25 | [FIX] | `previousTradingDay` / `latestBhavcopyDate` walked INTO holidays and skipped the Budget Sunday; the calendar walk skips holidays and visits special sessions | `tests/market-calendar.test.ts`, `tests/trading-day.test.ts` |
| F-7 | 2026-09-25 | [FIX] | The live window ended at a typed 15:40 — the new F&O CLOSE, no margin for its last print; now the F&O mark minute, 15:45 | `tests/quotes-mapping.test.ts` |
| D-4 | 2026-09-25 | [DECISION] | W1 session decisions: `fo_mktlots.csv` accepted as item 7 + 4 supporting NSE circulars; one union `auction` band 15:30–15:40 (not the spec's 15:15–15:40 "closing window"); 2026-11-08 a special session, not a holiday; `nse-holidays.json` retired; OpenAlgo disclosure "3" → "4"; digest fallback 15:45 with the column default untouched | DECISIONS 2026-09-25 W1 entry |
| L-6 | 2026-09-25 | [LESSON] | PDF tables do not extract into facts a parser can trust, and a hand-typed timing with no link to its file ships a typo silently. Type the fact once, ANCHOR it to a phrase the file must contain, and refuse the build when it is missing | `scripts/build-market-calendar.mjs` |
| L-7 | 2026-09-25 | [LESSON] | A consent text that names an exact minute ("15:31 IST") is a promise about timing: when the rule moved, the accepted sentence became false and the disclosure version had to bump (every user re-accepts). The new text states the rule and the minutes together | `lib/domain/openalgo-disclosure.ts` |
| R-6 | 2026-09-25 | [RESEARCH] | VYUHA broker inventory: 8 brokers in `BROKERS`; Fyers + Nuvama had no support of any kind; OpenAlgo calls exactly `tradebook`, `funds` (probe), `multiquotes`; no OpenAlgo version ever recorded | `VYUHA/LIVE-DESK-RESEARCH/23-BROKERS-OPENALGO-2026-09-25/R5-VYUHA-BROKER-INVENTORY.md` |
| R-7 | 2026-09-25 | [RESEARCH] | OpenAlgo 2.0.2.6: sandbox fills via analyzer mode, Groww ÷100 (< 2.0.2.4), Zerodha MCX units, `NCO`/`NCDEX`, 3 security fixes, WebSocket `:8765`, MIT SDKs / AGPL platform | `…/R6-OPENALGO-UPSTREAM.md` |
| R-8 | 2026-09-25 | [RESEARCH] | Fyers (free API, reads exempt from static IP, date-range history reports, Prime/Standard plans) and Nuvama (static IP stated mandatory, untyped fields, no OpenAlgo plugin — "Nubra" is Zanskar) | `…/R7-FYERS-NUVAMA.md` |
| R-9 | 2026-09-25 | [RESEARCH] | Kotak Neo: TOTP → MPIN, static IP orders-only, today-only trade book, MIT SDK; OpenAlgo's `kotak` adapter IS Neo | `…/R8-KOTAK-NEO.md` |
| R-10 | 2026-09-25 | [RESEARCH] | Landscape: static IP binds order calls only (NSE FAQ 3 Nov 2025); Q24's citation NSE/INVG/67858 A.8; no current pull broken; no "any broker" API exists | `…/R9-BROKER-API-LANDSCAPE.md` |
| D-5 | 2026-09-25 | [DECISION] | Rulings B1–B6: only v4.6.0 + v4.7.0; W8 OpenAlgo safety (next), W9 Fyers + Nuvama list/rates/parsers, C6 native pulls Fyers/Kotak Neo/Nuvama, C7 OpenAlgo WebSocket; no-AGPL rule written into AGENTS.md | `00-SPEC-PLAN.md` §0; DECISIONS 2026-09-25 brokers entry |
| L-8 | 2026-09-25 | [LESSON] | A bridge you do not control changes under you: OpenAlgo shipped 27 releases in 2026, several changing tradebook semantics (units, prices, sandbox routing) with no API version bump. Record the tested upstream version and gate on it | W8 (spec §2) |
| L-9 | 2026-09-25 | [LESSON] | The researcher / Explore agents cannot write files here (read-only guard) — the session saved each report verbatim from `subagents/agent-<id>.jsonl`; the task output file is empty | memory `planner-agent-cannot-write-files` |
| U-2 | 2026-09-25 | [UPGRADE] | W8 OpenAlgo version gate: `GET /auth/app-info` (OpenAlgo's documented check, since 2.0.0.0); a pull below 2.0.2.6, unreadable or pre-2.0 is refused for every broker with the upgrade steps; the feed warns beside "Feed OK"; setup/upgrade guide in `docs/OPENALGO_SETUP.md`, the client guide and Help | `tests/openalgo-w8-safety.test.ts`, `tests/openalgo-gate.test.ts` |
| F-8 | 2026-09-25 | [FIX] | A tradebook OpenAlgo answered from Analyzer (sandbox) mode would import simulated fills as real — `openAlgoPost` dropped the envelope's `mode: "analyze"`; now refused whole | `tests/openalgo-w8-safety.test.ts`, `tests/openalgo-gate.test.ts` |
| F-9 | 2026-09-25 | [FIX] | A row on an exchange VYUHA cannot price (`NCO`, `NCDEX`, any unmapped stated code) imported with a null exchange hint for the classifier to guess; now refused and named in the preview warnings | `tests/openalgo-w8-safety.test.ts` |
| F-10 | 2026-09-25 | [FIX] | A zero-quantity MCX row was "repaired" from trade_value ÷ price, which on MCX is contracts × multiplier × price — an invented size; now refused, and a disagreeing trade value on a positive row is noted | `tests/openalgo-w8-safety.test.ts` |
| F-11 | 2026-09-25 | [FIX] | Installs on the stored default sent the "end of day" Telegram digest at 15:35, before the F&O close (15:40); migration 0075 moves rows still at '15:35' to '15:45' and the schema default is '15:45' (owner ruling) | `tests/migration-0075-telegram-send-time.test.ts` |
| D-6 | 2026-09-25 | [DECISION] | W1 follow-ups answered (Telegram 15:35 → 15:45 by migration; 15:30–15:40 band stands); below 2.0.2.6 every broker refuses; live 2.0.2.6 pull owner-owed; disclosure "4" amended (never shipped), not bumped; save stays offline | DECISIONS 2026-09-25 W8 entry |
| L-10 | 2026-09-25 | [LESSON] | A "stale statements corrected" commit wrote a NEW false claim ("saving also makes a /funds check") from a function's name (`verifyOpenAlgoConnection`) without reading the save branch — the function had no caller. Check a behaviour claim against the code path that runs, never a helper's name | DECISIONS 2026-09-25 W8 entry |
| F-12 | 2026-09-25 | [FIX] | `privacy-feed-disclosure`'s paragraph split (`\n[ \t]*\n`) never matched CRLF, so every Windows run judged each client doc as ONE paragraph and passed vacuously; Linux CI caught a W8 card sentence ("the Live Desk's feed makes a /funds check") that named the feed without the loopback default. Split is now `\r?\n[ \t]*\r?\n` (red on Windows with the old card, as on CI) and the card reworded | `tests/privacy-feed-disclosure.test.ts` |
| U-3 | 2026-09-25 | [UPGRADE] | W2 stock universe: `lib/data/stock-universe.json` — 5,450 of 5,482 listed equities (99.4%) carry the exchanges' own 4-level classification (NSE+BSE agree 98.4% of 2,429), validated against NSE Indices' July-2023 structure; AMFI's half-yearly rank is THE cap band (Emerge blank + reason); built by `scripts/build-stock-universe.mjs` from a polite crawl (11,647 calls, 0 refusals); the sector chain reads it first, `sector-map.json` only for the 8 ISINs it leaves blank | `tests/stock-universe.test.ts`, `tests/cap-band.test.ts` |
| U-4 | 2026-09-25 | [UPGRADE] | Row 10: `/instruments` shows as-of + canonical-JSON sha256 for the stock universe and the NSE index map (Q52's sha256 half discharged), beside the calendar; the Atlas cap tab splits into "Cap bands (AMFI)" and "Index membership (Nifty size indices)" | `tests/stock-universe.test.ts`, `tests/atlas-query.test.ts` |
| F-13 | 2026-09-25 | [FIX] | (caught in W2 review, before commit) the sector chain keyed the universe by TICKER, last writer wins: five NSE Emerge companies (MAL, GSTL, SEL, ZEAL, RAJPUTANA) took a BSE-only company's sector; an entry now claims a ticker only if the listing ranks its ISIN the owner | `tests/stock-universe.test.ts` |
| F-14 | 2026-09-25 | [FIX] | (caught in W2 review) a reissued ISIN AMFI does not list (REMAGNET INE519N01014) resolved to nothing after the refresh; `build-isin-symbols.mjs` records same-issuer `superseded` pairs against the snapshot it replaces, and every fallback join is issuer-guarded | `tests/stock-universe.test.ts` |
| L-11 | 2026-09-25 | [LESSON] | A PDF table's text cannot say where a wrapped label ends — the Basic Industry column runs into Definition, a label crosses a page break with no code row, and one word wraps mid-word. Extract by column x-position, restrict to table pages, and ASSERT each hand repair's hit count so a revised PDF fails instead of keeping a stale fix | `scripts/extract-ics-structure.mjs` |
| L-12 | 2026-09-25 | [LESSON] | Refreshing a live-only listing snapshot drops SUPERSEDED ISINs (a face-value split reissues one) while the journal still holds trades stated under them — INE887D01016 vanished and the owner's demo book would show a bare scrip code again. A refresh needs an old → new alias step in the resolver | `tests/stock-universe.test.ts`, `tests/isin-bundle-coverage.test.ts` |
| D-7 | 2026-09-25 | [DECISION] | W2: the session ran the crawl; universe first, sector-map fallback (owner answers); listing snapshot refreshed in the same run; BSE crawled whole; code-only rename to `IndexBand`; U3's DQ line = the /instruments report, not a scored issue; one allowlisted label; universe rows at the `high` tier | DECISIONS 2026-09-25 W2 entry |
| R-11 | 2026-09-25 | [RESEARCH] | Fyers + Nuvama rate cards from the brokers' own pages (Prime is a paid ₹499/month plan; Fyers Standard commodity NOT STATED; Nuvama MTF brokerage/interest NOT STATED; Nuvama's pricing page and FAQ say "Lite Plus", its comparison page "Lite" with a 0.01% cap vs 2%); no page states an effective date | DECISIONS 2026-09-25 W9 entry (URLs + quotes) |
| L-13 | 2026-09-25 | [LESSON] | Ruling B6 called the Nuvama report "per-scrip realised P&L, no trades" from its NAME; the file carries 63 per-day trade legs with billed charge heads. Open the file before ruling on what it holds — a question group at the wave's start is where the difference surfaces. | `NUVAMA_PnL_Report_23-Sep-26.xlsx` sheet `Detail Realised`; DECISIONS W9 |
| L-14 | 2026-09-25 | [LESSON] | A tradebook can carry rows that are not trades: Fyers mirrors every fill with an opposite-side `NDIR` row at 12:00:00 AM (81 of 179). Only the broker's OWN P&L file, reconciled per symbol, says which rows are fills — a parser built from the tradebook alone would have imported a book that nets to zero. | `tests/golden-books.test.ts` "81 mirror rows are skipped" |
| L-15 | 2026-09-25 | [LESSON] | A pro-rata charge breakdown keeps every column sum intact while putting the WRONG head mix on each position (Nuvama CE-23750: billed brokerage 40 / STT 43, stored 36.9 / 46.97) — a Σ-per-head test cannot see it. Pin one position's exact heads, not just the totals. | `tests/golden-books.test.ts` (Nuvama per-position heads) |
| F-15 | 2026-09-25 | [FIX] | (caught by the skeptic before commit) Fyers execution ladders put MORE fills on a position than its quantity when FIFO split a fill across positions (ALKEM 125/125 with 250 execs) — `writeLadder` would have written legs that do not sum to the parent (invariant 5); the window filter was copied from `zerodha.ts`, which had the SAME defect on split lots — both now allocate fills per position through the new `lib/import/leg-allocation.ts` (same-day first, then FIFO; a shared fill split by qty at its own price) | `tests/golden-books.test.ts` (Σ executions per side = parent qty: Fyers + both Zerodha tradebook fixtures) |
| F-16 | 2026-09-25 | [FIX] | (caught by the skeptic before commit) Nuvama per-position `reportedCharges` were the INSTRUMENT's totals spread pro rata by NetCharges; now the heads of the legs the position consumed, a split day-leg pro rata by qty | `tests/golden-books.test.ts` (CE-23750 02-Sep brokerage 40 / sttCtt 43) |
| U-5 | 2026-09-25 | [UPGRADE] | W9: `fyers` + `nuvama` in `BROKERS` (10 brokers); rate cards for Fyers Standard/Prime and Nuvama Lite Plus/Elite in `seed-data.ts` (14 broker-plans, 238 keys / 980 rows; no migration — the desktop refreshes from the template); parsers `fyers-tradebook` (98 fills → 22 closed / 1 open / 1 opening-sell, per-symbol gross = Fyers' own P&L to the paisa), `fyers-realised-pnl` (reference only), `nuvama-pnl-report` (64 legs → 32 closed / 1 open, billed charges 3,859.52 stored as stated, Summary as reference); redacted fixtures; BROKER_FORMATS sections; MTF `not-bundled` kind; OpenAlgo Fyers supported, Nuvama not | `tests/golden-books.test.ts`, `tests/import-detection-matrix.test.ts`, `tests/broker-rates.test.ts`; DECISIONS W9 |
| D-8 | 2026-09-25 | [DECISION] | W9: owner answers (Nuvama legs imported as trades; negotiated Nuvama plan → published plans only; Fyers default Standard; equity layouts UNVERIFIED); session: no SQL migration for rates (the code wins over the spec), unpublished figures by seed precedent, per-lot fees seeded per order (stated approximation), Fyers detection by filename, Nuvama identity = the file's instrument string, overnight F&O shorts stay W6's, `ALUMINI` commodity, disclosure amended not bumped | DECISIONS 2026-09-25 W9 entry |
| U-6 | 2026-09-25 | [UPGRADE] | W3 analytics hubs: `lib/domain/hubs.ts` is the ONE registry (3 hubs, 7 tabs, each tab's legacy route); `/reports/edge-clinic`, `/reports/capital`, `/reports/costs` render the old page bodies (`_tabs/*.tsx`, byte-identical bar header/gate) by `?tab=`; the seven old URLs answer a config-level 307 AND a page stub; Analytics nav 10 → 6; PRO_FEATURES, help, palette (tab rows), the saved nav order (`migrateNavHrefs`) and the workspace map (`tabVisible`: the Expiry TAB, not the hub) all derive from it | `tests/hubs.test.ts` (44), `e2e/z-hubs.spec.ts` (8) |
| F-17 | 2026-09-25 | [FIX] | `tests/pro-gating.test.ts` returned null for any PRO_FEATURES href with a `?`, so a tab-level Pro entry was never checked for `<ProGate>` — a hub with its gate removed stayed green (proved P-B: the HEAD test passed on the ungated hub). `pageFileFor` now maps a `?tab=` href to its path's page.tsx and a non-partial entry pointing nowhere fails | `tests/pro-gating.test.ts`, `tests/hubs.test.ts` (one gate wrapping `BODIES[tab.id]()`) |
| L-16 | 2026-09-25 | [LESSON] | `redirect()` in a page under a root `loading.tsx` boundary is NOT a 307 on the wire: the shell streams first and the redirect arrives as an RSC `NEXT_REDIRECT` payload over a 200 that only client JS follows (measured with curl on the dev server; no meta refresh). A status code is proven with `curl -o /dev/null -w %{http_code}`, never by reading the call; the real 307 is `next.config.ts redirects()` | `tests/hubs.test.ts` (config pin), `e2e/z-hubs.spec.ts` (`page.request.get` with `maxRedirects: 0`) |
| L-17 | 2026-09-25 | [LESSON] | Re-keying a registry by href (help entries → `?tab=` URLs) silently emptied a CONSUMER that filtered by the old key: `/help` grouped entries by bare nav hrefs and would have shown seven fewer cards with every registry test green. Grep every `.includes(e.href)` / `=== href` reader before re-keying, and give the consumer its own pin | `tests/hubs.test.ts` ("Help Desk still shows every entry"), `navGroupHrefs` |
| L-18 | 2026-09-25 | [LESSON] | A stale Turbopack dev cache (`.next/dev`) answered 404 for EXISTING routes (`/reports/performance`, `/api/license`) with nothing in the log while `/`, `/trades` were 200 — an hour of wave-blaming avoided only by probing an UNCHANGED route first. Before diagnosing a route, probe a sibling that did not change; if it 404s too, stop the server and delete `.next/dev` | this session's log (twenty-fifth) |
| D-9 | 2026-09-25 | [DECISION] | W3 session decisions: the seven routes live under `/reports/*` (the spec's `/scaling` shorthand; the code wins); a 307 via `redirects()` + the page stub, never a 308 (4.7.0 re-maps the Clinic default); only the active tab's body renders (its queries run per tab); a hidden Expiry tab still opens by deep link (the sidebar's `isCurrent` rule); help = 7 tab entries + 3 hub entries; `readme-claims` counts a page.tsx that only redirects as NO screen (45, not 52); the two data badges moved into their tab bodies; `aria-controls` dropped from link-triggers | DECISIONS 2026-09-25 W3 entry |
| U-7 | 2026-09-25 | [UPGRADE] | Help revamp (H1–H4): every entry gains a task-first layer (`lib/domain/help-topics.ts`: 50 records — 3–5 steps, a watch-out, related topics, 14 screenshots; card ≤ 120 words), `/help` is a search hero + accordion (`@radix-ui/react-accordion` 1.2.15 spliced by text) + an 18px topic dialog opened by `#topic-…`; a "?" in every page header; a glossary (35 terms) on the existing Tooltip; a shortcuts sheet on `?`; the palette gains help rows under a typed query; 14 dark demo-data webp bundled (`npm run help:shots`, 941 KB) | `tests/help-content.test.ts`, `tests/glossary.test.ts`, `tests/shortcuts.test.ts`, `e2e/z-help.spec.ts`; DECISIONS 2026-09-25 W4 entry |
| U-8 | 2026-09-25 | [UPGRADE] | Getting-started strip (row 9.5) on the dashboard: account made · trades imported · charges plan set · a stop recorded · backup taken (a per-machine localStorage marker — the DB records no backup), from existing data through `getSelectedAccountId()`; gone when all five are done or dismissed | `tests/getting-started.test.ts`, `tests/getting-started-query.test.ts`, `tests/account-isolation.test.ts` OWNERS |
| F-18 | 2026-09-25 | [FIX] | Global search's "screens" source listed `NAV_ITEMS` only, so a hub TAB was not found as a screen (D-9's recorded gap): one row per tab derived from `HUBS`, labelled "Hub › Tab" | `tests/hubs.test.ts` ("one screens row per tab, '<Hub> › <Tab>', opening hubTabHref…") |
| L-19 | 2026-09-25 | [LESSON] | A bundled screenshot is a snapshot of the VERSION that shot it: the 14 help webp carry the sidebar footer (`v4.5` today) and the dashboard one carries the strip's "2 of 5". Re-run `npm run help:shots` AFTER `bump-version` and before the release build, every release — or the v4.6.0 installer ships v4.5 pictures | `public/help/README.md`; STATE §0.3 V8 |
| L-20 | 2026-09-25 | [LESSON] | A single-character shortcut (`?`) is eaten by whatever editable element has focus — on `/help` that is the auto-focused search box, so the sheet's own screen cannot open it by key. Either the key handler refuses editable targets (it does) and a button exists (it does), or the key is not a shortcut; a test that presses the key on a screen with an auto-focused input reads as "the handler is broken" | `e2e/z-help.spec.ts` presses `?` on `/trades`; DECISIONS W4 entry |
| D-10 | 2026-09-25 | [DECISION] | W4 session decisions: the task-first layer is ADDED over each entry (body/refusals unchanged under "In detail" — the ~60 copy pins hold; the 120-word budget is the card); ids `topic-<slug>`; content in its own file joined at load (throws on a miss or an orphan); the hovercard is the existing Tooltip (one new dep only); `help-desk.tsx` stays the `/help` root (four tests read it); help rows in the palette only under a query; "backup taken" is a localStorage marker; shots re-captured after the bump; `search-route`'s 5 s timeout is FAIL-J (2.3 s on HEAD, 2.0 s on W4, alone; 5.2 s under the suite in 2 of 3 runs → that one `it` gets 15 s with the measurement in its comment) | DECISIONS 2026-09-25 W4 entry |
| D-11 | 2026-09-25 | [DECISION] | W5 opened: the twenty 06a AQ answers (Option B scope, five tabs, regime → my names → sector RS → cap bands → breadth, all nine breadth + five cap-band figures, ONE rotation table with a level toggle, 1w/1m/3m/YTD, RS vs the market MEDIAN only, rank Δ printing its shortfall, "turnover share", median shipped + mean toggle with `atlas-core/2.0.0`, regime thresholds editable, two-input rule kept, index memberships as a FILTER, floors 3/8 and cohort ≥ 5 priced + 60%, coverage-instead-of-number below 30%, greyed tiles naming the gate, everything in W5) + Q51's nine decided by the session | DECISIONS 2026-09-25 "W5 opened" entry; spec §4 |
| L-21 | 2026-09-25 | [LESSON] | A cache versioned on its HEADER row is not versioned on its DETAIL rows: `atlas_metric` carries no `spec_version`, and the recompute deletes only rows AFTER the anchor, so every pre-bump row for an earlier session survives a formula change. The first cross-session reader (rank Δ) would have mixed two definitions silently. A detail row is read only through a JOIN to the header that names its version | `tests/atlas-query.test.ts` "1.0.0 rows for an earlier as_of never enter a 2.0.0 rank Δ" |
| L-22 | 2026-09-25 | [LESSON] | Two call sites that "use the same guard" diverge when they build the guard's INPUT differently: the rotation tab built its corporate-action gap map over the ALIGNED universe with the aligned market baseline, the cohort would have built one over the raw series — the same threshold, a different exclusion set. Extract the builder and pin set equality across the two consumers | `tests/atlas-cohort.test.ts` "rotation exclusion set == cohort exclusion set" |
| L-23 | 2026-09-25 | [LESSON] | A progress envelope with no reset path is fine behind a button (a stuck `running` needs a mid-kill AND a person to notice) and fatal behind a job that runs on every app open (one mid-run close blocks every later open forever). Any state machine an automatic job shares needs a dead-run rule stamped from the clock that WRITES the state | `tests/bhavcopy-backfill.test.ts` "a 'running' envelope nobody has touched for ten intervals is DEAD"; `tests/atlas-catchup.test.ts` |
| L-24 | 2026-09-25 | [LESSON] | A user-selectable FILTER over a cached compute must never persist through the cache's key: `atlas_daily` is unique on `as_of` with a checksum over ALL bars, so a Nifty-500 recompute stored there would have been served as "the market" on the next unfiltered open with `checksum_unchanged`. Filters compute in memory or carry their own key | `tests/atlas-route.test.ts` "writes NO cache row" |
| F-19 | 2026-09-25 | [FIX] | The cohort tab compared a name against its sector with NO corporate-action guard (a 1:2 split inside the window read as −50% "your pick underperformed") and stated `cohortSize` as MEMBERS, not priced constituents; now `gapsBySymbol` excludes the row with the date, and "14 of 17 priced" is the denominator | `tests/atlas-cohort.test.ts` |
| F-20 | 2026-09-25 | [FIX] | The regime label voted on `%>SMA50` regardless of coverage — 40 of 1,900 priced could set "expansion" while the tile beneath it should blank; `classifyRegime` now refuses any input below `COVERAGE_FLOOR_PPM` (`unknown` / `coverage_below_floor`) | `tests/atlas-regime.test.ts` "an input under the coverage floor cannot vote" |
| F-21 | 2026-09-25 | [FIX] | `runBhavcopyBackfill` refused forever on a `running` envelope nothing reset; a stale envelope (untouched 10 × the rate limit) is now reset at the refusal | `tests/bhavcopy-backfill.test.ts` "…is DEAD" |
| U-9 | 2026-09-25 | [UPGRADE] | W5 Atlas panel v3 (`atlas-core/2.0.0`): median group returns (mean behind a labelled toggle, both persisted), ONE rotation table at sector/industry with 1w/1m/3m/YTD, RS vs the cross-sectional median (5/21/63 × 0.5/0.3/0.2, floors ₹1 cr turnover / ₹20), rank Δ printing its snapshot shortfall, turnover share, RSI14 extremes, volume split, per-group and per-AMFI-band breadth family, industry cohorts falling UP to sector with "n of m priced" + rank + percentile + descriptive verdict, "breadth on the days you opened these positions" through `getTrackerTrades()`, index-membership FILTER computed in memory (`GET /api/atlas/view?index=`), the honesty list (`lib/atlas/not-computed.ts`), coverage-instead-of-number below 30%, regime thresholds editable in Settings (migration 0076, travels with the backup, Reset → defaults) | `tests/atlas-{rs,levels,cohort,not-computed,regime-settings}.test.ts`, `tests/migration-0076-atlas-regime.test.ts`, `e2e/z-atlas.spec.ts` |
| U-10 | 2026-09-25 | [UPGRADE] | Q51 freshness: `lib/jobs/bhavcopy-catchup.ts` tops up missing sessions after the auto-MTM run under the SAME consent — ≤ 10 files per app open, 1.5 s apart, consent + abort re-read per file, `globalThis` lock, dead-run reset, the calendar's date walk, no URL of its own; the panel names the remaining hole and points at the backfill; PRIVACY item 2 states the cadence | `tests/atlas-catchup.test.ts`; `docs/client/PRIVACY.md` |
| D-12 | 2026-09-25 | [DECISION] | W5 session decisions: both statistics persisted; rank Δ denominator = daily snapshots via the JOIN; one gap map over the aligned universe; cohort level once per symbol on the 1m count, `groupByLevel` never falls up, user tag replaces the taxonomy row, `tier` deleted; regime re-derived at read time in `getVerifiedSnapshot()`, A9 literal; catch-up gated on `autoMtmEnabled` only and excluding the top-up's own date; filter never cached; `2m` removed; floors and the 30% are PROPOSALS; W6 → migration 0077 | DECISIONS 2026-09-25 W5 built entry |
| L-25 | 2026-09-25 | [LESSON] | `getAtlasView()` re-reads EVERY stored bar on every render to checksum the cache (1,900 symbols x 300 sessions = 570,000 rows: ~10 s first compute, ~3 s steady-state on a checksum HIT, measured on a temp DB) - the cache saves the compute, not the read. v3 adds the aligned gap map, cohorts and spans on that same path. Measure `/atlas` on the owner's real DB before v4.6.0 ships; a cheaper freshness key (max date + row count) is the obvious lever and is NOT built | phase-2 builder probe, DECISIONS W5 built entry |
| F-22 | 2026-09-25 | [FIX] | (caught by the skeptic before commit) the catch-up's dead-run threshold (10 x the 1.5 s rate limit = 15 s) was no longer than ONE archive fetch timeout (15 s, two attempts per date) and the envelope was stamped only after each apply, so a slow NSE answer made a LIVE run read as dead - the backfill button or the next app open would have reset it and started a second walker on the same host; and `runBhavcopyBackfill` never took the lock the catch-up takes. Now a heartbeat stamps the envelope before every fetch, the threshold is derived from the fetch timeout (2 x timeout + 4 x rate), and both jobs share one process lock | `tests/atlas-catchup.test.ts`, `tests/bhavcopy-backfill.test.ts` |
| F-23 | 2026-09-25 | [FIX] | (caught by the skeptic before commit) the regime-threshold editor posted a CLEARED field as 0 (`Number("") === 0`), so clearing the contraction box saved a 0% ceiling silently; a blank is now NaN and the route's 400 fires | `tests/atlas-page.test.ts` "a cleared field is not 0" |
| L-26 | 2026-09-25 | [LESSON] | A "dead job" rule is only as good as the longest LEGITIMATE silence it sits above: derive the threshold from the fetch timeout x attempts + spacing and stamp a heartbeat BEFORE the slow step, never after it. A threshold chosen as "N intervals" that ignores the network timeout turns every slow response into a duplicate worker | F-22 |
| F-24 | 2026-09-25 | [FIX] | (found by extending the console audit to /settings) `app/layout.tsx` mounted `<CommandPalette key={selectedAccountId}>` and `<SearchPanel key={selectedAccountId}>` as siblings under one provider with the SAME key - React's duplicate-key warning on every /settings render since v3.9 W3 (`c04e1ad`), invisible because `zz-console-audit` never visited /settings or /atlas. Keys are now prefixed per component; the audit visits both routes (7 pages, 0 messages) | `e2e/zz-console-audit.spec.ts` |
| L-27 | 2026-09-25 | [LESSON] | A console audit proves only the pages it visits: a React key warning sat on /settings for 16 releases while "0 console messages" stayed green on five OTHER routes. When a wave adds or reworks a screen, add it to `zz-console-audit`'s PAGES in the same commit; a warning with no page attribution needs a CDP owner-stack capture, because React 19 does not append the component stack to the console text | F-24; `e2e/zz-console-audit.spec.ts` PAGES |
| U-11 | 2026-09-25 | [UPGRADE] | W6 `trades.side` (migration 0077, nullable, CHECK long/short): the side that OPENED a flat row is stored, written by every leg writer (an AST rule fails an insert/update that sets a quantity without `side`), read through `sideOf()` (quantities first; the column only on a flat row) and `statedSideOf()` (null when nothing stated); backfill by the data fix `trades-side-v1` through `normalizeDate` — a flat, same-day or undated row with no short note stays NULL. Overnight F&O shorts: `pairLegs(legs, {shortable})` opens a pending short lot on an uncovered derivative sale and turns it into ONE closed short only when a LATER buy in the same file covers it (`OVERNIGHT_SHORT_NOTE`, a Data Quality review line naming the other reading); an uncovered sale stays `opening-sell` byte-for-byte; `leg-allocation` and the Dhan `allocateFills` replay the same lots; the eight callers decide `shortable` from `classify()`. A pre-4.6 legacy pair (opening-sell + open) is listed in Data Quality and joined through the confirm dialog; a re-import after W6 refuses the whole symbol group when any row of the pre-W6 pairing is already stored | `tests/side-column.test.ts`, `tests/pair-legs-shortable.test.ts`, `tests/leg-allocation.test.ts`, `tests/parser-overnight-short-seam.test.ts`, `tests/side-sequences.test.ts`, `tests/harness-book-sequences.test.ts` |
| U-12 | 2026-09-25 | [UPGRADE] | W6 `/trades` speed lever, measured first: at 25,001 rows the page's whole-book read (`getJournalTrades` + `tradeStatsOf`) cost 359-412 ms of a ~1,000 ms render; now the KPI strip is `getTradeStatsSql()` (ONE aggregate over integer paise, bit-identical to the reduce, 5-10 ms) and the three side panels read `getJournalPanelRows()` (acquisition-stated rows + unmarked open equity, same total ORDER BY, 12 ms); the page no longer reads the whole book | `tests/trades-kpi-sql.test.ts`; DECISIONS 2026-09-25 W6 entry |
| U-13 | 2026-09-25 | [UPGRADE] | W6 referral: `license-issue.mjs --ref <CODE>` and `sell.mjs --ref` write a `ref` field on the ledger line (normalised; blank/NONE = none; never in the signed key), `license-upgrade.mjs` carries it forward, `license-list.mjs --by-ref [CODE]` sums keys per creator (revoked never "active"), and the share card's funnel line points at the BUYER page `thejesh-k463.github.io/VYUHA-LOG` instead of the source repo | `tests/license-upgrade.test.ts`, `tests/sell-flow.test.ts`, `tests/share-card.test.ts` |
| L-28 | 2026-09-25 | [LESSON] | An unmatched sale has TWO readings — a short opened, or a holding bought before the file's window — and only the file's own LATER buy of the same contract can choose one: the first `shortable` design turned every uncovered derivative sale into a short, and the owner's real Fyers file (COFORGE26AUG1500CE sold 950 on 2026-07-28, bought before the window, pinned against Fyers' own P&L) would have become a phantom open short that auto-MTM marks. Caught by `vyuha-design-reviewer` probing the golden files before the builder | `tests/golden-books.test.ts` (COFORGE pin unchanged), `tests/pair-legs-shortable.test.ts` "uncovered stays opening-sell" |
| L-29 | 2026-09-25 | [LESSON] | A pairing rule lives in THREE replays (`pairLegs`, `leg-allocation.ts`'s `allocateSymbolLegs`, the Dhan `allocateFills`); a lot shape added to one and not the others loses the ladder (invariant 5), nulls the fill times and drops Nuvama's stated bill heads — silently, because `matchAllocations` returns null for a shape it does not know | `tests/leg-allocation.test.ts`, `tests/parser-overnight-short-seam.test.ts` |
| L-30 | 2026-09-25 | [LESSON] | A derived column must never outrank the facts it was derived from while those facts still speak: a stored `side='short'` on an equity opening-sell would have made an IPO-linked or basis-written sale CLOSE BY BUYING and flipped auto-MTM's sign; `sideOf` reads quantities first and the column only on a FLAT row, and every writer that touches a leg writes the column in the same statement (AST rule `trade-side-writer`) | `tests/side-column.test.ts` "a lopsided row reads by quantity", `tests/readers-follow-writers.test.ts`, `tests/side-sequences.test.ts` |
| L-31 | 2026-09-25 | [LESSON] | Never compare dates in SQL over a column the tree admits holds two formats: 4.2.x rows carry `DD-MM-YYYY` beside ISO, so a byte comparison `sell_date < buy_date` labels a long as short; the migration adds the column only and the backfill runs in JS through `normalizeDate` — one implementation, never SQL plus a JS copy | `tests/side-column.test.ts` "DD-MM-YYYY" |
| L-32 | 2026-09-25 | [LESSON] | A dedup that compares LEGS is defeated by any pairing that splits or merges legs: the per-leg legacy check caught a one-sale-one-buy pre-W6 short and missed a partial cover (the leftover opening-sell 40 inserted, sold qty 140) and two sells covered by one buy (both new rows inserted). The check now re-derives the OTHER pairing's rows and refuses the whole symbol group when any is already stored. Caught by the skeptic before commit | `tests/harness-book-sequences.test.ts` (`legacyPartialReimport`, `legacyTwoSellsReimport`), `tests/side-column.test.ts` |
| L-33 | 2026-09-25 | [LESSON] | A backfill must distinguish "no signal" from a value, or every later reader believes a guess: writing `'long'` on a same-day flat row with no note would have re-entered those rows into the stop-migration direction map that F3 had deliberately excluded. `backfillSide` returns null there; `sideOf` keeps the old default reading, `statedSideOf` says null | `tests/side-column.test.ts` "no signal, no side", `tests/trade-craft.test.ts` |
| L-34 | 2026-09-25 | [LESSON] | A source-scan guard needs an allow marker, or a builder routes around it: `sellQty >= buyQty` (a genuine MTF quantity test) was rewritten as `sellQty - buyQty >= 0` to get past the scan, which then could not see the next real direction read written the same way. The scan now matches the arithmetic, `!` and `?? 0` forms, skips only whole comment lines, and honours `// side-scan: quantity` | `tests/side-column.test.ts` (scan plants) |
| L-35 | 2026-09-25 | [LESSON] | Measure the whole CONSUMER set before choosing a lever: the KPI reduce the spec named cost 5 ms; the 350-410 ms was the whole-book read feeding three OTHER panels on the same page. Replacing the reduce alone would have saved 5 ms; replacing the read with an aggregate plus a narrow query saved ~340-390 ms. The old float reduce printed "-₹0" on a book netting exactly zero; the integer-paise aggregate prints "₹0" | `tests/trades-kpi-sql.test.ts`; DECISIONS 2026-09-25 W6 entry |
| F-25 | 2026-09-25 | [FIX] | (caught by the skeptic before commit) the per-leg legacy-short dedup missed a partial cover and a two-sells-one-buy short, so a pre-4.6 F&O file re-imported after W6 counted the sale twice; the whole symbol group is now refused when any row of the pre-W6 pairing is stored, in preview and commit | `tests/harness-book-sequences.test.ts` |
| F-26 | 2026-09-25 | [FIX] | (pre-existing, found by the W6 seam test) `lib/import/generic-map.ts` took a symbol's earliest and latest time-of-day across ALL fills, ignoring side and date, so a short's entry time was its cover; and the contract-note time enrichment in `commit.ts` took the first BUY fill as the entry regardless of side — both now read by side | `tests/parser-overnight-short-seam.test.ts`, `tests/side-sequences.test.ts` |
| F-27 | 2026-09-25 | [FIX] | (design review R-5) the "join the legacy pair" path did not exist: `isStaleLot` admitted a short lot only with `acquisition == null`, and a pre-4.6 opening-sell is stored `'unknown'`; Data Quality now lists the pair outside `NO_SHORT_SEGMENTS` and the join sets `acquisition = null`, `side = 'short'` and records the closed-short hash as an alias | `tests/side-column.test.ts`, `tests/harness-book-sequences.test.ts` |
| F-28 | 2026-09-25 | [FIX] | (design review R-8) the auto-close slice insert carried no side, so a same-day cover of a short lot derived `'long'`; the slice takes `side` from the plan | `tests/side-sequences.test.ts`, `tests/side-column.test.ts` |
| D-13 | 2026-09-25 | [DECISION] | W6 session decisions: `side` nullable, never `DEFAULT 'long'`; the column is read only on a FLAT row; no UI toggle (a flat row's side is fixed by editing legs: lopsided → quantities, flat with two dates → the date rule, same-day/undated flat → stored); a short exists only when covered in the same file; `shortable` = not `eq_*` by `classify()`; the legacy pair is joined through the existing confirm dialog (never automatically); the dedup hash is unchanged; a Dhan API overnight short across two daily pulls is recorded, not built; the speed lever is aggregate + narrow query (a cached stats row rejected); referral codes are per CREATOR (no buyer code on the card), a revoked key is not "active", an upgrade carries the ref; W7 not started (beside-W6 run left to the owner) | DECISIONS 2026-09-25 W6 entry |
| U-14 | 2026-09-25 | [UPGRADE] | W7 AIS purchase side split per ENTRY leg: `purchaseRows()` (lib/analytics/realised-rows.ts, pure) states one purchase per buy leg at the leg's own qty × price on the leg's own date (consideration, no charges, no average cost), settled to the parent's `buyValue`; `app/api/ais/route.ts` bumps each row in its own FY, so a ladder built across two FYs states its purchases in both; the IPO-link dedup stays keyed on the parent id; `getRealisedAndPurchaseRows()` is ONE staged-views batch for both sides | `tests/purchase-rows.test.ts`, `tests/ais-purchase-legs-db.test.ts`; the oracle purchase pins (49000/52000/51000) unchanged by construction |
| U-15 | 2026-09-25 | [UPGRADE] | W7 `/reports/harvest` STT split counts ONE trade per ladder per FY (distinct parent id; rupee amounts unchanged) and lists each ladder's fills beneath its half (date · qty · STT, grouped by fill leg AND date) in a native `<details>` — owner answer Q1 (one per ladder, fills beneath) | `tests/tax-levers.test.ts` |
| U-16 | 2026-09-25 | [UPGRADE] | W7 the LTCG grandfathering FMV editor lists ONE row per scrip (`grandfatherKey` = SYMBOL|ISIN, ISIN trimmed and upper-cased) for the tax person, the lots beneath; one save writes every eligible lot of that scrip through `POST /api/trades/fmv {ids, fmv, person}` (legacy `{id}` still accepted): one transaction, one audit row per trade, refused whole (400, no write) when an id is missing, outside the person's accounts, a second scrip, not a pre-2018 delivery/MTF lot, or when a blank would wipe a mixed group; the lots are the parents behind the REALISED rows, so a partly-sold pre-2018 ladder is editable at last | `tests/grandfather-groups.test.ts`, `tests/fmv-route.test.ts` |
| L-36 | 2026-09-25 | [LESSON] | A row derived from LEGS must first check that the legs still describe the PARENT: `setAcquisitionAction` (app/trades/actions.ts) rewrites buyQty/buyValue/buyDate/side on a staged row and leaves the legs (sell executions stored as entries), and a corporate-action split rescales the parent without them — the naive purchase split settled [15,000, −5,000] and stated a purchase in the SALE's FY; and a quantity + value check alone passes by coincidence at break-even, so the guard also requires the parent's buyDate to equal the date `parentAggregate` would write (first entry for a long, last cover for a short). Caught by `vyuha-design-reviewer` (R-1) and the skeptic (the break-even hole) | `tests/purchase-rows.test.ts` (basis-write, corporate-action, break-even cases) |
| L-37 | 2026-09-25 | [LESSON] | L-31 again on the tax page: `buyDate < GRANDFATHER_DATE` was a BYTE compare while every reader decides eligibility through `isGrandfatherEligible` → `normalizeDate`; a legacy `15-06-2019` row sorted below `2018-02-01` and was offered for an FMV no reader would apply, and `31-12-2017` was hidden. Every date order or eligibility decision goes through the helper; the pure `grandfatherLotsOf()` is the ONE selection | `tests/grandfather-groups.test.ts` ("15-06-2019" excluded, "31-12-2017" included) |
| L-38 | 2026-09-25 | [LESSON] | A grouped editor must not let an EMPTY input mean "clear all": a mixed group (lots with and without an FMV) opens blank, and a blank Save would have wiped the values that existed; "mixed" needs an explicit value (Save disabled, helper text) and the shown value is DERIVED (`edits[key] ?? initial`) with the editor re-keyed by person, so a person switch under `router.refresh()` cannot carry one person's typed value onto another's same-scrip group | `tests/grandfather-groups.test.ts` (editor render + source pins) |
| L-39 | 2026-09-25 | [LESSON] | A "fill" on a SHORT ladder is not one realised row: the realised rows of one cover carry the ENTRY tranches' dates (`realised-rows.ts` sellDate = entryDay when short), so grouping by `fillLegId` alone showed the first short-sale date at the whole cover quantity (and repeated a React key); group by (fillLegId, sellDate) | `tests/tax-levers.test.ts` (short-ladder fills) |
| L-40 | 2026-09-25 | [LESSON] | An unreadable leg date must be a GUARD FAILURE, never a fallback that cannot fire: `dayOf()` returns an unreadable string raw, so `?? parent.buyDate` never applied and `fyOfDate` either dropped the rupees (null FY) or invented FY 2000-01 ("Mar 5"); `purchaseRows` now uses `normalizeDate` and returns the parent whole when any purchase leg's date cannot be read | `tests/purchase-rows.test.ts` ("garbage" leg) |
| L-41 | 2026-09-25 | [LESSON] | Pin a residual to its CAUSE, not its size: the ~₹1 Upstox gap (engine Plus 227.56 vs the ledger's 226.57) is exactly the day's STT rounded once on the contract note (9.36 → 9) against per-row rounding (4+3+3 = 10), plus ₹0.01 of SEBI/GST paisa — and the describe comment had stated the reverse ("the ledger rounds per ORDER, the engine per position"); the new `it` derives the delta from the seed's `sttPct`, so it cannot agree with itself | `tests/broker-plan.test.ts` describe 16 |
| F-29 | 2026-09-25 | [FIX] | (design review R-1 + skeptic) the AIS purchase split trusted legs that no longer described the parent — after a basis write or a corporate-action rescale it would state a purchase in the sale's FY or drop it; `purchaseRows` splits only when Σ leg qty = parent.buyQty, |Σ r2(qty×price) − buyValue| ≤ 0.01·(n+1) AND the parent's buyDate is the aggregate's own leg date, else the parent row whole | `tests/purchase-rows.test.ts` |
| F-30 | 2026-09-25 | [FIX] | (skeptic) an unreadable purchase-leg date dropped that leg's rupees from AIS while the parent was still marked counted (the IPO allotment skipped) — now the parent row whole | `tests/purchase-rows.test.ts` |
| F-31 | 2026-09-25 | [FIX] | (skeptic) the harvest fill list grouped by `fillLegId` alone and showed a short cover as one line at the first tranche's date; grouped by (fillLegId, sellDate) | `tests/tax-levers.test.ts` |
| F-32 | 2026-09-25 | [FIX] | (skeptic, data loss) a blank Save on a MIXED FMV group would have cleared every lot's FMV, and one-shot `useState` would have carried a typed value across a person switch; Save disabled on blank+mixed, values derived, editor keyed by person | `tests/grandfather-groups.test.ts` |
| F-33 | 2026-09-25 | [FIX] | (skeptic) `grandfatherKey` did not trim or upper-case the ISIN, so " ine009a01021" and "INE009A01021" made two groups | `tests/grandfather-groups.test.ts` |
| F-34 | 2026-09-25 | [FIX] | (pre-existing, design review R-4/R-5) the FMV editor listed `closedTrades` by a byte compare against GRANDFATHER_DATE: a pre-2018 partly-sold ladder (whose realised rows the readers already price with the FMV) was never editable, a DD-MM-YYYY 2019 row was listed and a 2017 one hidden; the lots are now `grandfatherLotsOf(parents, realised)` through `isGrandfatherEligible` | `tests/grandfather-groups.test.ts` |
| D-14 | 2026-09-25 | [DECISION] | W7 session decisions: purchase consideration = leg qty × price (no charges, no average cost — AIS SFT-18 is per transaction) settled to the parent; the IPO dedup key stays the parent id; open ladders state purchases too; harvest counts distinct parents and lists only ladders (a flat trade is one row); fills = (fillLegId, sellDate) lines; ONE key function `grandfatherKey` for page and route; one FMV per scrip per TAX PERSON (not per account); the route refuses ineligible lots (a value no reader applies is a misstatement) but does not require `!isOpen`; "Trade not found" is now 400 (was 404; the only caller is the editor); the Upstox ~₹1 residual is ACCEPTED (owner answer Q2): no engine change, no Data Quality warning, the bound stated in broker-compare, the tax footnotes and the broker-compare help card ("per trade row, per fill on a ladder; a contract note rounds once"); NO migration (0078 unused); no e2e added. Recorded, not built: `setAcquisitionAction` has no `hasLadder` guard where the IPO push has one — a basis write on a staged row leaves legs contradicting the parent (candidate for the post-W7 audit fix wave) | DECISIONS 2026-09-26 W7 entry |
