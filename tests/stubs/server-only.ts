// Stub for the `server-only` package so engine/import modules can be unit-tested
// in the Node (Vitest) environment. In the real app, `server-only` guards these
// modules from being imported into client bundles.
export {};
